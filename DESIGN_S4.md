# 箱庭クロニクル — 同盟の深掘り 設計

`CLAUDE.md` の未実装#2「同盟の深掘り」（同盟名変更／同盟ミッション／加入AIの援軍／AI同士の戦争）の設計。

前提: 既存の規約を守る。バランス定数は `CONFIG`、静的定義は `S` に入れない、
派生値はキャッシュしない、`SAVE_VERSION` は非破壊マイグレーション。

---

## 0. 今の同盟に足りないもの

同盟は既に**5つの仕組み**を持っている。

| 既存 | 内容 |
|---|---|
| 勧誘 [S11] | AI勢力を仲間にする。成功率は城Lv差で決まる |
| 役職とバフ [S11] | 軍師/将軍/都督/斥候。研究時間・戦力・生産・行軍速度 |
| 忠誠と事件 [S19] | 均衡値へドリフト。45未満で事件、12未満で脱退 |
| 同盟倉庫 [S24] | 加入AIが毎分わずかに資源を納める |
| 盟主の下知 [S24] | 硬い目標を包囲して防御を最大45%削る |

**足りないのは「同盟が味方として動く姿」**。今あるのは全部
**プレイヤーが受け取る数値**（バフ・資源・防御ダウン）で、
同盟員が戦場に現れることが一度もない。チャットで喋るだけ。

> **設計の軸**: 同盟を「数値の供給源」から「**共に戦う相手**」にする。
> そのために最優先は **援軍**。他の3つは味付け。

---

## 1. 加入AIの援軍（★最優先）

### 1-1. なぜこれが最優先か

- AIの侵攻（[S12]）は既に**赤い線＋兵種アイコン**で見えるようになった。
  同じ画面に**味方の色の部隊**が現れれば、それだけで「同盟が居る」が成立する
- 描画は `drawMarchToken()` が**すでにある**。色を変えて呼ぶだけ
- 忠誠・役職という既存の軸に**もう一つ意味**を与えられる
  （忠誠が高いほど早く来る／強い）

### 1-2. 仕様

**プレイヤーが要請する**形にする。自動で来ると「何が起きたのか」が分からないため。

```
タイル詳細（自領）→「🤝 援軍を要請」
  ↓ 同盟員が1人選ばれ、その城から部隊が出る（マップに味方色の線＋⚔️）
  ↓ 到着すると、そのタイルに一定時間**駐留**する
  ↓ 駐留中はそのタイルの守りに援軍の戦力が乗る
```

| 項目 | 値 | 理由 |
|---|---|---|
| 同時に受けられる援軍 | 1件 | 下知と同じ「同時1件」の方針 |
| 要請の条件 | 加入AI 1人以上・忠誠50以上の者が居る | 忠誠の意味を増やす |
| 選ばれる同盟員 | **忠誠が最も高い者** | 「誰が応えてくれるか」が読める |
| 到着まで | 距離ベース（AIの侵攻と同じ式） | 既存の `AI_MARCH_TILES_PER_SEC` を流用 |
| 駐留時間 | 180秒 | 侵攻1〜2波を受け止める長さ |
| 守りへの寄与 | `AI城Lv × 220 × (忠誠/100)` | 城Lv10・忠誠80で 約1,760 |
| クールダウン | 240秒 | 常時貼れると防衛が作業になる |
| 費用 | 同盟倉庫から 食料 3,000 | 倉庫に使い道が増える |

- **要請できるのは自領のみ**。攻撃の援軍は下知（包囲）が既にあるので重複させない
- 到着前にそのタイルを奪われたら、援軍は**引き返す**（消える）。
  奪われた土地に味方が居座るのは筋が通らない
- 駐留中は `getPlayerTileDefense` に加算する。**`withTroopRatioPreserved` は不要**
  （プレイヤー部隊の最大兵力に触らないため）

### 1-3. 見え方

- マップに**同盟員の色**で線と⚔️トークン（`drawMarchToken` を色違いで呼ぶ）
- 到着時にトースト＋チャット（`onAidArrive` を Grok に発注）
- タイル詳細の守りの内訳に「🤝 ○○の援軍 +1,760（残り2分30秒）」

### 1-4. スキーマ

```js
S.alliance.aid = {
  aiId, targetIdx, departAt, arriveAt, expiresAt, power
} | null
S.alliance.aidCooldownUntil = 0
```

`shiftTimersForOffline` に3つの時刻を足すこと（離席で援軍が消えないように）。

---

## 2. AI同士の戦争

### 2-1. 何が起きるか

非同盟のAI同士が、**隣接する相手の資源タイルを奪い合う**。城は落とさない。

- 城を落とし合うと**勢力が勝手に消える**。プレイヤーが攻略対象を失い、
  制覇率の分母も動いて混乱する
- 資源タイルの奪い合いだけなら、**境界線が動く**という景色の変化に留まる

| 項目 | 値 |
|---|---|
| 発生 | AIの行動1回あたり 12%（攻撃・築城・拡張が成立しなかった時） |
| 対象 | 隣接する**他のAI**の資源タイル（城・要衝・プレイヤー領は除く） |
| 判定 | 攻撃側の城Lv > 防御側の城Lv なら奪う（同値は奪わない） |
| 同盟員 | **仲間は攻撃しない／されない**（＝加入すると守られる） |

### 2-2. 何が良くなるか

- **勧誘の価値が上がる**。仲間にすると、その勢力は他のAIから守られる
- 弱い勢力が削られ、**攻めやすい相手が自然に生まれる**
- 上級の14勢力が互いに削り合うので、終盤の掃討が単調にならない

### 2-3. 注意

- **通知は出さない**。自分に関係ない戦闘のトーストが流れると邪魔になる。
  記録（📜）にも残さず、マップの色が変わることだけで表現する
- 難易度バランスへの影響は小さいはず（プレイヤー領は対象外）だが、
  **実測でAI間の勢力図がどれだけ動くか確かめてから**数値を決める

---

## 3. 同盟ミッション

### 3-1. 仕様

同盟に**共通の目標**を出し、達成すると同盟倉庫に報酬が入る。

```js
const ALLY_MISSIONS = [
  { id: "capture",  text: "領地を{n}枚制圧せよ",       n: 5,  reward: { food: 20000, wood: 20000 } },
  { id: "castle",   text: "敵城を{n}つ落とせ",         n: 1,  reward: { iron: 30000 } },
  { id: "recruit",  text: "同盟員を{n}人に増やせ",     n: 3,  reward: { food: 30000, stone: 30000 } },
  { id: "defend",   text: "侵攻を{n}回撃退せよ",       n: 3,  reward: { stone: 25000 } },
  { id: "research", text: "研究を{n}件完了させよ",     n: 2,  reward: { wood: 40000 } },
];
```

- **同時1件**。達成すると次が出る（間隔を置く）
- 進捗は**既存の状態から算出できるものだけ**にする。
  「制圧5枚」は `ownedTiles.length` の差分で足りる。カウンタを新設しない方針
  → ただし「撃退3回」「研究2件」はカウンタが要る。**`S.alliance.mission.progress` を1つ持つ**
- 達成報酬は**同盟倉庫へ**入れる。個人の倉庫に直接入れると容量で溢れる

### 3-2. 何が良くなるか

- 「次に何をするか」が常に1行で提示される。中盤の目的の切れ目を埋める
- 同盟倉庫に**溜まる理由**が増える（今は加入AIの納入だけ）

---

## 4. 同盟名の変更

`S.alliance.name` は既にあり、初期値「無名同盟」。**入力させるだけ**。

- 同盟タブの見出しをタップ → 入力シート
- 12文字まで。`escapeHtml` を通して表示（既存のチャットと同じ扱い）
- 空文字は拒否。既定へ戻すボタンも置く
- チャットの `{ally}` 変数が既にあるので、**セリフにそのまま反映される**

**実装量は最小だが満足度は高い**。自分の同盟に名前を付けると、
チャットで同盟員が「{ally}の名に懸けて」と言い出す。

---

## 5. 実装順

| 順 | 内容 | 依存 | SAVE_VERSION |
|---|---|---|---|
| **5-1** | 同盟名の変更 | — | ✅ **完了**（変更なし） |
| **5-2** | **加入AIの援軍**（本命） | — | ✅ **完了**（29） |
| **5-3** | 同盟ミッション | — | ✅ **完了**（30） |
| **5-4** | AI同士の戦争 | — | 32 |
| **5-5** | 援軍・ミッションのセリフをGrokへ発注 | 5-2・5-3 | なし |

**5-1 を先に置くのは、小さくて確実に効くから**。
5-2 が本命なので、そこに時間を使えるよう軽いものを先に片付ける。

5-4 は**最後**。他の3つと違ってバランスに触るので、
先に入れると援軍やミッションの検証が濁る。

---

## 6. 確認していただきたい点

1. **援軍は「要請する」形でよいか**（§1-2）。自動で来る形にもできますが、
   何が起きたか分からなくなるので要請制を推しています
2. **援軍は防衛だけでよいか**。攻撃の援護は既に下知（包囲）があるので重複させない案です
3. **AI同士の戦争は資源タイルの奪い合いだけ**でよいか（§2-1）。
   城まで落とし合うと勢力が勝手に消え、攻略対象と制覇率の分母が動きます
4. **AI同士の戦闘は通知しない**でよいか（§2-3）。自分に関係ない戦闘の
   トーストが流れると邪魔になる、という判断です
5. 実装順は **名前 → 援軍 → ミッション → AI戦争** でよいか（§5）

---

## 7. 5-1 の実装結果（2026-08-07）

**`SAVE_VERSION` は上げなかった**。`S.alliance.name` は最初から存在するので、
スキーマは何も変わっていない（設計では29としていたが不要だった）。

### エスケープが必須になった

見出しは `<h3>🤝 ${S.alliance.name}</h3>` と**素で埋めていた**。
初期値が固定文字列だった間は問題なかったが、**ユーザー入力になった瞬間に穴になる**ので
`escapeHtml()` を通した。実測: `<img src=x onerror=...>` を入れても
img要素は生成されず、文字列として表示される。

チャット側は `chatFill` で埋めたあと `renderChatLog` が `escapeHtml` するので元から安全。

### 挙動

| | 結果 |
|---|---|
| 見出しをタップ | 入力シートが開く（現在名が入った状態で全選択） |
| 12文字超え | 12文字で切る |
| 空白のみ | 拒否してシートは開いたまま（見出しが消えるのを防ぐ） |
| 既定に戻す | 「無名同盟」へ |
| チャットの `{ally}` | 「今日から蒼天連合の一員。精進致します。」— 即反映 |

`{ally}` を含むセリフは7本（onJoin 2／onHolyWin 1／onPlayerSend.greet 1／
onPlayerSend.thanks 1／onIncident.idle 2）。**加入時と挨拶で必ず名前を呼ぶ**ので、
付けた名前が生きているのが分かる。

`allianceSignature()` に名前を含めた（含めないと次のtickで古い見出しに戻る）。

---

## 8. 5-2 の実装結果（2026-08-07）

SAVE_VERSION 29（設計では30としていたが、5-1 で上げなかったぶん1つ手前）。

### 設計どおりに入ったもの

| | 値 |
|---|---|
| 同時1件／自領のみ | ✅ |
| 応じる者 | 忠誠が最も高い同盟員（実測: 忠誠45/82/61 のうち82の者が選ばれる） |
| 守り | `AI城Lv × 220 × (忠誠/100)`。城Lv10・忠誠82で **1,804** |
| 到着 | 距離ベース（AIの侵攻と同じ式）。実測 14マスで76秒 |
| 駐留 | 180秒 |
| クールダウン | 240秒 |
| 費用 | 同盟倉庫の食料3,000 |

### 実測

- **到着前は守りに乗らない**（360のまま）。到着した瞬間に 360→2,164
- 二重要請・クールダウン中・忠誠不足・倉庫不足はすべて理由付きで弾く
- **奪われたら引き返す**（「守るべき土地が失われた」）
- **応じた勢力が同盟を抜けても引き返す**（「応じた勢力が同盟を離れた」）
- 離席60秒で `departAt`/`arriveAt`/`expiresAt` が**3つとも同じだけ**動く。
  1つでも忘れると到着済み扱いで即引き揚げになるので、ここは必ず3つ揃える
- 援軍のいるタイルは 2,108 に対し他は304〜360。**AIは守りの薄い順に狙う**ので、
  援軍を置いた場所は自然に避けられる＝「守りたい所へ呼ぶ」が機能する

### 見え方

- マップに**同盟員の色の点線＋🤝トークン**。自分の出兵（金の破線）とも
  敵の侵攻（赤の実線）とも線種で区別できる
- 駐留中はマスの上に🤝を留める（部隊の駐留マークと重ならない位置）
- タイル要約に「到着まで33秒」／「残り3分19秒」を出し分ける

### チャット

`onAidSend` / `onAidArrive` のフックを置いた。**プールは空**なので、
届くまでは静かに何も喋らない（`chatEvent` は空プールで即returnする）。5-5でGrokへ発注する。

---

## 9. 5-3 の実装結果（2026-08-07）

SAVE_VERSION 30。目標5種を順に1件ずつ出し、達成で同盟倉庫へ報酬が入る。

### 進捗の数え方は種別で分けた

設計では「取れるものは状態から算出」としたが、**実際に算出できたのは `recruit` だけ**だった。

| 種別 | 数え方 |
|---|---|
| `recruit` | **状態から算出**（`allianceAIs().length`）。カウンタ不要 |
| 他4種 | カウンタ（`mission.progress`） |

`capture` を「領地数の差分」で測る案は捨てた。**奪われた時に進捗が巻き戻って理不尽**になる。
「開始してから何回やったか」は状態から復元できないので、ここは素直にカウンタを持つ。

加算は `bumpMission(id)` の1本に集約し、**中で種別を照合する**ので
呼び出し側（制圧・撃退・研究）は何も判断しなくてよい。

### 報酬は実際に入った量を出す

倉庫は容量で頭打ちになる（既存の納入と同じ挙動）。名目値をトーストに出すと嘘になるので、
**差分を測って表示する**ようにした。実測: 余裕があれば「🪵40.0k を同盟倉庫へ」、
残り5,000しかなければ「🪵5,000 を同盟倉庫へ（容量いっぱいで一部は入りませんでした）」。

**加入者ゼロだと同盟倉庫の容量は0**なので、その時だけ自分の倉庫へ入れる。
入れないと報酬がそのまま消える。

### 実測

| | 結果 |
|---|---|
| 指南中 | 目標を出さない（`isGuiding()` で早期return） |
| capture | 資源タイルの制圧で+1。**城を落とした時は増えない**（castle側で数える） |
| castle / defend / research | それぞれ+1 |
| recruit | 2人加入で 2（カウンタ不使用） |
| 達成 | 報酬→ログ→120秒の間隔→次の目標 |
| 連続8回 | 前回と同じものは出ない |
| 離席30秒 | `nextAt` が同じだけ動く |
| 同盟タブ | 4秒間で再描画0回。進捗が動いた時だけ1回組み直す |

`allianceSignature()` に目標の進捗を含めた。残り時間は**10秒刻みに丸めて**入れてある
（毎秒入れると全再構築が毎秒走り、ガチャタブで起きた問題を繰り返す）。

### チャット

`onMissionDone` のフックを置いた。プールは空なので届くまでは静か。5-5でGrokへ発注する。
