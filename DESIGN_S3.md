# 箱庭クロニクル — 第三次拡張 設計（難易度・築城・編成）

異民族討伐までクリアした実機フィードバック（2026-07-31）を受けた設計。
**「ノーマルをクリアした人が上級で遊ぶときの没入感」** を軸に、
上級でしか成立しない攻防を作ることを目的にする。

前提: 既存の規約（`CLAUDE.md`）を守る。バランス定数はすべて `CONFIG`、
静的定義は `S` に入れない、派生値はキャッシュしない、`SAVE_VERSION` は非破壊マイグレーション。

---

## 0. 今回の範囲

| # | 項目 | 出どころ |
|---|---|---|
| 1 | 難易度選択（ノーマル／上級） | 異民族の波・攻撃戦力・AI勢力数 |
| 2 | 異民族編の**開始許諾ダイアログ** | 「対策してから挑めるように」 |
| 3 | マップ施設の拡張（**柵・駐屯所・櫓**）＋研究ツリー連携 | 「幕舎より防御力がある駐屯所、櫓」 |
| 4 | **兵舎Lv20で第4部隊** | 「20レベルで上級用の一部隊増設」 |
| 5 | **部隊編成のドラッグ＆ドロップ**（主将・副将） | 「知将を編成しやすく」 |
| 6 | 異民族AIも施設を建てる | 「敵のAIもそれらの施設を建てられる」 |

**すでに反映済み**（このドキュメントの対象外）:
育成の＋でスクロールが飛ぶ問題、レベル上限の赤表示、ピンチイン／ピンチアウト。

---

## 1. 難易度選択

### 1-1. どこで選ばせるか

新規ゲーム開始時に一度だけ。`S.difficulty`（`"normal" | "hard"`）に保存し、
途中変更はできない（途中で上げ下げできると、積み上げた防御の意味が壊れるため）。

既存セーブは `migrateSave` で `"normal"` を入れる。**ノーマルの数値は現行と完全に同一**にして、
今遊んでいるセーブの手触りが変わらないことを保証する。

### 1-2. プリセット

`CONFIG.DIFFICULTY` に束ねる。ゲーム側は `diff("AI_COUNT")` のような
1つの参照関数を通して読む（`CONFIG` を直接書き換えると規約2に反するため）。

| 項目 | ノーマル（現行） | 上級 |
|---|---|---|
| AI勢力数 | 10（北東4／北西4／南2） | **14**（北東5／北西5／南4） |
| AIの攻撃確率 | 拡0.45／均0.20／守0.05 | **拡0.60／均0.32／守0.10** |
| AIの同時侵攻 | 3 | **4** |
| 異民族 第1波 | 3勢力・城Lv12 | **3勢力・城Lv14** |
| 異民族 第2波 | 3勢力・城Lv14 | **4勢力・城Lv16** |
| 異民族 第3波 | 2勢力・城Lv16 | **6勢力・城Lv18** |
| 異民族の攻撃補正 | ×1.4 | **×1.8** |
| 異民族の同時侵攻 | 5 | **7** |
| 第3波の出現方向 | 北のみ | **東西南北** |

異民族は計 8勢力 → **13勢力**。

### 1-3. 攻撃戦力の実測（`180 × 城Lv^1.3 × 補正`）

| 城Lv | 素 | ノーマル ×1.4 | 上級 ×1.8 |
|---|---|---|---|
| 12 | 4,552 | 6,373 | 8,194 |
| 14 | 5,562 | 7,787 | **10,012** |
| 16 | 6,617 | 9,263 | **11,910** |
| 18 | 7,711 | 10,796 | **13,880** |

> **★確認したい点**: 「攻撃戦力は1K越えくらいが丁度良い」を
> **「1万越え」** と解釈しました。現行ノーマルが既に 6,373〜9,263 なので、
> 1,000 だと今より弱くなってしまうためです。
> この解釈だと**上級は第1波から1万越え**（10,012）で始まり、第3波で 13,880 になります。
> もし「もっと控えめ／もっと苛烈」であれば補正値だけ変えれば済みます。

### 1-4. プレイヤー側の防御と噛み合うか

現行の自領防御（`(300 + 本城Lv×160 + 兵舎Lv×120 + 施設) × 城塞普請1.30`）:

| 状態 | 防御 | 上級で耐えられる波 |
|---|---|---|
| 本城15・兵舎15・施設なし | 5,850 | **どの波も抜かれる** |
| 本城20・兵舎20・施設なし | 7,670 | どの波も抜かれる |
| 本城20・兵舎20・防衛施設 | 10,920 | 第1波は止まる／第2波で抜かれる |
| ＋ 駐屯所・櫓（§3） | **約14,000** | 第3波（13,880）を辛うじて止める |

**上級は §3 の新施設を建てないと守り切れない**という関係になる。
これが「上級で遊ぶときの没入感」の中身になる。ノーマルは現行のまま素直に勝てる。

### 1-5. 第3波を東西南北から出す

現在の出現は `TRIBE_SPAWN_MAX_Y: 3`（北端のみ）に固定されている。
波ごとに **出現辺** を持たせる形に一般化する。

```js
TRIBE_WAVES: [
  { count: 3, castleLv: 14, edges: ["n"] },
  { count: 4, castleLv: 16, edges: ["n", "e"] },
  { count: 6, castleLv: 18, edges: ["n", "e", "s", "w"] },   // 上級のみ四方
],
TRIBE_SPAWN_DEPTH: 4,   // 各辺からこの距離までを出現候補にする
```

南下ロジック（`aiExpand` で異民族だけ「皇都に最も近い候補」を選ぶ）は
方向に依存していないので、**そのまま四方から皇都へ収束する**。ここは変更不要。

---

## 2. 異民族編の開始許諾

### 2-1. 現状の問題

`stageTick` が第二ステージに入れた次の `tick` で、`tribeTick` が第1波を即座に湧かせる。
建物レベル上限20が解放された瞬間に襲来が始まるので、**準備する時間がない**。

### 2-2. 分ける

| 段階 | 条件 | 内容 | 許諾 |
|---|---|---|---|
| 第二ステージ解放 | 皇都制圧＋本城Lv10 | 建物レベル上限 20 | 自動（現行どおり） |
| **異民族編の開始** | 上記の後、プレイヤーが選ぶ | 波の襲来が始まる | **ダイアログでOKしたら** |

上限20の解放を先に渡し、**育ててから自分のタイミングで宣戦する**形になる。

### 2-3. ダイアログ

`openConfirmSheet(title, bodyHTML, okLabel, onOk)` を新設（現在 confirm系のヘルパーが無い）。
`showSheet(html, "normal")` の上に載せるだけなので、既存のシート機構を流用できる。

> **🐺 北の草原に異民族の兵が集まっている**
>
> 討伐編を始めますか？ 始めると **3波・計13勢力**（上級）が
> 皇都を目指して押し寄せます。**皇都を保持したまま全勢力を討伐**すれば
> あなたの勝ちです。
>
> ・第1波 城Lv14 × 3勢力（攻撃戦力 約10,000）
> ・第2波 城Lv16 × 4勢力（約11,900）
> ・第3波 城Lv18 × 6勢力（約13,900・**東西南北から**）
>
> 一度始めると引き返せません。防衛施設と駐屯所を整えてから挑むことを勧めます。
>
> 　[ ⚔️ 討伐編を始める ]　[ まだ準備する ]

- 断ったら `S.tribe.offeredAt` を立て、**二度と自動では出さない**（毎tick出ると邪魔）
- 勝利バナーに常設の「⚔️ 討伐編を始める」ボタンを出し、好きな時に始められるようにする
- 数値はダイアログ内で `CONFIG` から生成する（難易度によって表示が変わる）

---

## 3. マップ施設の拡張（柵・駐屯所・櫓）

### 3-1. 役割の分担

既存の2種（幕舎・防衛施設）に3種を足し、**「面で薄く硬くする／前線を硬くする／削る」**
の3方向に分ける。数値だけ違う施設を並べても選択にならないため。

| 施設 | 性格 | 自タイル防御 | 周囲 | 迎撃 | 駐留 | 建設上限 |
|---|---|---|---|---|---|---|
| **柵** 🪵 | 面を薄く硬く | +250 | — | — | 不可 | 4 + 本城Lv×0.5 |
| 幕舎 ⛺（既存） | 出撃拠点 | +400 | — | — | 可 | 2 + 本城Lv×0.2 |
| **駐屯所** 🏕 | 硬い前線拠点 | +1,200 | — | — | **可・防御寄与0.8** | 2 + 本城Lv×0.15 |
| **櫓** 🗼 | 着弾前に削る | +800 | +400／範囲3 | 射程4・固定800 | 不可 | 2 + 本城Lv×0.15 |
| 防衛施設 🏯（既存） | 拠点防衛 | +2,500 | +900／範囲2 | 射程3・固定1,200 | 不可 | 2 + 本城Lv×0.2 |

- **駐屯所は幕舎の上位互換**にする。防御が3倍になるだけでなく、
  駐留部隊の戦力が防御に乗る割合を `GARRISON_DEF_RATIO 0.5 → 0.8` に上げる。
  「兵を置いて守る場所」として幕舎と役割が変わる
- **櫓は迎撃専門**。射程4は弓兵の駐留迎撃と同じで、**着弾前に固定800を削る**。
  複数建てれば削りが累積するので、通り道に並べる意味が出る
- **柵は安くて数が置ける**。単体では 250 しか増えないが、
  資源タイルの列に並べると上級AIの「資源を奪いに来る」動きへの初期対応になる

### 3-2. 費用と解禁

| 施設 | 解禁 | 費用 |
|---|---|---|
| 柵 | 研究「柵塁」 | 🪵1,500 ／ 🪨800 |
| 駐屯所 | 研究「駐屯制」 | 🪵25,000 ／ 🪨20,000 ／ ⚙️10,000 |
| 櫓 | 研究「望楼術」 | 🪵30,000 ／ 🪨25,000 ／ ⚙️20,000 |

### 3-3. 研究ツリーに **築城** 枝を足す

`RESEARCH_BRANCHES` は配列なので、4本目を足すだけで既存UIに乗る。
軍事枝（兵法→甲冑改良→城塞普請）は既に3段あるので、そこへ足すと縦に伸びすぎる。

```js
{ id: "palisade", name: "柵塁",   icon: "🪵", branch: "築城", labLv: 2,  requires: null,
  desc: "マップに柵を建てられるようになる", effect: { unlock: "palisade" },
  cost: { wood: 6000, stone: 4000 }, sec: 300 },
{ id: "garrison", name: "駐屯制", icon: "🏕", branch: "築城", labLv: 7,  requires: "palisade",
  desc: "駐屯所を建てられる／駐留部隊の防御寄与 +60%", effect: { unlock: "garrison_post", garrisonDef: 1.6 },
  cost: { wood: 60000, stone: 50000, iron: 30000 }, sec: 1500 },
{ id: "watchtower", name: "望楼術", icon: "🗼", branch: "築城", labLv: 11, requires: "garrison",
  desc: "櫓を建てられる／迎撃で削る量 +25%", effect: { unlock: "tower", intercept: 1.25 },
  cost: { wood: 90000, stone: 90000, iron: 60000 }, sec: 2400 },
```

研究所は第二ステージでLv20まで伸びるので、labLv11 は上限20の解放とちょうど噛み合う。
`effect.unlock` は既存の数値バフと形が違うので、`researchMult()` は触らず
`hasResearch(id)` の判定で扱う（数値バフの合成に混ぜない）。

### 3-4. 実装の載せ方

**新しい配列は作らない**。既存の `S.player.outposts` / `S.player.forts` と同じ形の
`S.player.structures`（`{ id, kind, ti }`）に一本化し、幕舎・防衛施設も
マイグレーションでここへ移す……のは**やらない**。既存の2つは動いているので触らず、
`S.player.structures` を**新3種の置き場として追加**するだけにする。

`getPlayerTileDefense` に足すのは1行:

```js
const struct = getStructureDefAt(ti);        // 柵・駐屯所・櫓の自タイル防御
... + outpost + fort + struct + garrison + getFortAuraDef(ti) + getStructureAuraDef(ti)
```

迎撃も既存の `applyIntercept(m)` の中で櫓を足すだけで済む。

---

## 4. 兵舎Lv20で第4部隊

```js
SQUAD_UNLOCK_BARRACKS_LV: [1, 5, 10, 20],   // 現行 [1, 5, 10]
```

兵舎Lv20は第二ステージでしか到達できないので、**自動的に「異民族編の報酬」**になる。
難易度で分けず、ノーマルでも取れるようにする（Lv20到達自体が十分な条件のため）。

注意点:
- `ensureSquads()` / `getSquadSlots()` が配列長を見ているので、定数を伸ばすだけで解禁される
- 第4部隊が増えると**カードの取り合い**が起きる。`getSquadCards` の自動編成は
  「自分より前の部隊が取った分」を除外する実装なので、枠が増えても破綻しない
- 上級の同時侵攻4に対して守り3部隊では足りないので、**4部隊目は上級の必需品**になる

---

## 5. 部隊編成のドラッグ＆ドロップ（主将・副将）

### 5-1. 現状

カードを**単タップで追加、ダブルタップで詳細**。編成順は `sq.cardUids` の配列順だが、
UIからは順序を意識できず、**兵種は多数決**（`getSquadTroop`）で決まる。
知将を入れたい／外したいという操作が、リストの中から探して押す形になっている。

### 5-2. 変えること

`sq.cardUids` は**すでに順序付き配列**なので、データモデルの変更は要らない。
枠に名前を付けて、並べ替えられるようにするだけで済む。

```
┌ 第1部隊 ────────────────────────┐
│ [主将]  [副将]  [兵1]  [兵2]  [兵3] │   ← ここへドラッグして入れ替える
└──────────────────────────────┘
   ↓ 控えの武将（タップでも入る・従来どおり）
```

| 枠 | 効果 |
|---|---|
| **主将**（slot 0） | **部隊の兵種を決める**（多数決をやめる）。必殺技は従来どおり全員ぶん乗る |
| **副将**（slot 1） | 知将を置くと分かるよう印を出すだけ（数値効果なし） |
| 兵1〜3 | 従来どおり |

**兵種を主将で決める**のが実質的な変更点。現在は多数決なので、
知将を混ぜると狙わない兵種に化けることがある。主将で決まれば
「騎兵の主将＋知将の副将」のような意図した編成が組める。

- **入替はドラッグ＆ドロップ**（`pointerdown/move/up`。城タブの `cityAttachDrag` と同じ作法）
- **タップだけでも完結させる**（iPhoneでD&Dが滑る場面の保険。「主将にする」ボタンを長押しメニューに）
- **数値バフは付けない**。主将に統率ボーナス等を付けると最大兵力が動き、
  `withTroopRatioPreserved()` の対象が増えてバランス検証が要る。今回はUIと兵種決定に留める

> **★確認したい点**: 主将に統率ボーナス（例 ×1.15）を付けるかどうか。
> 付けると「誰を主将にするか」に数値の意味が出ますが、
> 最大兵力が動くので `withTroopRatioPreserved()` の検証が要ります。
> 今回は**付けない**前提で設計しています。

---

## 6. 異民族AIも施設を建てる

「敵の異民族AIもそれらの施設を建てられる」への対応。

AI側にプレイヤーと同じ建設UIは無いので、**行動の一種として持たせる**。
`aiExpand` と同じ `aiTick` の枠に `aiFortify(ai)` を足す。

```js
// 異民族は南下の途中に柵と櫓を残す。取り返しに行くと硬い＝押し返しが重くなる
CONFIG.TRIBE_FORTIFY_CHANCE: 0.30,     // 行動1回あたり
CONFIG.TRIBE_FORTIFY_MIN_TILES: 3,     // 3タイル以上持ってから
```

- 建てるのは**柵と櫓のみ**（駐屯所は駐留の仕組みが要るのでAIには持たせない）
- 置き場所は「自分の領地のうち、**皇都に最も近いタイル**」＝進軍路を固める動きになる
- 効果は `getTileDefense` 側で AI領にも施設ぶんを足す（プレイヤーが攻める時に効く）
- **上級のみ**にする。ノーマルで押し返しが重くなると、今の手触りが変わってしまうため
- マップ描画は既存のタイル上バッジに🪵/🗼を足すだけ

これで「奪われた土地を取り返すのが、奪われる前より重い」という圧が出る。

---

## 7. 実装順

一度に一機能の原則で積む。**上へ行くほど独立していて、下へ行くほど上に依存する。**

| 順 | 内容 | 依存 | SAVE_VERSION |
|---|---|---|---|
| **7-1** | 難易度選択（`S.difficulty`＋`diff()`＋新規ゲーム時の選択） | — | ✅ **完了**（22） |
| **7-2** | 異民族編の開始許諾ダイアログ | 7-1（表示する数値が難易度依存） | ✅ **完了**（23） |
| **7-3** | 築城枝＋柵・駐屯所・櫓 | — | ✅ **完了**（変更なし） |
| **7-4** | 兵舎Lv20で第4部隊 | — | ✅ **完了**（変更なし） |
| **7-5** | 異民族AIの築城 | 7-1・7-3 | ✅ **完了**（24） |
| **7-6** | 編成のドラッグ＆ドロップ（主将・副将） | — | ✅ **完了**（変更なし） |

7-3 と 7-6 は他と独立しているので、先にやりたい方から着手できる。

---

## 8. 確認していただきたい点

1. **「攻撃戦力1K越え」= 1万越え** という解釈でよいか（§1-3）。
   現行ノーマルが既に 6,373〜9,263 なので、1,000 だと弱くなってしまうためこう読みました
2. **上級は §3 の新施設を建てないと守り切れない**、という噛み合わせでよいか（§1-4）
3. **異民族編を「宣戦してから始まる」**形にしてよいか（§2）。
   建物Lv20の解放は従来どおり自動で、襲来だけ許諾制にする案です
4. 新施設は **柵・駐屯所・櫓の3種**、研究の **築城枝** で解禁、でよいか（§3）
5. **主将に数値ボーナスを付けない**（兵種決定のみ）でよいか（§5-2）
6. 異民族AIの築城は **上級のみ・柵と櫓だけ** でよいか（§6）

---

## 9. 7-1 の実装結果（2026-07-31）

「攻撃戦力1K越え」は **10K＝10,000** で確定（§1-3の解釈どおり）。

### 束ねた定数は CONFIG 直下から取り除いた

`CONFIG.DIFFICULTY.normal` / `.hard` に移し、**同名の定数を CONFIG 直下に残さなかった**。
残すと「どちらが効いているのか」がコードから読めなくなるため。
参照は `diff("KEY")` だけ。実測で `CONFIG.AI_COUNT` 等の残存参照は0件。

**ワールド生成中は `S` がまだ組み立てられていない**ので `diff()` が使えない。
`generateAIUsers(world, difficulty)` として引数で受け取る（呼び出しは `newGameState` の1箇所だけ）。
`boot()` は `newGameState(await chooseDifficulty())` の形で、**選び終わってから世界を生成する**。

### 実測（ノーマルは実装前と完全に一致）

| | ノーマル | 上級 |
|---|---|---|
| AI勢力 | 10（北西4／北東4／南2） | 14（5／5／4） |
| 攻撃確率 | 拡0.45／均0.20／守0.05 | 拡0.60／均0.32／守0.10 |
| 同時侵攻 | 3〜5 | 4〜7 |
| 第1波 | 3勢力・北・6,373 | 3勢力・北・**10,012** |
| 第2波 | 3勢力・北・7,787 | 4勢力・**北2/東2**・**11,910** |
| 第3波 | 2勢力・北・9,263 | 6勢力・**北2/東2/南1/西1**・**13,880** |
| 異民族合計 | 8 | 13 |

出現辺は `TRIBE_WAVES[].edges` で持ち、`isOnSpawnEdge()` が判定する
（`TRIBE_SPAWN_MAX_Y` の北端固定は廃止）。勢力数が辺の数で割り切れない時は
辺を順番に使い切るので偏りが最小になる（6勢力→2/2/1/1）。

### 途中で気づいて直したこと

- **`AI_NAMES` / `AI_COLORS` が10個しかなかった**。上級では4勢力が
  「AI10」「AI11」…という仮名＋灰色 `#888` で並び、勢力の見分けが付かなかった。
  14個に拡張し、追加分は既存10色＋異民族色から **RGB距離85以上**離れた
  色相の空き（78°／177°／240°／300°）から選んだ。
  16色全体の最小距離は52で、これは**元からある** `#8e7cc3↔#76a5af` の組なので悪化していない
- 難易度選択の画面だけ `fmt()` を使わず区切り付きの実数にした。
  `fmt` は1万以上を「13.9k」に丸めるため、「9,263 と 13.9k」では桁の比較ができない

### 移行

`v21 → v22` は `difficulty` の補完のみ。**既存セーブは必ずノーマル**に倒れ、
ノーマルの数値は実装前と同一なので手触りは変わらない（実測で確認済み）。

---

## 10. 7-3 の実装結果（2026-07-31）

### 設計から変えたこと: 新しい配列を作らなかった

§3-4 では `S.player.structures` を新設する案にしていたが、**やめた**。
既存の `S.player.outposts[]` は最初から `kind`（`"camp"` / `"fort"`）で種別を持っており、
ここに3種を足すだけで `getStructAt`（重複建設の防止）・`removeStruct`・
`releaseLostGarrisons`（奪われた土地の施設を失う）が**そのまま効く**。
配列を分けると、この3つを両方に書くことになる。

結果として **`SAVE_VERSION` の変更が不要**になった。`kind` に新しい値が入るだけで、
スキーマは変わらない。`kind` の無い v14以前のデータが幕舎として扱われる互換も維持している
（旧データ3種で実測: kind無し→幕舎 / camp→幕舎 / fort→防衛施設）。

### 5種を1つの表にまとめた

`STRUCT_DEFS` に幕舎・防衛施設も含めて集約し、値は `CONFIG` からそのまま引いている。
**既存2種の数値が変わっていないことを11項目で突き合わせ済み**（不一致0）。

| 施設 | 自タイル | 周囲 | 迎撃 | 駐留 | 上限(本城20) |
|---|---|---|---|---|---|
| 🧱 柵 | +250 | — | — | — | 14 |
| ⛺ 幕舎 | +400 | — | — | 0.5 | 6 |
| 🛖 駐屯所 | +1,200 | — | — | **0.8** | 5 |
| 🏹 櫓 | +800 | +400／範囲3 | −800／射程4 | — | 5 |
| 🗼 防衛施設 | +2,500 | +900／範囲2 | −1,200／射程3 | — | 6 |

絵文字はタイル種別（🌾🪵🪨⚙️⛩️🏯⛰️🌊⚓）と重ならないものを選んだ。
マップではタイルの上に重ねて描くため、被ると地形と施設の区別が付かなくなる。

### 実測

- 自タイル防御: 柵+250／幕舎+400／駐屯所+1,200／櫓+800（設計どおり）
- オーラ: 防衛施設900 に櫓400 が**重なって1,300**
- 迎撃: 防衛施設1,200＋櫓800 = **2,000 を着弾前に削る**
- 駐留比率: 幕舎0.5 → 駐屯所0.8。差937 = 施設差800 ＋ 駐留差137
- 解禁: 研究前は「研究「駐屯制」で解禁されます」等を返し建設不可

### §1-4 の噛み合わせ（上級・本城20兵舎20・研究フル）

| 積み上げ | 守り |
|---|---|
| 素の自領 | 7,384 |
| ＋防衛施設 | 10,634 |
| ＋櫓×5 | 12,714 |
| ＋周囲の防衛施設×5 | **16,224** |

上級の波は 10,012 / 11,910 / 13,880。**新施設なしでは第2波で抜かれる**（10,634 < 11,910）ので、
「上級は築城しないと守り切れない」という設計の狙いは成立している。

ただし**守りを固めた1枚の数値は意味を持たない**ことに注意。AIも異民族も
`aiAttack` で**守りが最も薄いタイル**を狙うので、効くのは最弱タイルの値。
上限（柵14／幕舎6／駐屯所5／櫓5／防衛施設6）があるため領地全体は固められず、
「どこを捨ててどこを固めるか」が上級の判断になる。

なお最大構成では**迎撃だけで10,000を削る**（櫓5×800＋射程内の防衛施設）。
拠点周辺は実質無傷になるが、これも「固めた場所は狙われない／薄い所を突かれる」で釣り合う。

### 途中で直したこと

- **`.stack-btn` でボタンが縦潰れした**（±ボタン用の30px固定幅クラス）。`.train-btn` に変更。
  [S26]の育成ボタンで一度踏んだのと同じ罠
- **幕舎の枠が配列全体の長さで数えられていた**（既存のバグ）。防衛施設を建てるほど
  幕舎の枠が減っていた。`countStructs(kind)` で種別ごとに数えるよう修正
- 呼び出しの無くなった `getOutpostAt` / `getFortAt` / `getFortAuraDef` / `getOutpostMax` /
  `getFortMax` と旧 `UIA.buildOutpost` / `buildFort` を削除。残すと「駐留できる施設か」を
  判定したい所で `getOutpostAt` を使って駐屯所を取りこぼす（過去に同種の事故がある）
- `getResearchBuffs` は effect の値を掛け合わせるので、`unlock:"palisade"` のような
  **非数値を弾く**必要があった（混ざると倍率が NaN になる）

---

## 11. 7-6 の実装結果（2026-07-31）

設計どおり **`SAVE_VERSION` 変更なし**。`sq.cardUids` は元から順序付き配列なので、
枠に名前を付けて並べ替えられるようにしただけで、データモデルは一切変えていない。

### 主将が兵種を決める

```js
function getSquadTroop(squadId) {
  for (const c of getSquadCards(squadId)) {
    const d = CARD_BY_ID[c.defId];
    if (d && d.troop) return d.troop;      // 主将＝先頭。持たない時だけ後続で埋める
  }
  return null;
}
```

実測: 騎兵/知将(弓)/歩兵/弓/兵器 の編成で、旧・多数決なら **archer**（弓が2枚）だが
新・主将基準では **cavalry**。主将を入れ替えると兵種も追従する（siege に交換して確認）。

> **既存セーブへの影響**: 手動編成の並びは「追加した順」なので、
> **読み込んだ時点で主兵種が変わる部隊がありうる**。
> 自動編成は戦力の降順なので主将＝最強カードになり、従来の多数決とほぼ一致する。
> 主兵種の表示に「（主将）」を添え、帯で並びが一目で分かるようにしてある。

### 操作は2通り

- **ドラッグ**: `pointerdown/move/up` ＋ `setPointerCapture`（城タブの `cityAttachDrag` と同じ作法）。
  移動中は `document.elementFromPoint` で落下先の枠を探して `.over` で光らせる
- **タップ2回**: 1枠タップ→もう1枠タップで交換。
  **iPhoneで指が滑った時に並べ替え不能にならないための保険**。同じ枠を2度押すと選択解除

### 実測した挙動

| | 結果 |
|---|---|
| タップ2回で交換 | ✅ 主将↔副将で兵種が cavalry → archer |
| ドラッグで交換 | ✅ 兵三→主将で兵種が siege に |
| 枠の外に落とす | ✅ 何も起きない |
| 空き枠と交換 | ✅ 「そこは空き枠です」で拒否 |
| 自動編成のまま並べ替え | ✅ `materializeAutoSquad` で手動編成に固定（cardUids 0→5枚） |
| 行軍中の部隊 | ✅ 「行軍中の部隊は編成を変えられません」 |
| ドラッグ中断（pointercancel） | ✅ ハイライトが残らない |

### 設計どおり見送ったもの

**主将に数値ボーナスは付けていない**（§5-2の確認事項5）。付けると最大兵力が動き、
`withTroopRatioPreserved()` の対象が増えてバランス検証が要る。
並べ替えだけなら統率の合計は変わらないので、この包みは不要のまま済んでいる。

---

## 12. 7-2・7-4 の実装結果（2026-08-01）

### 7-2 異民族編の開始許諾（SAVE_VERSION 23）

段階を2つに割った。**建物レベル上限20の解放は従来どおり自動**で、
**襲来だけプレイヤーが宣戦してから**始まる。

| 関数 | 役割 |
|---|---|
| `canStartTribes()` | 第二ステージ かつ `wave===0` かつ未討伐 |
| `startTribeInvasion()` | 第1波を湧かせる本体。失敗理由 or null |
| `offerTribeStart()` | 確認ダイアログ。`S.tribe.offeredAt` を立てる |

**打診は自動で1度だけ**。断っても `#winBanner` に常設した
「🐺 討伐編を始める」からいつでも始められるので、導線が消えない。

- **他のシートが開いている間は割り込まない**（次の tick で出す）。
  実測で、初回起動時は初陣指南のシートが開いているため打診が1tick遅れることを確認した
- ダイアログの波の構成・攻撃戦力は `diff("TRIBE_WAVES")` から生成する。
  数値を文面に直書きすると難易度で嘘になる

`openConfirmSheet(title, bodyHTML, okLabel, onOk, cancelLabel)` を新設した。
`window.confirm` は見た目が浮くので使わず、既存のシート機構に載せている。
`closeSheet()` で `_confirmOk` を捨てる＝閉じたら「やめておく」と同じ扱いになる。

#### 移行（v22 → v23）

```js
if (data.tribe && data.tribe.offeredAt === undefined) {
  data.tribe.offeredAt = data.tribe.wave > 0 ? (data.tribe.startedAt || Date.now()) : null;
}
```

**すでに襲来が始まっているセーブを打診済み扱いにする**のが要点。
これが無いと、進行中のプレイヤーが「討伐編を始めますか？」と訊かれる。

実測: `wave:2` の v22 セーブ → `wave:2` のまま `offeredAt` が入り、10回tickしても訊かれない。
`wave:0` の v22 セーブ → `offeredAt:null` で読み込まれ、tick で打診される。

> **検証時の注意**: 10秒ごとの自動保存が走るので、**細工したセーブを書いた直後に
> `localStorage.setItem` を潰してからリロードする**こと。そうしないと
> 生きている `S` に上書きされて、移行経路を通らないセーブを読んでしまう（一度これで誤検証した）。

### 7-4 兵舎Lv20で第4部隊（SAVE_VERSION 変更なし）

```js
SQUAD_UNLOCK_BARRACKS_LV: [1, 5, 10, 20],
```

定数1行。`ensureSquads()` の名前配列に「第四隊」が既にあり、
`getSquadSlots()` は配列長を見ているのでそのまま解禁される。

実測: 兵舎Lv1→1／Lv5→2／Lv10→3／**Lv19→3／Lv20→4**。
Lv20未満では部隊タブに `🔒 兵舎Lv20` が薄く並び、「次の部隊は兵舎Lv20で解禁」も出る。
第四隊でも編成の帯（7-6）は5枠で正常に動く。

---

## 13. 7-5 の実装結果（2026-08-01）— DESIGN_S3.md 完了

SAVE_VERSION 24。これで §0 の6項目すべてが入った。

### 保存先はタイル自身（`world.tiles[].struct`）

配列を別に持たず、`ownerId` や `level` と同じ**タイルの状態**として置いた。
参照が O(1) で済むのが決め手。`getTileDefense` はAIの攻撃対象を選ぶソートの中で
何度も呼ばれるので、ここで全施設走査をしたくない。

**移行では何もしない**。「未築城＝フィールドが無い」で始まるので補完は不要
（1,600タイルすべてに `struct: null` を書くとセーブが無駄に膨らむ）。
実測でセーブ長 114,750、`struct` を持つタイルは5枚だけだった。

### 効くのは「そのタイルの防御」だけ

プレイヤー側の柵・櫓はオーラと迎撃も持つが、**異民族の施設は自タイル防御のみ**にした。
オーラや迎撃まで持たせると `getTileDefense` が全施設走査になり、上のソートに響く。
「取り返しに行くと硬い」という §6 の狙いはこれで足りている。

```js
function getTribeStructAt(ti) { /* 異民族が保持している間だけ効く */ }
```

滅ぼされた勢力・通常AIの領地・プレイヤー領では効かない。実測で3通りとも確認済み。

### 挙動

| | 結果 |
|---|---|
| 領地2枚 | 建てない（`TRIBE_FORTIFY_MIN_TILES: 3`） |
| 領地3〜5枚 | 🧱柵 |
| 領地6枚以上 | 🏹櫓（`TRIBE_TOWER_MIN_TILES: 6`） |
| 建てる場所 | 皇都に最も近い自領から順（距離18→19→19→19） |
| 守り | 素1,388 → 柵1,638 → 櫓2,188 |
| ノーマル | `TRIBE_FORTIFY: false` で建てない |
| 通常AIの領地 | 効かない（656のまま） |
| 滅亡後 | 効かない（2,188 → 1,388） |
| プレイヤーが制圧 | **取り壊す**。「異民族の施設を取り壊した」をトーストに出す |

**接収はしない**。残すと `S.player.outposts` に無い施設が自領に居座り、
建設上限の外側で防御だけ増えてしまう。

### 築城のペース（式から）

異民族は `expander` 固定で行動間隔20〜60秒。攻撃(0.65)が優先なので、
築城に回るのは `(1-0.65) × 0.30 = 10.5%` ＝ **1勢力あたり約6.3分に1つ**。
上級の13勢力が生き残り続けたとして全体で**毎分約2つ**、30分で約60。
実際は討伐が進むほど勢力が減るので、これは上限側の見積り。

### 途中で直したこと

マップ描画の差し込み位置を最初に誤り、**プレイヤー領のブロックの中**に入れてしまった
（かつタイルindexを `ti` ではなく `i` と書いていた）。敵領は別の分岐なので、
そこに描いても永久に出ない。自領のマークとは独立した `if` に直した。

---

## 14. 上級の序盤猶予（2026-08-01・実機フィードバック）

上級を最初から遊んだところ、**拡張型AIの攻撃確率0.60が序盤に効きすぎて、
確保した土地をすぐ奪われ足場が作れない**状態だった。

`diff("AI_ATTACK_MIN_CASTLE_LV")` を足し、**本城がこのレベルに届くまでAIは
プレイヤー領を奪えない**ようにした。上級のみ 5、ノーマルは 0（＝実装前と同一）。

```js
if (S.player.castleLevel < diff("AI_ATTACK_MIN_CASTLE_LV")) return false;
```

- **猶予であって免除ではない**。Lv5以降は通常どおり狙われる
- 既存の「領地3枚未満は保護」(`AI_ATTACK_MIN_PLAYER_TILES`) はそのまま併存する
- 異民族は第二ステージ（本城Lv10以降）にしか現れないので、この判定に掛からない
- **難易度選択画面に明記した**。見えない仕様にすると「なぜ急に襲われ始めたのか」が読めない

実測（領地24枚・隣接するAIあり・60回試行）:

| 本城Lv | 上級 | ノーマル |
|---|---|---|
| 1 / 3 / 4 | **0/60**（保護） | 60/60 |
| 5 / 6 / 10 | 60/60 | 60/60 |

> **しきい値の解釈**: ご指示の「城レベル5以下は奪えない／城レベル5以上は除く」は
> 5でちょうど噛み合わないため、**「Lv5未満は保護、Lv5以上で解除」**として実装した。
> `CONFIG.DIFFICULTY.hard.AI_ATTACK_MIN_CASTLE_LV` の1箇所なので、
> 「Lv6から解除」にしたければ 6 に変えるだけで済む。
