# 第二ステージ設計書 — 武将育成・知将・異民族・レベル上限20

作成: 2026-07-27 / Claude（リードエンジニア）
対象: 拡張ロードマップ **#3 新知将武将** ＋ **#4 レベル上限20** ＋ **#5 Lv10以降の戦略性**

総監督指示を1つの設計にまとめたもの。**実装前にこの設計の確認をお願いします。**

---

## 0. 総監督指示の整理

| 指示 | 設計への落とし込み |
|---|---|
| 知将は攻撃・防御が低いが**必殺技が強い** | 知将タイプ = 武力低め・知力高め・**専用の強力な必殺技**を持つ |
| 新武将は攻撃と守備力が**100前後** | 新武将の武力/統率を 95〜115 の帯に置く（既存最高は業焔の105） |
| **SRの武将を消費するとSSRやURの経験値が上がる** | **武将レベル**を新設。SR以上を素材に、SSR/URを育てる |
| ゲーム終盤に**MAX200超える** | 武将Lv30で素の値の約2.05倍 → 武力100が205に |
| レベル10以降、**首都を制圧してから第二ステージ** | 皇都制圧＋本城Lv10 で第二ステージ開始 |
| そこそこ強い**異民族が流入**してくる | 異民族勢力を波状で流入させる。既存のAI侵攻の仕組みを再利用 |
| **全異民族を討伐して首都を堅守** | 勝利条件＝全異民族撃破 かつ 皇都を保持したまま |
| レベル上限20 | 建物の maxLevel 15→20（**コスト曲線と倉庫容量の修正が必須**。§4） |

---

## 1. 武将レベル（新規）— ステータスが伸びる軸

### 1-1. なぜ必要か

いま所持カードが持つ軸は2つある。

| 軸 | 上がると | 素材 |
|---|---|---|
| `rank`（合体・[S22]） | 実効レア度が上がる（倍率） | **同名同ランク5枚** |
| `skillLv`（必殺技・[S26]） | 必殺技の効果が伸びる | N/R が主（安い・大量） |

ここに **`level`（武将レベル）＝素のステータスが伸びる軸** を足す。素材は **SR以上**。
これでレア度ごとの役割が完全に分かれ、「無駄な武将」が消える。

| レア度 | 主な使い道 |
|---|---|
| **N / R** | 戦法（`skillLv`）の素材。大量に湧くので受け皿になる |
| **SR** | **武将レベルの素材**。または相性が噛み合う場面で普通に使う |
| **SSR / UR** | 育てる対象。Lv30・必殺技Lv10・合体で終盤まで伸び続ける |

### 1-2. 数値

```
武将レベル上限 … 30（レア度によらず一律）
ステータス倍率 … 1 + 0.035 × (Lv − 1)      → Lv30 で ×2.015
```

| 武将 | 素の武力 | Lv30 | 合体+1（実効レア度UP）も乗せた場合 |
|---|---|---|---|
| 天佑・神楽坂（UR） | 99 | **199** | 199 |
| 獄炎・業焔（UR） | 105 | **212** | 212 |
| 新武将（想定） | 110 | **222** | 222 |
| 鬼神・紅丸（SSR） | 97 | **195** | ×1.3で254 |

→ **終盤にMAX200超え**という指示を満たす。

### 1-3. 必要経験値と素材

```
Lv n → n+1 に必要な経験値 … 6 × 1.18^(n−1)
Lv30 までの累計          … 約 4,300
素材が与える経験値        … SR:40 / SSR:120 / UR:0（URは素材にしない）
```

→ Lv30 まで育てるには **SR約107枚ぶん**（SSRを混ぜれば大幅に短縮）。
10連ガチャのSR排出15%なので、**約71回の10連**に相当。終盤の長期目標として妥当な重さ。

**素材のルール（戦法強化と同じ思想）**
- **SR以上のみ**素材にできる（N/Rは戦法へ回す）
- **編成中のカードは素材にできない**
- **URは素材にできない**
- 対象は**どのレア度でも育てられる**（SRを育ててもよい）が、上限Lv30は共通なので
  素のステータスが高いSSR/URを育てるのが有利

### 1-4. 既存システムとの掛かり方

`getCardRankMult()`（合体の倍率）とは**独立に掛ける**。

```
実効ステータス = 素の値 × rankMult(合体) × levelMult(武将Lv)
```

`cardPowerOf` / `cardLeadOf` は既にこの1箇所を通るので、**接続点は2関数だけ**。
最大兵力（統率×20）にも自動的に効く。

---

## 2. 新武将 12枚（うち知将6枚）

### 2-1. 設計方針

- **武力/統率は 95〜115 の帯**（既存最高105の少し上）
- **知将は武力を60〜75に落とし、知力を115〜135に上げる**。代わりに**専用の強力な必殺技**
- 新規レア度は作らない（SSR6枚・UR6枚として既存のガチャに乗せる）
- **既存26枚と合わせて38枚**。imageKey は `card_ssr_*` / `card_ur_*` の規則を踏襲

### 2-2. 追加する必殺技 6種（知将向けの強化版）

既存11種に加え、**倍率が高いかわりに知将しか持たない**技を用意する。

| id | 名前 | 系統 | 効果（Lv1あたり） | 狙い |
|---|---|---|---|---|
| `daikakei` | 大火計 | 謀 | 敵防御 −4.0% 無視 | 火計の上位。硬い皇都・異民族城を抜く鍵 |
| `rakurai` | 落雷 | 謀 | 敵防御 −2.8% ／ 部隊戦力 +1.5% | 攻防両立 |
| `tenpen` | 天変 | 謀 | 敵防御 −2.2% ／ 損耗 −2.0% | 長期戦向け |
| `hachimon` | 八門 | 防 | 駐留時の守り +6.0% | **首都堅守**の主役 |
| `gunryaku` | 軍略 | 補 | 部隊戦力 +2.2% ／ 行軍 +2.2% | 万能型 |
| `kikou` | 奇襲 | 攻 | 部隊戦力 +2.4% ／ 要衝 +3.0% | 攻城の補助 |

`SKILL_CAP` は据え置き（power40/casualty40/speed40/siege60/guard50/pierce35）。
**大火計Lv10を2枚積めば pierce 上限35%に到達**する＝知将を並べる意味が数字で出る。

### 2-3. 追加武将の一覧（案）

**UR 6枚**（うち知将3）

| imageKey | 名前 | 兵種 | 武 | 知 | 統 | 必殺技 | 型 |
|---|---|---|---|---|---|---|---|
| `card_ur_kagen` | 臥龍・臥玄 | 弓兵 | 68 | 132 | 105 | 大火計 | **知将** |
| `card_ur_seiran` | 蒼嵐・青蘭 | 騎兵 | 72 | 128 | 108 | 落雷 | **知将** |
| `card_ur_mumei` | 無銘・霧明 | 楯兵 | 70 | 125 | 115 | 八門 | **知将** |
| `card_ur_gouki` | 剛鬼・轟鬼 | 歩兵 | 115 | 70 | 105 | 奇襲 | 武将 |
| `card_ur_hayabusa` | 隼将・疾隼 | 騎兵 | 110 | 82 | 100 | 軍略 | 武将 |
| `card_ur_kongou` | 金剛・不動 | 楯兵 | 98 | 88 | 118 | 八門 | 武将 |

**SSR 6枚**（うち知将3）

| imageKey | 名前 | 兵種 | 武 | 知 | 統 | 必殺技 | 型 |
|---|---|---|---|---|---|---|---|
| `card_ssr_gyokuen` | 玉煙・玉艶 | 弓兵 | 62 | 118 | 95 | 天変 | **知将** |
| `card_ssr_suiun` | 水運・翠雲 | 兵器 | 66 | 115 | 92 | 大火計 | **知将** |
| `card_ssr_reimei` | 黎明・玲明 | 槍兵 | 70 | 116 | 98 | 落雷 | **知将** |
| `card_ssr_tessai` | 鉄砕・鉄斎 | 兵器 | 108 | 68 | 96 | 奇襲 | 武将 |
| `card_ssr_shunrai` | 迅雷・駿雷 | 騎兵 | 105 | 75 | 94 | 軍略 | 武将 |
| `card_ssr_gagoze` | 牙吾・牙皇 | 歩兵 | 112 | 62 | 90 | 突撃 | 武将 |

**知将の戦力は低く出る**（`cardPower` は武力1.2・統率1.0・知力0.6の重み）。
例: 臥龍・臥玄 = (68×1.2 + 105×1.0 + 132×0.6) × 2.6 = **664**（神楽坂721より低い）。
**素の戦力では劣るが、必殺技で勝つ**という指示どおりの形になる。

### 2-4. アート

12枚の発注が必要。`IMAGE_PROMPTS.md` §1 の体裁で、**知将は「武器より書物・呪符・軍配」**、
武将は「重装・大型武器」で描き分ける。第4弾として発注する。

---

## 3. 第二ステージ — 異民族の流入

### 3-1. 開始条件と物語

```
皇都を制圧　かつ　本城Lv10以上
  ↓
「北の草原より異民族が流入した。皇都を守り抜き、これを討ち払え」
  ↓
第二ステージ開始（S.stage = 2）
```

現在の勝利後フェーズ（[S09] `isRulingPhase`）を**第二ステージに置き換える**。
「残る反乱軍を掃討せよ」は第二ステージ開始までの繋ぎとして残す。

### 3-2. 異民族の実装（既存の仕組みを最大限使う）

異民族は **`S.aiUsers` に `tribe: true` で追加**する。これだけで既存の以下がそのまま効く。

- AIの侵攻（[S12] `aiAttack` / `resolveAIMarches`）→ 積極的に領地を奪いに来る
- 城の攻略（[S09] `defeatAI`）→ 討伐できる
- マップ描画・タイル所有・防御計算

**異民族の違い**

| 項目 | 通常AI | 異民族 |
|---|---|---|
| 勧誘 | 可（`recruitable`） | **不可**（`recruitable: false`・`tribe: true`） |
| 性格 | 3種 | **expander 固定**（常に攻める） |
| 城レベル | 1〜10 | **12〜16**（そこそこ強い） |
| 攻撃頻度 | 0.45/行動 | **0.65/行動** |
| 攻撃戦力 | `180 × Lv^1.3` | **同式 × 1.4**（異民族補正） |
| 地域の壁 | 越えない | **越える**（皇都を目指して侵入） |
| 出現位置 | ワールド生成時 | **マップ北端から波状に出現** |
| 色 | AI各色 | **統一色（褐色系）＋「蕃」バッジ** |

### 3-3. 波（ウェーブ）

```
第1波 … 開始と同時に 3勢力（城Lv12）
第2波 … 第1波を全滅させると 3勢力（城Lv14）
第3波 … 第2波を全滅させると 2勢力（城Lv16）※最終波
```

- 各波の出現は**マップ北端（y ≤ 3）の通行可能タイル**から、既存AI城と5マス以上離して配置
- 波の切り替わりでバナーとチャットが動く（`onTribeWave`）
- **全8勢力を討伐すると「異民族討伐」達成**

### 3-4. 首都堅守

- 皇都タイルを**異民族に奪われると警告**（バナーが赤くなる・記録に残る）
- 奪われた状態では**異民族討伐を達成しても勝利にならない**
- 勝利条件 = **全異民族撃破 かつ 皇都を保持している**

これで「首都を堅守する」が単なる文言でなくルールになる。
`八門`（駐留時の守り+6%/Lv）を持つ知将を皇都に置く動機が生まれ、
[S23] 幕舎・駐留・防衛施設・迎撃がすべて意味を持つ終盤になる。

### 3-5. バランス検算

異民族の攻撃戦力（`180 × Lv^1.3 × 1.4`）:

| 城Lv | 攻撃戦力 |
|---|---|
| 12 | **6,236** |
| 14 | 7,608 |
| 16 | 9,043 |

対して皇都（プレイヤー保持時）の守り:

```
皇都の地形防御 4,000（CAPITAL_DEF）
+ 本城Lv20 × 160 = 3,200
+ 兵舎Lv20 × 120 = 2,400
+ 防衛施設 2,500（隣接に建てれば +900 のオーラも）
+ 駐留部隊の戦力 × 0.5 × 八門の guard 倍率
= 12,100 + 駐留ぶん
```

→ **内政と駐留を仕上げれば最終波(9,043)を弾ける**が、
放置していれば奪われる水準。§3-4 の堅守条件が機能する。

---

## 4. 建物レベル上限 20 —（ここが最も注意が必要）

### 4-1. 検算で見つかった問題

**現在の maxLevel は 15**（10ではない）。そのうえで実測すると:

| 本城 | 必要木材 | その時の倉庫容量 | 判定 |
|---|---|---|---|
| Lv15→16 | 495.4k | 599.1k | 可 |
| **Lv16→17** | **792.6k** | **668.6k** | **★詰む** |
| Lv19→20 | 3.25M | 895.4k | ★詰む |
| Lv20→21 | 5.19M | 977.0k | ★詰む |

**maxLevel を単に20にすると Lv16 で物理的に進めなくなる。**
`CASTLE_COST_GROWTH = 1.6` の指数と、倉庫容量 `6000 × Lv^1.7` の伸びが釣り合っていない。

### 4-2. 修正案（Lv1〜10 には一切触らない）

**① 本城コストの成長率を段階化**

```
Lv2〜10  … ×1.6（現状のまま）
Lv11以降 … ×1.35
```

**② 倉庫容量に高レベル帯の別指数を入れる**

```
容量 = 6000 × min(Lv,10)^1.7 × (Lv > 10 ? (Lv/10)^3.0 : 1)
```

Lv10以下は**現状と完全に同じ**。Lv11以降だけ伸びが加速する。

**修正後の検算**

| 本城 | 必要木材 | 倉庫容量 | 占有率 |
|---|---|---|---|
| Lv10→11 | 47.2k | 300.7k | 16% |
| Lv15→16 | 251.1k | 1.01M | 25% |
| Lv19→20 | 834.0k | 2.06M | 40% |

→ 後半ほど倉庫を育てる必要が増すが、**詰まない**。

### 4-3. 生産・防御・戦力の伸び

Lv20まで開けるなら、Lv10以降の伸びも見直す必要がある。

| 項目 | 現状の式 | Lv20での値 | 判定 |
|---|---|---|---|
| 建物生産 | `prodBase × Lv` | 農場600×20 = 12,000/h | 線形。倉庫容量2.4Mに対し**遅すぎる** |
| 自領防御 | `本城Lv×160 + 兵舎Lv×120` | 5,600 | 異民族9,043に**届かない**（施設と駐留で補う設計） |
| 部隊戦力 | `兵舎Lv×60 + 本城Lv×25` | 1,700 | カード戦力（数千）に対し補助的。妥当 |
| 最大兵力 | `統率×20 + 兵舎Lv×200` | 兵舎ぶん4,000 | 妥当 |

**建物生産だけ線形で追いついていない。** 提案:

```
生産 = prodBase × Lv × (Lv > 10 ? 1 + 0.08 × (Lv − 10) : 1)
```

Lv20で ×1.8 → 農場 21,600/h。倉庫2.4Mを埋めるのに約111時間 → 長すぎる。
**倉庫容量の指数3.0が強すぎる**ので、`2.5` に落として Lv20で1.71M、
生産21,600/hで約79時間ぶんとする。※ここは実プレイでの調整前提。

### 4-4. 上限解放のゲート

```
建物 maxLevel … 15（従来）→ **第二ステージ開始で20に解放**
```

`BUILDING_DEFS[].maxLevel` は静的定義なので触らず、
`getBuildingMaxLevel(type)` を新設して **ステージで返り値を変える**（規約2を守る）。

これにより「首都を制圧して第二ステージに入る」ことが上限解放の報酬になり、
指示にあった「レベル10以降を解禁してレベル20まで増える」という段階感が出る。

---

## 5. データモデル（SAVE_VERSION 19）

```js
S.player.cards[] … { uid, defId, obtainedAt, rank, skillLv, skillExp,
                     level, exp }              // ★追加: 武将レベルと経験値
S.stage          … 1 | 2                       // ★追加: 第二ステージ
S.tribe          … {                           // ★追加: 異民族の進行
                     wave: 0,                  // 0=未開始 / 1〜3
                     startedAt: null,          // 第二ステージ開始時刻
                     clearedAt: null,          // 討伐達成時刻
                   }
S.aiUsers[]      … { …既存…, tribe: bool }     // ★追加: 異民族フラグ
```

**マイグレーション（すべて非破壊）**
- `level` 未設定 → 1、`exp` 未設定 → 0
- `stage` 未設定 → 皇都制圧済みかつ本城Lv10以上なら2、それ以外は1
- `tribe` 未設定 → `{ wave: 0, startedAt: null, clearedAt: null }`
- `aiUsers[].tribe` 未設定 → false
- 既に勝っているセーブは**第二ステージの開始告知から始まる**（いきなり異民族が湧かない）

---

## 6. 実装の順序（1つずつコミット）

| 段 | 内容 | 検証の要点 |
|---|---|---|
| **6-1** | **武将レベル**（`level`/`exp`・SR以上を素材・カード詳細に育成UI） | Lv30でステータス×2.015／200超え／編成中とURの素材化拒否 |
| **6-2** | ✅ **完了**（必殺技6種＋新武将12枚＋アート12枚同梱） | 下記の実測で確認 |
| **6-3** | **建物Lv20**（コスト段階化・倉庫指数・生産補正・ステージゲート） | Lv11〜20で詰まないこと／Lv10以下が現状と完全一致 |
| **6-4** | **第二ステージと異民族**（波・皇都堅守・勝利条件・チャット） | 波の切替／皇都を奪われると勝利にならない／既存AIと共存 |
| **6-5** | 異民族のセリフをGrokへ発注（`onTribeWave` 等） | 空プールでも壊れない |

**6-3 は 6-4 の前に入れる**。上限解放は第二ステージの報酬なので、
先にゲートの仕組みを作っておかないと 6-4 で二度手間になる。

---

## 7. 確認していただきたい点

1. **武将レベルの上限30／SR以上を素材**という配分でよいか
   （N/R=戦法、SR=武将育成、SSR/UR=育てる対象 という役割分担）
2. **新武将12枚（知将6・武将6）**の枚数と、UR/SSRの内訳でよいか
3. **異民族は3波・計8勢力**という規模でよいか
4. **上限解放を第二ステージの報酬にする**（＝皇都制圧前はLv15止まり）でよいか
5. §4-3 の**生産と倉庫容量の再調整**は、実プレイで手触りを見ながら詰める前提でよいか

---

## 8. 6-2 の実装結果（2026-07-27）

### pierce の数値を設計から変更した

設計では `大火計 pierce 4.0/Lv`・`SKILL_CAP.pierce 35%` としていたが、実装後の検算で2つ破綻が出た。

1. **5枚編成だとLv2で上限35%に到達**し、Lv3〜10がまったく無意味になる
2. **pierce は power/siege に負ける**。power/siege は戦力に掛け算で効くのに対し、
   pierce は敵防御を1回引くだけ。同じ%では割に合わない
   （大火計Lv10の臥玄=戦力3,540 vs 奇襲Lv10の轟鬼=戦力8,490）

**「知将を1〜2枚混ぜる」前提のカーブに直した。**

| | 設計 | 実装 |
|---|---|---|
| 大火計 | pierce 4.0/Lv | **2.0/Lv** |
| 落雷 | 2.8/Lv | **1.6/Lv** |
| 天変 | 2.2/Lv | **1.4/Lv** |
| 火計（既存） | 2.5/Lv | **1.5/Lv** |
| 水計（既存） | 1.5/Lv | **1.0/Lv** |
| 混乱（既存） | 1.8/Lv | **1.1/Lv** |
| `SKILL_CAP.pierce` | 35% | **50%** |

### 修正後の実測（皇都・防御4,000への攻撃）

| 必殺技Lv | 武将5枚 | 武将3+知将2 |
|---|---|---|
| Lv1 | 余裕0.98倍 | 0.98倍（互角） |
| Lv5 | 1.53倍 | 1.54倍（互角） |
| **Lv10** | 1.89倍 | **2.64倍（知将が逆転）** |

知将2枚の pierce 推移: Lv1:4% → Lv5:20% → Lv10:40%（上限50%に張り付かない）。

**「素の戦力では劣るが、必殺技を育てると勝つ」が数字で成立した。**
知将を混ぜた編成は戦力6,330 < 7,549 と低いのに、皇都の防御を4,000→2,400まで削るため実効性が上回る。

### 素の戦力の実測（設計§2-3の検算違いを訂正）

設計書では臥龍・臥玄を664と計算していたが、正しくは**691**（計算間違い）。
同レア度での比較は設計意図どおり成立している。

| | 知将 | 武将（新） |
|---|---|---|
| UR | 691〜712 | 731〜750 |
| SSR | 480〜503 | 523〜533 |

ただし**第一陣のSSR一部（沙耶458・天羽492）は新知将SSRより低い**。
新武将は「武力/統率100前後の帯」＝第一陣より上の帯という指示のためで、意図どおり。

### カードアートの解像度を 256px → 192px に変更

38枚で 759KB → 478KB（-37%）。表示枠140pxに対し192pxで1.37倍あり、
140px表示での平均画素差は2〜5/255で判別不能。index.html は 1.96MB → 1.59MB。
