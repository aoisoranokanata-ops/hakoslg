# CLAUDE.md — 箱庭クロニクル プロジェクト規約 / 継続ガイド

Claude Code はセッション開始時にこのファイルを読むこと。
読む順序: **本書（規約と現状）→ `HANDOVER.md`（詳細な引き継ぎ）→ `DESIGN.md`（設計意図）**。
`index.html` の全読は不要。セクション記号 `[Sxx]` で該当箇所へ飛べる。

---

## 0. ⚠️ 最優先: 作業前の同期確認

**ローカルの `hakoslg/index.html` と、チャット側で改修した版に差分がある。**
まずここを解消してから新規作業に入ること。

| | ローカル(Claude Code生成) | チャット改修版 |
|---|---|---|
| セクション | `[S01]`〜`[S16]` | `[S01]`〜**`[S17]`** |
| `SAVE_VERSION` | 1 | **2** |
| 城タブ | 建物のリスト表示 | **箱庭ビュー（ドラッグ配置・タップ強化・Lvで外観変化）** |
| 行数 | 約1,100行 | 約1,492行 |

### 同期手順
1. チャット側で配布された `index.html`（S17入り）を `hakoslg/index.html` に**上書き**する。
2. 上書き後、`grep -n '\[S17\]' index.html` で反映を確認。
3. `sw.js` の `CACHE` バージョンを1つ上げる（更新が配信されない事故を防ぐ）。
4. `HANDOVER.md` の実装済みリストに城タブ箱庭ビューを追記（本書§5が最新版）。

**セーブ互換**: `SAVE_VERSION: 2` へ上げ済み。`loadGame()` から `migrateSave()` が呼ばれ、
旧セーブの建物に城内座標 `x,y` を自動補完する。既存プレイデータは破棄せず引き継がれる。

---

## 1. プロジェクト概要

課金型SLG（三國志真戦 / クラッシュ・オブ・クラン系）を、**ローカル完結・完全無料・オフライン**で
遊べる箱庭シミュレーションとして再設計したもの。

- 自城を強化し、マップ上の**聖地**を領有するのが目的
- 「本来は課金で渋いガチャ用ダイヤ」を、**領土拡大に比例して自動で増える**設計に反転（本作のキモ）
- 周囲のAI勢力10城はすべて**ルールベースの軽量な状態機械**。LLM/外部APIは実行時に一切使わない

### 技術制約（厳守）
- 単一HTMLファイル。ビルドツール不要、CDN不使用、CSS/JSすべてインライン
- 保存は `localStorage`（セーブ）＋ `IndexedDB`（画像）。オフラインファースト
- キーは必ず `hakoslg_` プレフィックス
- モバイル（iPhone / GitHub Pages配信）で快適に動くこと
- 例外は `sw.js` のみ（Service Workerはブラウザ仕様上インライン不可）

---

## 2. ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | ゲーム本体。**これ1枚で動く** |
| `sw.js` | Service Worker（唯一の分離ファイル） |
| `CLAUDE.md` | 本書。規約と現状 |
| `HANDOVER.md` | 引き継ぎ詳細（スキーマ・チェックリスト） |
| `DESIGN.md` | 基本設計（データモデル・バランス方針） |
| `IMAGE_PROMPTS.md` | 画像生成プロンプト集（imageKey対応表つき） |
| `CHAT_SCRIPT.md` | 同盟チャット/事件のセリフ執筆ブリーフ（Grok担当） |
| `UX_REVIEW.md` | UX検品ブリーフ（Gemini担当）。制約・撮影チェックリスト・出力形式 |
| `DESIGN_BRIEFS.md` | 設計発注ブリーフ集（ChatGPT担当）。初回導線／地形システム |
| `DESIGN_S2.md` | 第二ステージ設計書（武将育成・知将・異民族・レベル上限20）。**全項目 実装済み** |
| `DESIGN_S3.md` | 第三次拡張 設計書（難易度・築城・編成D&D）。**全項目 実装済み** |
| `DESIGN_S4.md` | **同盟の深掘り 設計書**（名前・援軍・ミッション・AI同士の戦争）。Claude作成・**確認待ち** |
| `tools/embed_art.py` | art/*.png を index.html へ base64 同梱（冪等・安全ガード付き） |
| `art/` | 建物画像 PNG（256×256・背景透過・PNG8最適化済み）＋カード画像 JPEG（256×256・不透明・q80） |

---

## 3. 絶対に守る規約

新機能を足すときも、この設計思想は崩さないこと。

1. **可変状態はグローバル `S` ただ1つ**。`S` を `JSON.stringify` したものがそのままセーブデータ。
2. **静的定義は `S` に入れない**（`BUILDING_DEFS`, `CARD_POOL`, `ROLE_DEFS`, `TILE_DEFS` 等）。
   `S` 側は id/type/level 等の最小参照のみ持ち、**派生値（コスト・生産量・戦力）は毎回計算**。
   キャッシュ禁止。
3. **バランス調整は `CONFIG` のみで完結**させる。式に定数を直書きしない。
4. **時刻は `Date.now()` のエポックms絶対値**（`upgradeEndsAt`, `arriveAt`, `nextActionAt`）。
   相対カウントダウンを保存しない。オフライン跨ぎで破綻するため。
5. **ロジック関数の失敗は「理由文字列を返す / 成功は `null`」**。throwしない。
6. **UI操作の入口は必ず `UIA.動詞`**。inline onclick からロジックを直接呼ばない。
7. **`S` のスキーマを変えたら `CONFIG.SAVE_VERSION` を上げ、`migrateSave()` に移行処理を書く**。
   移行は欠損フィールドの補完のみの非破壊操作にする。
8. `sw.js` は **v25 でネットワーク優先（network-first）に変更済み**。毎回サーバーの最新を取りに行き、
   取得できない時だけキャッシュへフォールバックする。**このため `index.html` 更新時の `CACHE` bump は不要**
   （旧方式ではキャッシュ優先だったため bump しないと更新が届かなかった）。
   `ASSETS` を増やす等でキャッシュを作り直したい時だけ上げる。
9. 命名: 定数`UPPER_SNAKE` / 関数`camelCase`動詞始まり（`getXxx`/`renderXxx`/`startXxx`）/
   DOM id はタブページ `page-<tab名>`。

---

## 4. セクション索引

```
[S01] CONFIG          全バランス定数。調整はここだけ触る
[S02] STATIC DEFS     建物/カード/役職/タイル種別の静的定義（セーブに含めない）
[S02b] EMBEDDED ART   建物アートのbase64同梱(ART_DATA)。tools/embed_art.py で再生成
[S03] UTILS           $/fmt/toast等の汎用ヘルパー
[S04] PERSISTENCE     saveGame/loadGame/migrateSave + AssetDB(IndexedDB画像)
[S05] WORLD GEN       generateWorld / generateAIUsers（新規ゲーム時のみ）
[S06] STATE INIT      ★セーブデータ完全スキーマのコメントはここ。newGameState()
[S07] ECONOMY         getProdPerHour / applyEconomy / grantOfflineGains
[S08] BUILDINGS       getUpgradeCost / startUpgrade / finishUpgrades
[S09] MILITARY        getArmyPower / getTileDefense / startMarch / resolveMarches
[S10] GACHA           rollRarity(天井込み) / doGacha
[S11] ALLIANCE        recruitAI / setMemberRole / getAllianceBuffs
[S12] AI USERS        aiTickAll — ルールベース状態機械
[S13] UI: PANELS      renderHeader/City/Cards/Gacha/Alliance
[S14] UI: MAP CANVAS  MapView（描画・パン・ズーム・タップ）+ openTileSheet
[S15] UI: ACTIONS     UIA.* — ボタンonclickの入口
[S15A] TUTORIAL GUIDE 初陣指南。getTutorialStep()が状態から現在ステップを算出
[S16] GAME LOOP       tick(1秒) / switchTab / boot
[S17] UI: CITY PLOT   城タブの箱庭ビュー（ドラッグ配置・タップ強化・ティア外観）
[S18] ALLIANCE CHAT   同盟チャット。CHAT_LINES(セリフプール)+chatEvent/chatTick で再生
[S19] ALLIANCE INCIDENTS 同盟内事件。忠誠低下→不満→なだめ/任命/放置/追放
[S20] EVENT LOG       出来事ログ。toast(msg,kind)で記録し📜ボタンから読み返す
[S21] RESEARCH        研究ツリー。研究所から解禁する恒久バフ（同時1件）
[S22] CARD FUSION     カード合体。同名同ランク5枚→1枚でランクUP（cards[].rank）
[S23] OUTPOST/GARRISON 幕舎と駐留。前線の拠点（出撃起点・防御・回復・迎撃）＋防衛施設
[S24] ALLIANCE VAULT  同盟倉庫＋盟主の目標指示（包囲で硬い目標の防御を削る）
[S25] DISCOVERY HINTS 新機能の気づき。解禁の告知＋タブの●バッジ
[S26] SKILL           必殺技（戦法）＋武将レベル。武将を消費してLvUP・カード詳細シート
[S27] TRIBE           異民族（第二ステージ）。波状流入・皇都堅守・討伐が勝利条件
[S28] EVENT           期間限定イベント。プレイ時間で開催／期間中はUR・SSRの排出率UP
[S29] AUDIO           BGMと効果音。**WebAudioの生成音**で音源ファイルを持たない
```

### データフロー（一方向）
```
ユーザー操作 → UIA.*(S15) → ロジック(S07-S12)が S を変更
  ↓ 毎秒 tick(S16) → applyEconomy / finishUpgrades / resolveMarches / aiTickAll
  ↓ render*(S13/S14/S17) が S を読んでDOM/canvasを再構築
  ↓ 10秒ごと saveGame → localStorage["hakoslg_save_v1"]
```

---

## 5. 実装状況（最新）

### ✅ 実装済み
- 40×40ワールド生成（距離帯レベル・資源分布・聖地×4・AI城10）
- 資源生産（建物＋領有タイル）、倉庫キャップ、研究所の生産バフ
- オフライン精算（12h上限）、10秒自動保存、`visibilitychange` 保存
- 建物アップグレード（コスト・実時間タイマー・本城レベルゲート）
- 出兵→制圧（隣接ルール・防御力計算・行軍時間）、放棄
- 領有→ダイヤ自動加算、聖地勝利判定＋バナー
- ガチャ（単発/10連・天井50・10連SR保証）
- AI状態機械（3性格）、勧誘、役職とバフ（将軍/都督/斥候）
- マップUI（canvas・パン・ズーム・タップ詳細シート・出兵アニメ）
- 5タブUI、モバイル対応、IndexedDB画像ストア＋取り込みUI、Service Worker
- **[NEW] 城タブ箱庭ビュー** — 城塞区画に8棟を配置、ドラッグで移動（グリッド吸着＋
  衝突回避）、座標を `S.player.buildings[].x/y` に保存、タップで強化ボトムシート、
  **Lv6でティア2 / Lv11でティア3**に外観が育つ（`CONFIG.CITY_TIER_UP`）
- **[DONE] 建物アート全8棟** — 本城/農場/製鉄所/兵舎/伐採所/採石場/研究所/倉庫。
  `art/<imageKey>.png`（256px透過）を原本に、`index.html` の `[S02b] ART_DATA` へ base64 同梱。
  `artHTML` は IndexedDB取り込み > 同梱 > 絵文字 の順で解決。**全オリジンで初回から絵表示**。
  差し替え・追加は art/ に置いて `python tools/embed_art.py` を実行（冪等・安全ガード付き）。
- **[DONE] ティア対応の描画基盤** — `artTierKey(baseKey, level)` が Lv6/11 で
  `bld_x_t2` / `bld_x_t3` を優先し、未配置なら基本キーへフォールバック。city plot と強化シート両方が対応済み。
- **[DONE] ティア2/3アート全8棟** — Lv6=`bld_x_t2` / Lv11=`bld_x_t3`（256px透過）を同梱済み。
  3段階(Lv1→Lv6→Lv11)の外観成長が全建物で完成。
- **[DONE] 同梱PNGの減色最適化** — art/ の24画像を PIL FASTOCTREE(256色＋Floyd-Steinberg)で
  PNG8化。**index.html 2.6MB → ≈660KB（約75%減）で視覚劣化なし**。art/ 自体が最適化済みで
  `tools/embed_art.py` はそのまま base64 化。今後の新規アートも同方式で最適化してから配置する
- **[DONE] 同盟チャット [S18]** — LLM不使用のルールベース再生。`CHAT_LINES`(性格別ambient＋
  イベント別プール)を `chatEvent(kind,opts)` / `chatTick(t)` で再生。加入/制圧/昇格/本城UP/勝利に連動、
  プレイヤー発言可(HTMLエスケープ済)、未読バッジ、ログ上限60。**Grok納品の第1弾セリフ185本を反映済み**
  （追加増量の手順・フォーマットは [CHAT_SCRIPT.md](CHAT_SCRIPT.md)）。
- **[DONE] 同盟内事件 [S19]** — AIに `loyalty`(0-100) を持たせ、一定間隔で低下（役職なしは早い）。
  45未満で不満を訴え事件発生（同時1件）→ 盟主が**なだめる/役職を与える/放置/追放**の4択。
  未対応4分で自動「放置」、忠誠12未満で自主脱退（再勧誘可）、追放は勧誘不可。
  同盟タブに事件パネル＋忠誠バー、タブに「!」バッジ。SAVE_VERSION 4。
  **事件セリフはGrok第2弾153本を反映済み**（チャット全体で338本／CHAT_SCRIPT.md）。
  結末3種(onIgnore/onExile/onLeave)は**性格別プール対応済み**（`chatEvent` に `sub: ai.personality` を渡す）。
  `resolveChatPool` がフラット配列/サブキー別オブジェクトの両方を受け、キー欠落時は平坦化して発話を落とさない
- **[DONE] `{other}` 変数** — 発言者以外の同盟AI名。`randomOtherAllianceAI()` が本人を除いて抽選し、
  該当者が居ない時は `{other}` を含むセリフを候補から除外する（文が壊れない・自己言及も起きない）。
  ambient も `chatEvent("ambient",…)` 経由に統一済み。AI同士の掛け合いセリフの土台

- **[DONE] マップの視覚的誘導** — 出兵可能タイルの強調（隣接ルールの可視化）／自城の脈動リング＋
  「自城」ラベル／画面外の聖地を指すコンパス（タップでジャンプ）。`MapView.startAnim()` は
  マップタブ表示中のみ15fpsで再描画。定数は `MAP_HIGHLIGHT_MARCHABLE` / `MAP_HOLY_COMPASS` / `MAP_ANIM_FPS`

- **[DONE] 勧誘導線** — マップのAI城に外交バッジ（同盟員=金「盟」／勧誘可=緑「勧」）＋
  同盟タブに「近隣の勢力（勧誘候補）」一覧（距離順・城Lv・成功率・🗺️で地図へジャンプ・その場で勧誘）。
  `recruitChance()` / `getRecruitCandidates()` を [S11] に切り出し、UIと実処理で共用
- **[DONE] 出来事ログ [S20]** — `toast(msg, kind)` の第2引数で `S.log[]` に記録（上限30・SAVE_VERSION 5）。
  マップの📜ボタンから履歴シートを表示、未読はマップタブと📜ボタンにバッジ。
  制圧/敗北/建物完成/同盟/事件/オフライン収益を種別ごとに色分け（敗北は赤で強調）。
  kind省略のトースト（操作エラー等）はログに残さない

- **[DONE] 初陣指南 [S15A]** — ChatGPT設計の確定仕様を実装。最初の約1分で
  「領地→本城→兵舎→10連→もう1枚領地」とコアループを1周させる5段階の導線。
  ヘッダー直下に指南バー1要素のみ（新タブ・操作ロック・追加報酬なし）。
  現在ステップは保存せず `getTutorialStep()` が状態から算出するので順不同でも破綻しない。
  ダイヤの出どころ `+X/h ←領地` は完了後も恒久表示。SAVE_VERSION 6。
  既存セーブは「完全初期状態のみ指南開始、遊んでいれば完了扱い」で押し付けない

- **[DONE] 地形システム [S05/S09]** — ChatGPT設計の確定仕様を実装。40×40は維持。
  山/大河は「出兵対象にならない地形」、埠頭/関所だけが地域の連絡路、中央に皇都州（Lv8〜10の資源12枚）。
  三地方（北西4/北東4/南方2のAI配置）に分かれ、プレイヤーは南方(20,34)から始まり皇都(20,20)制圧が勝利条件。
  通行判定は `canStepBetween()` に集約（斜めの角抜けを禁止）。行軍時間だけ地形を考慮し、
  `buildTerrainDistanceMap()` のBFSは起動時1回のみ（`RUNTIME` に保持・セーブしない・draw()から呼ばない）。
  AIは境界（埠頭/関所/皇都）を越えないので自地方に閉じこもる。SAVE_VERSION 7。
  **旧セーブは旧マップ・旧勝利条件（聖地）のまま**遊べる（`world.ruleset` で分岐、tilesは不変）

- **[DONE] 戦闘の深掘り [S09]** — 兵種の三すくみ（🛡️歩兵→🐎騎兵→🏹弓兵→歩兵／有利×1.25・不利×0.8）、
  部隊(squad)制で手動編成、兵舎Lv1/5/10で最大3部隊まで解禁。カードは部隊間で重複不可なので
  「1部隊に集中して硬い関所を抜く」か「複数部隊で並行して資源地を取る」かの選択が生まれる。
  タイルにも `defTroop`（Lv5以上＋関所/埠頭/皇都）が付き、タイル詳細で部隊ごとの
  相性込み戦力を見て出撃先を選ぶ。未編成の部隊は自動編成（戦力上位）で補われる。SAVE_VERSION 8

- **[DONE] UX検品第2回の対応** — 指南中はマップの高度な表示（目標コンパス/外交バッジ/📜記録）を
  抑えて手順に集中させ、完了時に解放を告知（`TUTORIAL_HIDE_ADVANCED_UI`）。
  出撃画面に「有利な兵種」を明記し最適な部隊を緑で強調、相性勝利を制圧トーストで演出。
  通行不可タイルはLv/所有者を出さず「⛔通行不可」を明示。
  残りは [UX_REVIEW.md](UX_REVIEW.md)「第2回検品の結果」の未対応欄（カードのスタック表示ほか）

- **[DONE] バランス再調整（2026-07-26）** — 総監督指示による大幅調整:
  ・**レア度5段階**（N/R/SR/SSR/**UR**＝★1〜5表記併記）。UR排出1%・SSR4%。カード26枚
  ・**兵種6種**。歩兵→槍兵→騎兵→弓兵→楯兵→歩兵の一本の輪＋**兵器**（輪の外／要塞に×1.6・野戦×0.75）。
    守備兵種には兵器を使わない（有利な兵種が存在しなくなるため）
  ・**行軍速度を大幅低速化**（2→0.3タイル/秒、最低8秒）し**兵種別の速度差**を導入。
    部隊は最も遅い兵種に律速（騎兵単独27秒 vs 兵器混成104秒＠距離14）
  ・**敵地・要塞への進軍線を赤**に（無主地は金）。着弾マーカーも追加
  ・**同時アップグレードの抑制**: 無料枠1件（「基本は一施設に一つ」の方針は維持）、
    超過は💎80、**即完了は残り時間×💎12/分**（2026-07-26に緩和）
  ・**本城コスト増**: Lv2以降 木材1100・石材900から×1.6で増加（同・緩和）。倉庫を育てないと貯めきれない
    （倉庫容量に連動させると「倉庫を上げるほど本城が遠のく」逆インセンティブになるため非連動）
  ・**オフライン進行を停止**（`OFFLINE_PROGRESS:false`）。離席中は生産も普請も行軍もAIも止まり、
    `shiftTimersForOffline()` が全タイマーを離席時間ぶんずらして続きから再開する

- **[DONE] 負傷兵システム（兵力）[S07/S09]** — 部隊が**兵力**を持ち、戦力＝カード戦力×**充足率**。
  最大兵力＝編成の統率合計×20＋兵舎Lv×200（初期1,180／中盤7,000／終盤11,220）。
  損耗は「敵防御÷自軍戦力」比に連動（**実プレイの手触りで2026-07-26に約3割軽くした**:
  楽勝2%／互角15.9%／敗北34.7%。互角の相手に**10連戦**できる）。自然回復は0.8%/分（満タン約2時間）。
  充足率20%未満は出撃不可。**徴兵所**を新設し予備兵を時間で蓄積（Lv×500上限・Lv×20/分）、
  1兵につき食料2で補充。編成/兵舎変更時は `withTroopRatioPreserved()` で充足率を維持。
  **兵器の特性**: 要塞×1.6・野戦×0.75／**射程2（後方攻撃）だが未所有の要衝は飛び越えられない**／
  損耗×1.6（部隊内平均）。SAVE_VERSION 9・旧セーブは満タン開始で徴兵所Lv1を追加

- **[DONE] カードのスタック表示** — 同名カードを1枚に集約し `×3` の所持数バッジ＋`＋/−` で
  未使用の1枚を出し入れする方式に（22枚→15枚に集約）。編成の実体は uid 単位のまま。
  自動編成のまま1枚触ると他が消えるため `materializeAutoSquad()` で先に固定する。
  未解禁の部隊タブも `🔒兵舎Lv5` として薄く表示（Gemini提案）

- **[DONE] カードアート全26枚（2026-07-27）** — UR3/SSR5/SR6/R6/N6 を4回に分けてChatGPTへ発注し同梱。
  **カードは建物と別方式**: 背景ありの全面イラストで透過不要のため PNG8(256色)ではなく
  **256px JPEG q80**（`art/card_*.jpg`・1枚11〜27KB・全26枚で475KB）。256色に落とすと
  グラデーションが破綻するため。`tools/embed_art.py` は png/jpg 両対応（MIMEは拡張子から決定、
  同名の png/jpg 併存は中断）。**発注で効いた指示**は ①顔を画面の1/3以上（引きの戦場絵は
  139pxの表示枠で潰れる。UR業焔だけ中央寄りクロップで救済）②キャラ別に主色を指定
  （SSRで白銀髪＋金装飾が3枚重なり一覧で区別不能になったため）③レア度ごとに演出の強さを
  明示的に下げる（UR=虹光／SSR=金の光条／SR=紫と銀／R=淡い逆光のみ／N=演出なし）。
  結果、URほど非現実的・Nほど写実的というグラデーションになり、絵柄だけでレア度が読める

- **[DONE] AI城への攻撃・AIからの反撃 [S09/S12]（2026-07-27）** — 双方向の戦争。SAVE_VERSION 11。
  ・**AI城を攻略できる**。落とすと `defeatAI()` でその勢力は滅亡し、領有タイルは**無主に戻る**
    （プレイヤーへ一括譲渡すると一度の勝利で地方が丸ごと手に入り以降の攻略が消えるため）
  ・同盟員の城は攻撃不可＝**勧誘するか征服するかの選択**が生まれる（攻めるには先に追放）
  ・城の防御 = `CASTLE_DEF_BASE × Lv^CASTLE_DEF_EXP`。**指数は1未満(0.88)**。AIはすぐ城Lv10まで
    育つので、指数を1より大きくすると城が勝利条件の皇都(4,000)の数倍という破綻が起きる
  ・**AIが自領へ進軍してくる**（非同盟のみ／領地3枚未満は保護／同時3部隊まで）。狙うのは
    守りが最も薄い1枚。最低25秒かけて進軍し、赤い実線＋標的タイルの警告リングで見える
  ・自領の防御 = 地形防御 + 本城Lv×160 + 兵舎Lv×120（＋研究）。守備部隊を足さずに
    「内政を伸ばせば守れる／放置すれば削られる」を成立させた
  ・`S.aiMarches[]` と `ai.defeated` の純粋な追加。ログ種別 raid / defend
- **[DONE] 研究ツリー [S21]（2026-07-27）** — 研究所を「貯めた資源の使い道」にする恒久バフ。
  SAVE_VERSION 12。内政/軍事/開拓の3系統×3段の直列、**同時に進められるのは1件**（普請と同じ方針）。
  研究所をタップ →「🔬 研究ツリー」から入る。💎で即完了も可能（普請と同じ単価）。
  ・効果は `getResearchBuffs()` が乗数として集計し、`researchMult(key)` で各所に掛かる
    （prod/cap/buildTime/power/casualty/tileDef/speed/troops/diamond の9キー）
  ・**軍師の役職バフを有効化**（研究時間 -25%）。`getAllianceBuffs()` は `Math.max` で集計するため
    減少系バフが握り潰されていた → `LOWER_IS_BETTER_BUFFS` を導入して min で集計するよう修正
  ・**注意**: 「徴兵制」は最大兵力を上げるので `finishResearch` を `withTroopRatioPreserved()` で
    包んでいる。包まないと最大だけ増えて充足率が下がり「研究したのに部隊が弱くなる」
  ・`getResearchBlocker()` は研究所Lv・前提研究を「他の研究が進行中」より先に返す
    （後にすると進行中の間すべてのノードが同じ理由になり、次の目標が読めなくなる）

- **[DONE] 戦争セリフ（Grok第3弾・2026-07-27）** — AIの攻撃・反撃に同盟員が反応するようになった。
  `warChat(kind, ctx)` が**話し手を先に決めてから** chatEvent に渡す（そうしないと sub＝性格別プールに
  何を渡すか決められない。ambient と同じ作法）。5キー計153本／チャット全体491本。
  onEnemyRaid（侵攻開始＝着弾前の警告）／onTileLost（奪われた・性格別）／onDefend（撃退）／
  onCastleFall（敵城陥落で一勢力が滅亡・性格別）／onCastleFail（城攻め失敗）。
  変数に `{enemy}`（敵対勢力名）を追加（chatFill は汎用置換なのでコード変更不要）。
  **城陥落時は `onPlayerCapture` を抑制する**（`m.targetAiId` で判定）。しないと滅亡の重い台詞の直後に
  「良い土地を押さえました」等の資源タイル向け祝辞が重なって台無しになる

- **[DONE] 忠誠度のバランス調整（2026-07-27）** — 「同盟AIがすぐ脱退する」という実プレイ指摘への対応。
  原因は**忠誠が下がる一方**で、上げる手段が「事件が起きた時になだめる」しか無かったこと。
  役職を与えても減衰が続くため、放置すれば必ず全員が抜けていた。
  **均衡値へ向かってドリフトする方式**に変更（`getLoyaltyEquilibrium` / `applyLoyaltyDrift`）。
  ・役職あり = 68＋実績ボーナス（領地数×0.4・最大+15）→ 68〜83で安定。事件も起きない
  ・役職なし = 40＋実績ボーナス。ただし **`INCIDENT_THRESHOLD - 3` で頭打ち**にする。
    ここを開放すると領地が増えた時点で役職なしでも不満を言わなくなり、
    同盟内事件が中盤以降まったく起きなくなる＝役職を配る動機も消えるため
  ・制圧／本城強化／侵攻の撃退で同盟員全員の忠誠 +3（`gainAllianceLoyalty`）
  ・**結末は残っている**: 放置を2回続けると脱退ラインを割って抜ける（実測で確認）

- **[DONE] カード合体（重複の救済）[S22]（2026-07-27）** — SAVE_VERSION 13。
  同じカードを**同じランクで5枚**集めると1枚に合体し `rank` が1上がる。
  ・rank は **実効レア度**を押し上げる（N→R→SR→SSR→UR）。URを超えたぶんは**超人**段階（最大3）
  ・`CARD_POOL`（静的定義）は書き換えない。所持カード側の `rank` から毎回導出する
    （`getCardRarity` / `getCardTranscend` / `getCardRankMult` / `cardPowerOf` / `cardLeadOf`）
  ・倍率は戦力と統率の両方に掛かる＝**合体すると最大兵力も増える**
  ・**カード一覧の集約キーは `defId+rank`**。defIdだけでまとめると強化した1枚と素の束が
    同じ山になり選べなくなる。`addCardToSquad`/`removeCardFromSquad` も rank を取る
  ・合体時は**編成中のカードを残す側に選ぶ**ので部隊が崩れない。消費されるuidは編成から外す。
    統率が変わるので `withTroopRatioPreserved()` で包む
  ・**注意**: 強化したRは素のSRより弱い（基礎ステータスが違うため。rankはレア度倍率だけを動かす）。
    ダブりの救済であって上位互換にはしない、という意図

- **[DONE] 幕舎と駐留 [S23]（2026-07-27）** — SAVE_VERSION 14。本城から遠い前線を維持する仕組み。
  ・**幕舎** = 自領の空地に建てる前線基地（上限 = 2 + 本城Lv×0.2）。`S.player.outposts[]`
  ・**駐留地** = 幕舎 ＋ 占領した要衝（城・関所・埠頭・皇都）＋ 本城。`squad.stationedAt`
  ・駐留の効果は4つ: ①守りに駐留戦力の50%が乗る ②**出撃起点になる**
    ③兵力の自然回復3倍 ④補充の食料が3割引（前線で立て直せる）
  ・**出撃起点は「到達範囲」ではなく「行軍距離」に効く**。到達判定は従来どおり自領全体からなので
    隣接ルールは変えていない。効くのは距離＝実測で36タイル→1タイル（120秒→8秒）
  ・**迎撃**: 占領した要衝に駐屯していると、射程内へ来るAIの侵攻を着弾前に削る。
    弓兵を含む部隊は射程2→4・威力0.25→0.4（＝「弓の遠隔攻撃」）。幕舎では迎撃しない
  ・`getMarchableTiles(range, origin)` に起点を足し、キャッシュキーにも含めた。
    マップの強調は**部隊ごとに起点が違う**ので `getMarchableUnion()` で和集合を別キャッシュする
    （単一キャッシュを起点違いで連続呼びすると毎回捨てられるため）
  ・**注意**: タイルを奪われた／放棄した時は `releaseLostGarrisons()` を呼ぶ。
    怠ると敵地に駐留したままになり出撃起点が敵地になる

- **[DONE] 防衛施設 [S23]（2026-07-27）** — 本城Lv10で解禁する固定防御。幕舎と**同じ配列**
  `S.player.outposts[]` に `kind` で入れる（`"camp"`=幕舎／`"fort"`=防衛施設。kind無しは幕舎扱い＝v14互換）。
  ・**自城か占領した関所から3タイル以内**の自領にのみ建てられる（守りたい場所が限定される）
  ・施設タイル +2,500 ／ 周囲2タイルの自領に +900（オーラは重ねがけ可）
  ・**兵を置かなくても射程3を迎撃**する。幕舎は駐留地、防衛施設は固定砲台、と役割を分けた
  ・`getStructAt`（種別を問わない）/ `getOutpostAt`（幕舎のみ）/ `getFortAt`（砦のみ）を使い分ける。
    建設ガードで `getOutpostAt` を使うと砦の上に幕舎を建てられてしまう（実際に踏んで修正）

- **[DONE] 同盟倉庫 [S24]（2026-07-27）** — SAVE_VERSION 15。加入AIが毎分わずかずつ資源を納める共通備蓄。
  ・納入 = AI城Lv × 1.2 × (忠誠/100) ／分／資源。**忠誠に比例する**ので、
    忠誠を保つ意味がバフ以外にも生まれる（[S19]の事件対応と噛み合う）
  ・容量 = 加入AI数 × 20,000。加入者が居なくなれば容量0＝備蓄も消える
  ・引き出しは**自分の倉庫容量まで**。溢れるぶんは同盟倉庫に残るので取りこぼさない
  ・同盟タブの再描画は `allianceSignature()` に備蓄を1万刻みで含めた
    （毎tick変わる値をそのまま入れると入力中のチャットが飛ぶため）

- **[DONE] 盟主の目標指示 [S24]（2026-07-27）** — SAVE_VERSION 16。硬い目標を同盟の力で削る。
  ・敵城・関所・埠頭・皇都をタップして「下知する」と、同盟員が包囲を進める（同時1件）
  ・包囲 = 加入AI数 × 1.4 × (忠誠/100) ／分（%）、上限45%。**防御に直接掛かる**
    （実測: 関所1,500→825／AI城9,103→5,007）
  ・目標を制圧するか自領になれば指示は自動で解ける
  ・`getTileDefense` の**早期returnしている固定防御の分岐（聖地・皇都・関所・埠頭・城）にも
    包囲を掛ける**こと。最後の `return Math.round(def)` だけに掛けると要衝に効かない
  ・チャットキー `onOrder` は空プールで置いてある（Grokに発注可能）

- **[DONE] 下知への応答と挨拶（2026-07-27）** — チャット全体611本に。
  ・`onOrder`（性格別・各10本）= 盟主の攻撃指示[S24]への応答
  ・`onPlayerSend` を**フラット配列から意図別オブジェクトへ変更**。`detectChatIntent()` が
    盟主の発言から morning/day/evening/night/greet/thanks/apology/praise/question/default を判定する
  ・**すべての sub を埋めること**。`resolveChatPool` は空サブキーだと全プールを平坦化するため、
    空にすると「おはよう」に「おやすみなさい」が返る取り違えが起きる
  ・`CHAT_INTENT_RULES` は上から順に評価するので**具体的な語を先に置く**
    （greetの「ございます」を先にすると「おはようございます」がgreetに落ちる）
  ・挨拶など意図が取れた発言には必ず応答し、それ以外は従来どおり8割で相槌

- **[DONE] チャット増量（Grok第5弾・2026-07-27）** — チャット全体773本（第6弾で883本）。
  頻出プールを厚くした: ambient 64→139／onPlayerCapture 17→42／onEnemyRaid 20→35／
  onDefend 18→33／onPlayerCastleUp 14→29／事件の解決3種と onJoin も増量。
  増量分は**新システム（兵種・兵力・地形・AIの侵攻・幕舎・研究・合体・同盟倉庫・包囲）に
  言及する内容**にしてもらったので、常時流れる ambient が今のゲームを語るようになった。
  ・**注意**: 増量納品は既存行との重複チェックが必須。第5弾は185本中23本が既存と同一で、
    `onJoin` は10本すべてが再送だった（採用0）。突き合わせずに追記すると抽選が偏る

- **[FIX] マップ絵文字の中央ズレ（2026-07-27・iPhone実機報告）** — 鉄鉱の⚙️がマスの右寄りに見える問題。
  原因は canvas の `textAlign:"center"` が**送り幅(advance)の中心**に合わせる仕様で、
  ⚙️(U+2699+FE0F) のような**テキスト既定＋異体字セレクタ**の文字は環境により
  送り幅と実際のインク位置が食い違うこと。**Windows/Chromeでは再現しない iOS固有**。
  `fillEmojiCentered()` を追加し、送り幅ではなく**インクの箱の中心**で揃えるようにした。
  `measureText` は重いのでフォント＋文字ごとに一度だけ測ってMapにキャッシュする。
  同じ字形クラスは他に ⛩️聖地 / ⛰️険山 / 🛡️関所 があり、まとめて直っている

- **[DONE] タイル詳細シートの3層化（2026-07-27・ChatGPT確定仕様）** — Gemini第3回検品の最重要指摘への対応。
  情報が増えるほど「出撃」ボタンが下へ逃げる構造を作り直した。**SAVE_VERSION変更なし**。
  ・`#sheet.tile-mode` を **固定ヘッダー／スクロールする本文／固定アクション** のflex3層にした。
    `position:sticky` は使わない — `#sheet` 自体が transform で開閉するため、
    **アクションをスクロール領域の外へ出す**方が iOS Safari で堅牢（仕様書の判断）
  ・判断に要る結論だけを上に出し、内訳は `<details class="sheet-detail">`（HTML標準）へ畳む。初期は全て閉
  ・**部隊選択はドロワー化**。「⚔️出撃」を押すと固定アクションの直上に部隊一覧が開く。
    従来は最大3部隊を本文へ縦に並べていたため、これだけで高さを大きく消費していた
  ・**施設の建設は固定アクションに入れない**（反復するコア操作ではないため詳細の中）
  ・出撃不可でもボタンを消さず disabled にし、理由を1行出す（「なぜボタンが無いの？」を防ぐ）
  ・**実測: 敵の関所 564px → 333px**。375×667で出撃ボタンがスクロールなしで見える
  ・`showSheet(html, mode)` を唯一の入口にし、**7つのシート利用箇所すべてを経由**させた。
    直接 `innerHTML`＋`add("open")` を書くと tile-mode が残って他シートのレイアウトが壊れる
  ・放棄に確認ダイアログを追加（固定化で押しやすくなったぶんの誤操作対策）

- **[DONE] Gemini第3回の残り3件（2026-07-27）** — SAVE_VERSION 17。
  **【問題3】研究への導線** — 「城タブ→研究所→研究ツリー」の3階層を、
  城タブ最上部の常設行から**1タップ**で開けるようにした（`researchRowHTML`）。
  研究できる件数・研究中の残り時間がその場で見え、取れる研究があれば枠が光る。
  `citySignature()` に研究状態を含めて、行が古いまま残らないようにしている。
  **【問題2】新機能の気づき [S25]** — 2段構え。
  ・**解禁の告知**: 初めて条件を満たした瞬間に1度だけトースト＋記録。
    tickは毎秒なので `HINT_INTERVAL_SEC`(25秒) の間隔を空ける。空けないと複数が一瞬で流れて読み飛ばされる
  ・**タブの●バッジ**: 今できることがある間だけ点き、やれば自然に消える。
    未読件数ではないので数字ではなく●にした
  ・対象は 合体／部隊増設／研究／幕舎／防衛施設／同盟倉庫／下知 の7つ。
    「今できるか」は毎回状態から算出し保存しない（規約2）。保存するのは告知済み・使用済みの2つだけ
  ・**旧セーブは使用済みの機能に done を立てて移行**する。今さら「使えます」と言わないため
  ・指南中は告知もバッジも出さない（手順に集中させる）
  **【改善案③】勝利後の統治フェーズ** — 皇都制圧で目的が消えないようにした。
  ・バナーが「残る反乱軍 N 勢力を掃討せよ ｜ 制覇率 X%」に変わり、目標を出し続ける
  ・制覇率 = 領有タイル / 通行可能タイル(自城を除く1,482枚)。母数は `RUNTIME` に持ち保存しない
  ・全勢力を滅ぼし全タイルを領有すると **天下統一** を1度だけ祝う（`flags.unifiedAt`）
  ・統治フェーズ中は制圧のたびに制覇率のトーストが出る

### 🗺️ 拡張ロードマップ（2026-07-27・総監督指示）

実機で一区切りが付いたあとの追加要素。**上から順に1つずつ**積む。

| # | 項目 | 状態 |
|---|---|---|
| 1 | **必殺技（戦法）システム** | ✅ 完了（[S26]・SAVE_VERSION 18） |
| 2 | **カードのダブルタップで詳細** | ✅ 完了（#1と同時） |
| 3 | **新知将武将の追加** | 📋 設計済み（[DESIGN_S2.md](DESIGN_S2.md)）・確認待ち |
| 4 | **レベル上限20の解放** | 📋 設計済み（同上）・確認待ち |
| 5 | **Lv10以降の戦略性** | 📋 #3#4に統合して設計済み（第二ステージ＝異民族） |
| 6 | **難易度選択** — 上級はAI数を増やし、資源を積極的に奪う | 未着手 |
| 7 | **期間限定イベント＋ピックアップガチャ** | ✅ 完了（[S28]・SAVE_VERSION 28） |
| 8 | **BGM・効果音** | ✅ 完了（[S29]・SAVE_VERSION 27）。WebAudio生成音でサイズ増加ゼロ |
| 9 | **ガチャ演出** | ⛔ **撤去**（2026-08-06）。実機で点滅と操作の不安定を招いたため差し戻し。排出音のみ残す |

**#1の仕様解釈（要確認）**: 「SSRとURはレベル10から」を
**「必殺技レベルの上限がレア度で変わる（SSR/URは10まで）」**と解釈して実装した。
別の意図（例: SSR/URの排出が本城Lv10から）であれば直す。

- **[DONE] 必殺技（戦法）システム [S26]（2026-07-27）** — SAVE_VERSION 18。ロードマップ#1・#2。
  ・**必殺技11種**（攻/防/謀/補）を26枚全カードに配分。効果は `power / casualty / speed / siege / guard / pierce`
  ・**低レア武将を素材にして必殺技Lvを上げる**（三國志真戦の戦法強化）。
    SSR以下のみ素材化でき、**編成中のカードは素材にできない**。URは素材にならない
  ・**Lv上限はレア度で変わる**（N3/R5/SR7/SSR10/UR10）。上限は**実効レア度**で見るので、
    合体([S22])でレア度が上がれば必殺技の上限も伸びる
  ・経験値 = 素材のレア度（N1/R2/SR4/SSR8）。Lv10までの累計122＝低レア約60枚ぶんの吸収先
  ・`pierce`（火計・水計・混乱）は**敵防御を無視**する。UIの見積りと戦闘判定で同じ式を使うため
    `getEffectiveDefense(ti, squadId)` に集約し、出撃時の値を march に持たせて解決時に効かせる
  ・効果は部隊内で**加算**（同じ技を重ねる価値がある）。暴走を防ぐため `CONFIG.SKILL_CAP` で上限
  ・**カードのダブルタップで詳細シート**。iOS Safari では dblclick が不安定なので、
    **単タップの実行を260ms遅らせ、2打目が来たら取り消す**方式で自前判定している
  ・詳細シートは3層構造（[S14]の `showSheet(html,"tile")` を再利用）。素材の選択は±で行い、
    「→ Lv5 まで上がります」と結果を先に見せる

- **[DONE] 建物レベル上限20と第二ステージのゲート（2026-07-27）** — SAVE_VERSION 20。DESIGN_S2.md 6-3。
  ・**`getBuildingMaxLevel(type)` を新設**。静的定義 `BUILDING_DEFS[].maxLevel`(15) は触らず、
    `S.stage === 2` の時だけ 20 を返す（規約2）。**上限解放は第二ステージの報酬**
  ・第二ステージは **皇都制圧（or聖地勝利）かつ本城Lv10** の両方で開く（`stageTick`）
  ・**単に maxLevel を20にすると Lv16→17 で詰む**。本城コスト792.6k > 倉庫容量668.6k のため。
    本城コストの成長率を Lv11以降 ×1.35 に、倉庫容量に `(Lv/10)^2.5` を掛けて解決
  ・**Lv1〜10は従来と完全一致**（コスト・容量・生産の3つを実測で突き合わせ済み）。
    分岐は `CONFIG.HIGH_LV_FROM = 11` 以降にだけ効くので既存の体験は変わらない
  ・建物生産は Lv11以降 +8%/Lv。線形のままだと倉庫容量1.5Mに対して遅すぎるため

- **[DONE] 異民族と第二ステージ本体 [S27]（2026-07-27）** — SAVE_VERSION 21。DESIGN_S2.md 6-4。
  ・**`S.aiUsers` に `tribe: true` で足すだけ**で既存のAI侵攻・城攻略・防御計算・マップ描画が
    そのまま動く。固有の挙動は既存関数への分岐で済ませた（攻撃×1.4／確率0.65／同時侵攻5枠）
  ・**3波・計8勢力**（Lv12×3 → Lv14×3 → Lv16×2）。マップ北端(y≤3)から流入し、
    `aiExpand` の候補選択を異民族だけ「皇都に最も近い」に変えて南下させる
  ・**異民族は地域の壁を越える**（`isAIExpandableTile(ti, ai)` に ai を足して分岐）。
    通常AIは自地方に閉じこもるまま
  ・**首都堅守がルール**: 全異民族を討伐しても皇都を奪われていれば勝利にならない。
    奪われると `#winBanner.danger` の赤バナーに切り替わる
  ・**注意**: マップのバッジは指南中に抑制される（`TUTORIAL_HIDE_ADVANCED_UI`）。
    描画の検証をする時は指南を完了させてから測ること（一度これで詰まった）
- **[DONE] 異民族のセリフ（Grok第6弾・2026-07-31）** — **チャット全体883本**（24プール）。
  DESIGN_S2.md 6-5。`onTribeWave` 36／`onCapitalLost` 29／`onCapitalRetaken` 15／`onTribeCleared` 30。
  ・納品111本中、既存と重複したのは**1本だけ**。第5弾の反省（23本重複）を発注文に
    「★既存のセリフと重複しないものだけ」と明記した効果が出た。次回以降も必ず書くこと
  ・**`chatEvent` に `filter` を足した** — `onTribeWave` の36本中11本が「また来たか」
    「まだ来るのか」「波が続く」など**前の波があった前提**の言い回しだった。
    第1波は第二ステージが開く一番印象に残る場面なので、そこで出ると台無しになる
    （実測で30回中9回引いていた）。`tribeTick` の第1波だけ `TRIBE_FIRST_WAVE_OK` を渡す。
    絞った結果が0本なら元のプールに戻すので、**フィルタで無言になることはない**
  ・**教訓**: `{wave}` のように「何回目か」が入る変数を発注する時は、
    セリフが暗黙に2回目以降を前提にしていないか初回で必ず引いてみること
- **[FIX] `AI_NAMES` の混入** — 7番目が `"静waves"` になっていた（`"静波"` に修正）

### 🎮 実機フィードバック（2026-07-31・異民族討伐までクリア）
「怒濤の異民族の侵略に夢中になった」「ノーマルはカタルシスを味わうのに丁度良い難易度」との評価。

- **[DONE] 育成の＋でスクロールが飛ぶ** — `showSheet` は innerHTML を丸ごと差し替えるので、
  ＋のたびに `.sheet-body` と `.feed-list` のスクロールが0に戻り、押したボタンが画面外へ逃げていた。
  ・`openCardSheet` は**同じカードを開き直す時だけ**位置を持ち越す（対象を照合してから復元する。
    照合しないと別のカードやタイルを開いた時に無関係な位置を引き継ぐ）
  ・`scrollTop` は 0 も有効な値なので `!= null` で見ること（`if (keepScroll)` だと先頭が保存されない）
- **[DONE] レベル上限の赤表示** — `.lv-max`（赤文字）と `.lv-badge.max`（赤バッジ）。
  武将レベル・必殺技レベルの両方＋カード一覧のバッジ。「これ以上は素材を捧げても伸びません」
- **[DONE] ピンチイン／ピンチアウト** — 2本目の指で パン→ピンチ へ切り替え、
  **掴んだ2点の中点を動かさない**ように拡縮する。`zoomAt(z, sx, sy)` を新設し
  ±ボタン・ホイールも同じ経路に統一。`#mapCanvas` は元から `touch-action:none` なので追加不要だった。
  ・**掴んだ時の倍率を基準にする**（毎フレーム差分を掛けると誤差が溜まる）
  ・2本目を離した直後は残った指で基準を取り直す（しないとマップが飛ぶ）
  ・ピンチ直後にタップ扱いされないよう `moved` を立てておく
- **[DONE] 難易度選択（2026-07-31）** — SAVE_VERSION 22。DESIGN_S3.md 7-1。
  ・難易度で変わる値は**すべて `CONFIG.DIFFICULTY` に集め、CONFIG直下からは取り除いた**。
    同名を両方に残すと「どちらが効いているのか」が読めなくなる。参照は `diff("KEY")` のみ
  ・**ワールド生成中は `S` がまだ無い**ので `diff()` が使えない。
    `generateAIUsers(world, difficulty)` と引数で渡す。`boot()` は
    `newGameState(await chooseDifficulty())` で、**選び終わってから世界を生成する**
  ・上級 = AI14勢力（5/5/4）／攻撃確率0.60・0.32・0.10／同時侵攻4〜7／
    異民族13勢力3波（城Lv14・16・18／攻撃補正×1.8＝10,012・11,910・13,880）／最終波は四方から
  ・出現辺は `TRIBE_WAVES[].edges` に持たせ `isOnSpawnEdge()` で判定（北端固定 `TRIBE_SPAWN_MAX_Y` は廃止）。
    南下ロジックは方角に依存しないのでどの辺から湧いても皇都へ収束する
  ・**ノーマルは実装前と完全に同一**（AI10・4/4/2・波3/3/2・6,373/7,787/9,263 を実測で突き合わせ済み）。
    既存セーブは移行で必ずノーマルになる
  ・**[FIX] `AI_NAMES`/`AI_COLORS` が10個しかなかった** — 上級では4勢力が「AI10」＋灰色 `#888` で
    並び見分けが付かない。14個に拡張。追加色は既存＋異民族色から**RGB距離85以上**離れた
    色相の空き（78°/177°/240°/300°）から選定。**難易度を増やす時はここも必ず伸ばすこと**
- **[DONE] 築城 — 柵・駐屯所・櫓 [S21/S23]（2026-07-31）** — DESIGN_S3.md 7-3。**SAVE_VERSION据え置き**。
  ・**新しい配列を作らなかった**。`S.player.outposts[]` は元から `kind` で種別を持つので、
    そこに3種を足すだけで `getStructAt`(重複防止)・`removeStruct`・`releaseLostGarrisons` が
    そのまま効く。配列を分けるとこの3つを両方に書くことになる。**結果スキーマ変更なし**
  ・`STRUCT_DEFS` に**5種すべてを集約**（値は CONFIG からそのまま引く）。
    既存2種の数値が変わっていないことを11項目で突き合わせ済み
  ・🧱柵+250／⛺幕舎+400・駐留0.5／🛖駐屯所+1,200・駐留0.8／
    🏹櫓+800・周囲+400/3・迎撃-800/射程4／🗼防衛施設+2,500・周囲+900/2・迎撃-1,200/射程3
  ・**絵文字はタイル種別（🌾🪵🪨⚙️⛩️🏯⛰️🌊⚓）と重ねないこと**。マップではタイルの上に
    重ねて描くので、被ると地形と施設の区別が付かなくなる
  ・研究に**築城枝**（柵塁Lv2→駐屯制Lv7→望楼術Lv11）。`effect.unlock` は数値バフではないので
    **`getResearchBuffs` で非数値を弾く**必要がある（混ざると倍率が NaN になる）
  ・**[FIX] 幕舎の枠を配列全体の長さで数えていた**（既存のバグ）。防衛施設を建てるほど
    幕舎の枠が減っていた。`countStructs(kind)` で種別ごとに数えるよう修正
  ・**[FIX] `.stack-btn` でボタンが縦潰れ**。±用の30px固定幅クラスなので `.train-btn` を使う
    （[S26]の育成ボタンで一度踏んだのと同じ罠）
  ・呼び出しの無くなった `getOutpostAt`/`getFortAt`/`getFortAuraDef` 等は削除した。
    残すと「駐留できる施設か」を見たい所で `getOutpostAt` を使い駐屯所を取りこぼす
- **[DONE] 編成のドラッグ＆ドロップ（主将・副将）[S09/S13]（2026-07-31）** — DESIGN_S3.md 7-6。
  **SAVE_VERSION据え置き**。`sq.cardUids` は元から順序付き配列なので、枠に名前を付けて
  並べ替えられるようにしただけ。データモデルは変えていない。
  ・**`getSquadTroop` を多数決から「主将（先頭の1枚）」基準に変えた**。多数決だと
    知将を混ぜた時に狙わない兵種へ化ける。先頭が決めるなら意図した編成が組める
  ・**既存セーブは主兵種が変わる部隊がありうる**（手動編成の並びは「追加した順」のため）。
    自動編成は戦力の降順なので主将＝最強カードになり従来とほぼ一致する。
    主兵種の表示に「（主将）」を添えて追えるようにした
  ・操作は**ドラッグと「2枠タップ」の両方**を用意した。タップ経路は
    **iPhoneで指が滑った時に並べ替え不能にならないための保険**（D&Dだけにしない）
  ・ドラッグは `setPointerCapture` ＋ `document.elementFromPoint` で落下先を判定する
    （城タブの `cityAttachDrag` と同じ作法）。`renderCards` のたびに `lineupAttachDrag()` を貼り直す
  ・**並べ替えに `withTroopRatioPreserved` は不要**。統率の合計が変わらない＝最大兵力が動かないため。
    逆に言えば、主将に統率ボーナスを付けるなら包みが必要になる（今回は付けていない）
- **[DONE] 異民族編の開始許諾 [S27]（2026-08-01）** — SAVE_VERSION 23。DESIGN_S3.md 7-2。
  ・**段階を2つに割った**。建物レベル上限20の解放は従来どおり自動で、
    **襲来だけ宣戦してから**始まる。上限20を先に受け取って育ててから挑める
  ・`canStartTribes()` / `startTribeInvasion()` / `offerTribeStart()` の3つ。
    打診は自動で1度だけ（`S.tribe.offeredAt`）。断っても `#winBanner` の常設ボタンから始められる
  ・**他のシートが開いている間は打診しない**（次のtickへ回す）。初回は初陣指南が開いているため1tick遅れる
  ・ダイアログの波構成・攻撃戦力は `diff("TRIBE_WAVES")` から生成する。
    文面に数値を直書きすると難易度で嘘になる
  ・`openConfirmSheet(title, body, okLabel, onOk, cancelLabel)` を新設。`window.confirm` は
    見た目が浮くので使わず既存シートに載せた。`closeSheet` で `_confirmOk` を捨てる＝閉じる＝やめる
  ・**移行の要点**: `wave > 0` の旧セーブを**打診済み扱い**にする。しないと進行中の
    プレイヤーが「始めますか？」と訊かれる
  ・**検証の注意**: 10秒ごとの自動保存があるので、細工したセーブを書いた直後に
    `localStorage.setItem` を潰してからリロードすること。しないと生きている `S` に
    上書きされ、移行経路を通らないセーブを読む（一度これで誤検証した）
- **[DONE] 兵舎Lv20で第4部隊（2026-08-01）** — DESIGN_S3.md 7-4。**SAVE_VERSION据え置き**。
  `SQUAD_UNLOCK_BARRACKS_LV: [1,5,10,20]` の定数1行。`ensureSquads()` の名前配列に
  「第四隊」が既にあり `getSquadSlots()` は配列長を見ているのでそのまま解禁される。
  実測: Lv19→3部隊／Lv20→4部隊。未解禁の間は `🔒 兵舎Lv20` が薄く並ぶ
- **[DONE] 盟主の容姿（2026-08-01）** — SAVE_VERSION 25。男3・女3の計6枚から開始時に選ぶ。
  ・**名前は入力させず `PLAYER_NAME = "盟主"` で固定**。主役は率いられる武将たちで、
    盟主はその采配者、という立て付け。表示は「あなた」から「盟主」に統一した
  ・`AVATAR_DEFS`（[S02]）は id/label/sex/emoji/imageKey だけの静的定義。
    `S.player.avatar` にはidだけ持つ。**アート未納品でも絵文字で完全に動く**
  ・順番は**容姿 → 難易度**。難易度は世界の生成前に確定させる必要があるので後には置けない
  ・出るのは同盟タブの盟主の行（38pxの円）とチャットの自分の発言（16pxの円）。
    **円マスクなので、アートは四隅に重要なものを置けない**（IMAGE_PROMPTS.md §5に明記）
  ・**旧セーブは未選択(null)のまま**にして押し付けない。旗の絵文字で代用され、
    同盟タブの「🏳️ 姿を変える」からいつでも選べる。**新規時のみの導線にすると、
    既に遊んでいるセーブでは永久に使えない機能になる**ので、後から変える口を必ず用意すること
  ・**アート6枚 同梱済み**（2026-08-01）。`art/avatar_*.jpg`（192px JPEG q82・6枚で53KB）。
    振り分けは**背景の色相で機械的に確定**できた（濃紺228°/深紅3°/白紫256°/若草50°/紅5°/水色202°）。
    赤系2枚だけ色相が近いので実物を見て判別した。**納品順とファイル名の対応は当てにせず、
    色相か実物で突き合わせること**
  ・**女3枚は描き直して差し替え済み**（2026-08-01・IMAGE_PROMPTS.md §5-2）。
    初回は中性的で女性として読みにくかった。
    原因は2つあり、**どちらも次回以降の教訓**:
    ①**年齢しか書かず顔立ちを指定しなかった**（"woman in her thirties" だけ）ため
      画風のデフォルト（中性的な美形）に流れた
    ②**髪色の指定が2枚とも無視された**（f2=紅・f3=銀白で発注したのに両方黒髪で届いた）。
      結果、女性3枚が全員暗い髪になり16pxの円で見分けが付かない。
      **髪色は独立した一文で強調する**こと
    ・差し替え時は**背景の色相を初回どおりに保つ**よう発注文に明記してある
      （色相でファイルを振り分けているため、変わると突き合わせができなくなる）
- **[DONE] 異民族の築城 [S27]（2026-08-01）** — SAVE_VERSION 24。DESIGN_S3.md 7-5。
  **これで DESIGN_S3.md の6項目すべてが完了**。
  ・保存先は**タイル自身 `world.tiles[].struct`**。`ownerId`/`level` と同じタイルの状態なので
    配列を別に持つより素直で、参照が **O(1)** で済む。`getTileDefense` はAIの攻撃対象を選ぶ
    ソートの中で何度も呼ばれるため、ここで全施設走査をしたくない
  ・**移行では何もしない**。「未築城＝フィールドが無い」で始まるので補完は不要
    （1,600タイル全部に null を書くとセーブが膨らむ）
  ・**効くのは自タイル防御だけ**。プレイヤー側の柵・櫓が持つオーラと迎撃は持たせない
    （同じ理由。「取り返しに行くと硬い」という狙いはこれで足りる）
  ・`getTribeStructAt()` が**異民族が保持している間だけ**有効にする。滅亡後・通常AI領・
    プレイヤー領では効かない（3通りとも実測）
  ・領地3〜5枚で🧱柵／6枚以上で🏹櫓。置くのは**皇都に最も近い自領**＝進軍路の先端を固める
  ・**プレイヤーが制圧したら取り壊す**（接収しない）。残すと `S.player.outposts` に無い施設が
    自領に居座り、建設上限の外側で防御だけ増える
  ・ペースは式で 1勢力あたり約6.3分に1つ（攻撃0.65が優先、残りの30%）。上級13勢力で毎分約2つ
  ・**[FIX] マップ描画の差し込み位置を誤った** — 敵領の施設なのに**プレイヤー領のブロックの中**に
    書いてしまい（かつ index を `ti` でなく `i` と書いた）永久に描かれなかった。
    自領のマークとは独立した `if` に直した

- **[DONE] 上級の序盤猶予（2026-08-01・実機フィードバック）** — DESIGN_S3.md §14。
  上級を最初から遊ぶと**拡張型AIの攻撃確率0.60が序盤に効きすぎ、確保した土地をすぐ奪われて
  足場が作れない**。`diff("AI_ATTACK_MIN_CASTLE_LV")` を足し、**本城がこのレベルに届くまで
  AIはプレイヤー領を奪えない**ようにした（上級のみ5・ノーマルは0＝実装前と同一）。
  ・猶予であって免除ではない。Lv5以降は通常どおり狙われる
  ・既存の「領地3枚未満は保護」はそのまま併存。異民族は本城Lv10以降にしか出ないので掛からない
  ・**難易度選択画面に明記した**。見えない仕様にすると「なぜ急に襲われ始めたのか」が読めない
  ・実測（領地24枚・隣接AIあり・60回試行）: 上級 Lv1/3/4 は 0/60、Lv5以降は 60/60。
    ノーマルは全レベルで 60/60（不変）

- **[DONE] 期間限定イベント [S28]（2026-08-01）** — SAVE_VERSION 26。ロードマップ#7。
  一定のプレイ時間ごとに開催され、期間中だけ **UR 1%→2% / SSR 4%→5%**（＋1pt）になる。
  ・**時刻は絶対epoch msで持ち `shiftTimersForOffline` でずらす**＝実時刻ではなく
    **プレイ時間**で進む。オフライン進行を止めている本作で実時刻基準にすると、
    遊んでいない間に開催が終わって気付けない。ロードマップで保留にしていた判断はこれで決着
  ・**増やしたぶんは下位レア度から比例で引く**（合計を1に保つ）。`rollRarity` は
    `Object.entries` を順に減算していく実装なので、合計が1でないと後段の確率が狂う
  ・静的な `CONFIG.GACHA_RATES` は書き換えず `getGachaRates()` が毎回合成する（規約2）
  ・周期は 初回20分／以後は終了から90分／開催25分（`CONFIG.EVENT`）
  ・イベント定義は3種（英傑降臨祭・群雄集結・星辰の宴）を順に巡る。
    **画像は全イベント共通の1枚**（`event_banner`・2026-08-06 同梱済み）で、名前と口上だけ切り替える。
    **人物を描かせない**のが要点（特定武将に見えると「その武将のピックアップ」と誤読される）
  ・開催中はガチャタブに●バッジ。確率表は**実効値**を出し、上がっている行は▲＋強調色
  ・**ガチャタブは毎tick全再構築しない**（2026-08-06）。`gachaSignature()` が変わった時だけ
    `renderGacha()` を呼び、毎秒変わる残り時間と所持ダイヤは `updateGachaLive()` が
    span を書き換えるに留める。同盟タブと同じ作法。
    **ここを全再構築に戻すとパネル内の img が毎秒作り直される**（ガチャ演出の点滅と同根）
  ・実測（20万回）: 通常 UR 1.02% / SSR 3.94% → 開催中 UR 1.97% / SSR 5.03%
  ・**ピックアップ武将（2026-08-06・SAVE_VERSION 28）** — 開催ごとにUR1枚が選ばれ、
    その武将だけ出やすくなる。`S.event.pickup` に defId を持つ。
    ・**レア度の中の内訳だけを変える**。レア度そのものの確率は動かさない
      （動かすと `RATE_BONUS` と二重に効いて排出設計が読めなくなる）。
      `rollCardOfRarity()` が、そのレア度が出た時に `PICKUP_SHARE`(0.5) でピックアップを返す
    ・**表示確率は `rate × SHARE` では足りない**。ピックアップ枠を外した残り半分でも
      均等抽選で同じ武将が当たるため、`rate × (SHARE + (1-SHARE)/n)` になる。
      最初 1.000% と表示していたが実測は1.099%で、式を直した（現在1.11%表示）
    ・実測（30万回）: 開催中 その武将1.099%／他のUR各0.11%／UR合計1.98%。
      終了後 その武将0.11%／UR合計0.98%。**平常時の排出は一切変わらない**
    ・**前回と同じ武将は選ばない**（`rollPickupCard(prevId)`）。8回連続で確認
    ・**旧セーブの開催中には生えない**。途中から急に付くと、それまでの排出と辻褄が合わない
    ・アートは**その武将のカードアートを流用**する（バナーを増やさない）

- **[DONE] BGM・効果音 [S29]（2026-08-06）** — SAVE_VERSION 27。ロードマップ#8。
  **音源ファイルを一切持たない**。WebAudioのオシレータで合成する。
  増えたのは**コードの約15KBだけ**（1,771→1,786KB）。base64で数十秒のBGMを積めば
  数百KB〜MB単位で増えるので、単一HTML制約下ではこの方式以外は取りにくい。
  ・**iOS/Safariはユーザー操作の中でしか AudioContext を走らせない**。生成を最初のタップまで
    遅らせ、`AUDIO.unlock()` を `pointerdown` に1本だけ張る（何度呼んでも安全）。
    起動時に鳴らそうとしても失敗するだけなので、鳴らす条件は1箇所に集約する
  ・**効果音は `toast(msg, kind)` に集約**した。`SE_BY_LOG_KIND` が [S20] のログ種別から
    引くので、**新しい種別を足す時はこの表に1行足すだけ**で鳴るようになる。
    各所に `AUDIO.se()` を撒くと鳴る場面と鳴らない場面が散らかって管理できなくなる
  ・BGMは**先読みスケジューラ**（`setInterval` で少し先の音を `ctx.currentTime` 基準に予約）。
    `setTimeout` で1音ずつ鳴らすとタイミングが揺れる
  ・音階は**ヨナ抜き五音音階**。どの音を重ねても濁らないので、ランダムに音を選ぶ
    生成BGMでも不協和にならない。曲想は平時／交戦中／皇都陥落中の3種を戦況から自動で選ぶ
  ・`exponentialRampToValueAtTime` は**0に落とせない**ので必ず微小値(0.0001)で止める
  ・裏に回ったらBGMを止める（電池と、戻った時の唐突さ対策）
  ・設定はマップの🔊から。既定はオン。`S.settings` は **false のみをオフとして扱う**
  ・実測: 全9種の効果音が残響(0.0158)を超える出力を持つ（capture 0.066／gachaUR 0.097）。
    BGMは平時・交戦中とも平均0.011で継続、オフで0.0004まで落ちる。
    `toast` の kind から鳴り、kindなし・未登録kind（offline/tutorial）は鳴らない

- **[REVERTED] ガチャ演出 [S30]（2026-08-06 実装 → 同日撤去）** — 実機で
  **カードが点滅し、目が疲れる／ボタンの反応が不安定になる**という報告を受けて差し戻した。
  **効果音（排出音）は残している**。
  ・**原因は排出結果のCSSアニメーション**。`renderGacha()` は tick から毎秒呼ばれて
    `innerHTML` を作り直すため、`animation: cardin` が**毎秒再生し直されて点滅**していた。
    さらに毎秒10枚ぶんのアニメが走るので、描画が重なって操作の反応も落ちた
  ・**教訓: 毎tick再描画されるパネルにCSSアニメーションを載せてはいけない。**
    [S13]の `renderGacha` / `renderCards` / `renderCity` は tick から呼ばれる。
    アニメを付けるなら、**再描画されない要素**（オーバーレイ等）に載せるか、
    同盟タブのように「構成が変わった時だけ再描画する」仕組みを先に用意すること
  ・撤去したもの: `[S30]` 一式・`#gachaFx` オーバーレイ・`playGachaFx` ・
    `_gachaBusy`・`#gachaResult .unit-card` のアニメCSS・`animation-delay`
  ・残したもの: `doGacha` 末尾の排出音（最高レア度で1回だけ鳴らす）
  ・`UIA.gacha` は演出前の同期版に戻した（`async` も外した）

- **[DONE] 制圧エフェクト [S14]（2026-08-07）** — **SAVE_VERSION据え置き**。
  タイルを取った瞬間に、広がる輪＋弾ける粒＋マスの一瞬の発光を出す。
  ・**canvasに描くだけでDOMは触らない**。マップは元から15fpsで再描画されているので、
    ここに載せるのは安全（毎tick再描画されるパネルにCSSアニメを載せて点滅した
    2026-08-06 の件とは別の場所）
  ・置き場は **`RUNTIME.mapFx`＝保存しない**。セーブに入れると、閉じて開き直した時に
    古い演出が後から再生されてしまう。マップを見ていない間のぶんは期限切れで消える（それでよい）
  ・種別は2つ。資源タイルは `capture`、**AI城を落とした時は `castle`** で輪も粒も大きくする
  ・**輪は線を細くしながら広げる**。太いまま広げると消える方向が読めず散らかって見える
  ・粒は6〜10個で十分（15fpsなので多くしても粒として見えない）
  ・`MAP_FX_MAX`(12) で上限を切る。連続制圧で溜め込まない。画面外は描画を飛ばす
  ・実測: 制圧で1個発火し900msで自動的に消える。40個入れても12個で頭打ち。
    城陥落は `castle`／資源タイルは `capture`／**出兵に失敗した時は出ない**。
    セーブ文字列に `mapFx` は含まれない

### 💡 今後の追加案（2026-07-27・実機プレイのフィードバック）
実機でほぼ制覇まで遊べる完成度との評価。以下は「さらに面白くする」ための案。

1. **難易度選択** — 上級でAI勢力数を増やし、資源をより積極的に奪いに来るようにする。
   `CONFIG.AI_COUNT` / `AI_ATTACK_CHANCE` / `AI_ATTACK_POWER_BASE` / `AI_REGION_COUNTS` を
   難易度プリセットで束ねる形が素直。新規ゲーム時に選ばせ、`S.world.difficulty` に保存する
2. **期間限定イベントとピックアップガチャ** — 実際の課金ゲームのように定期イベントを発生させ、
   期間中だけ強力な武将が排出される。**オフライン進行を止めている**（`OFFLINE_PROGRESS:false`）ので、
   実時刻ベースの期間判定にするか、プレイ時間ベースにするかは要検討
3. **レベル上限の解放（10→20）** — 現在 `maxLevel` は建物ごとに設定。上限を上げるなら
   コスト曲線（`CASTLE_COST_GROWTH` 等）と生産・防御の伸びを再計算しないと、
   終盤が単なる待ち時間になる

### ⬜ 未実装（優先度順）
1. **カード重複の救済** — 強化素材/限界突破。現状は枚数表示のみ
2. **同盟の深掘り** — 📋 [DESIGN_S4.md](DESIGN_S4.md) 承認済み。
   ✅ 同盟名変更 ／ ⬜ **加入AIの援軍（本命）** ／ ⬜ 同盟ミッション ／ ⬜ AI同士の戦争
   ・**[FIX] 同盟名の見出しが未エスケープだった**。初期値が固定文字列の間は問題ないが、
     ユーザー入力になった瞬間に穴になる。`escapeHtml()` を通した
   ・`allianceSignature()` に名前を含めること（含めないと次のtickで古い見出しに戻る）
   ・`{ally}` を含むセリフは7本。加入時と挨拶で必ず名前を呼ぶ
3. **駐留部隊による防衛** — 現在の自領防御は本城/兵舎Lvと研究のみ。部隊を置いて守る案は未着手
4. **セーブのエクスポート/インポートUI**
5. **効果音・演出** — ガチャ演出、制圧エフェクト
6. **index.html のサイズ** — アート同梱で 1.43MB。`sw.js` はネットワーク優先だが条件付き
   リクエスト（304）で2回目以降は転送されないため実害は初回のみ。重いと感じたら
   カードを192pxに落とす（約1.15MBまで縮む。表示枠139pxなので実用上ほぼ劣化なし）

---

## 6. 4AI共同制作体制

Sora（総監督）のもと、4つのAIが専門スタッフとして分担する。

| AI | 役割 |
|---|---|
| ChatGPT | ゲームディレクター / システム・アート設計・画像生成・世界観統括 |
| **Claude Code** | **リードエンジニア（本番コードを編集するのはClaudeのみ）** |
| Gemini | UX・映像・ビジュアル分析（画像QA / プレイ動画のUX分析） |
| Grok | 世界シミュレーション / NPC・チャット脚本 |

### 鉄則
- **`index.html` を編集するのは Claude だけ**。他3AIは提案・仕様・素材・レビューまで。
  各AIが別方式で書き換えると設計が崩れるため。
- ChatGPTからの実装依頼は**確定仕様**の形で受け取る。必要なのは
  ①触るセクション ②`CONFIG`に足す定数と意味 ③`S`スキーマ変更とマイグレーション方針
  ④受け入れ条件（完成の定義）。
- Geminiの分析結果は、そのままでは実装指示にならない。「だから何をどう出すか」まで
  ChatGPT側で仕様化してから渡す。
- **依頼は一度に一機能**。スキーマ変更→マイグレーション→描画→検証、のリズムで積む。

### Grok担当分の実装方針（決定済み）
全体チャット/同盟チャットは**リアルタイムAPIを使わない**。Grokには
「性格別のセリフプール」と「イベント分岐テンプレ」を大量に書いてもらい、
ゲーム側は既存の `aiUsers` の性格・行動と紐付けてルールベースで再生する。
これでオフライン完結の制約を守れる。同盟内事件（例: 不満を持ったAIへ
なだめる/役職を与える/放置/追放）は既存の `ROLE_DEFS`・勧誘の枠組みに載せられる。

---

## 7. アート / IP方針

- **キャラクターは全て独自デザインに統一**する。これが最も安全で、本作の個性にもなる。
- 史実・古典神話の**人物や事実そのもの**（織田信長、曹操、ゼウス等の存在・来歴）は使用可。
- **特定の現代作品が創作したデザイン・設定・固有演出の流用は不可**
  （例:「あるゲーム版ゼウスの衣装や構図をそのまま使う」）。
- 世界観の基本設定: 時空崩壊により各時代・神話世界の英雄が一つの大陸へ召喚された。
  勢力体系案は 人類 / 神話 / 伝説 / 幻想。実装時は `CARD_POOL` に `faction` を1つ足せば対応可。

### 画像アセット運用
- 建物は `BUILDING_DEFS[].imageKey` に紐づく。本城`bld_castle` / 農場`bld_farm` /
  製鉄所`bld_ironmine` / 兵舎`bld_barracks` / 伐採所`bld_sawmill` / 採石場`bld_quarry` /
  研究所`bld_lab` / 倉庫`bld_warehouse`。
- **画像未登録でも絵文字プレースホルダーで完全動作する**（いつ差し替えても壊れない）。
- 素材仕様: **正方形キャンバス・被写体中央・背景透過**（`object-fit:cover` で切れないため）。
  4棟で光源方向と視点角度を揃えること。
- 取り込みはガチャタブ最下部の「🖼️ 画像取り込み（開発用）」で imageKey 指定。
- **注意: 画像もセーブもオリジン単位**。`file://` と GitHub Pages では別扱いになり、
  それぞれで取り込みが必要。回避するなら未実装#11のbase64埋め込みを実施する。

---

## 8. 公開（GitHub Pages）方針

- **個人プランでは「限定公開」は実質できない。** GitHub Pagesはリポジトリの公開範囲と
  サイトの公開範囲が別設定で、プライベートリポジトリからでも**公開サイトになる**。
  閲覧者を制限するアクセス制御は Enterprise Cloud 向け機能。
- 本作は個人情報も認証もない単一HTMLゲームなので実害はほぼない。
  「未完成を見られたくない」対策としては以下で十分:
  1. リポジトリ名を推測されにくいものにする（`hakoniwa-slg` より `hk-proto-7f2a` 等）
  2. `<head>` に `<meta name="robots" content="noindex,nofollow">`
  3. `robots.txt` を設置
- Pagesに置くと `file://` とはオリジンが別になるため、**セーブは共有されない**（周知事項）。

---

## 9. 既知の設計保留

決着していない判断。触るときは意図を確認すること。

- **帰還行軍の省略** — 出兵は到着時に即解決・即帰還扱い。往復にするかは未決
- **敗北時のペナルティなし** — 失敗は食料コストのみ。部隊損耗の概念は未導入
- **AIのオフライン進行** — `nextActionAt` はオフライン中1回分しか発火しない。
  放置でAIに埋め尽くされるのを防ぐ意図的な簡略化だが要検討
- **役職の重複可** — 同役職を複数人に付与できるがバフは重複しない（`Math.max`）
- **勝利後の継続プレイ** — 聖地1つでバナーのみ。「4聖地全制覇」等のエンドコンテンツ未設計
- **空地タイルのダイヤ寄与** — `level×0.25` で微量加算。0にするか未決
- **タイル資源分布が完全ランダム** — 偏りで序盤に特定資源が枯れる可能性。クラスタ生成にするか未決
- **建物の footprint** — 現状すべて1セル扱い。本城を2×2にする案は未着手

---

## 10. 次の一手（候補）

Sora の判断待ち。有力なのは以下。

- **A. 同期＋確定作業** — §0の同期を済ませ、画像4枚をbase64埋め込み（#11）してV1を確定する。
  全端末・全オリジンで初回から絵が出る状態になる。
- **B. アートの統一** — 残り4棟のLv1画像をChatGPTへ発注（#9）。絵と絵文字の混在を解消。
- **C. 遊びの手触り** — チュートリアル/クエスト導線（#6）。自分でテストプレイする際の
  目標がはっきりし、開発サイクルが回しやすくなる。
- **D. 拡張の土台** — 戦闘の深掘り（#1）。同盟の援軍・AIの反撃・カード限界突破が
  すべてこの上に乗るため、先にやると後が楽。
