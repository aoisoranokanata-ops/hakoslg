# 同盟チャット セリフ執筆ブリーフ（Grok担当）

箱庭クロニクルの同盟チャットは **実行時にLLM/APIを使わない**（オフライン完結の制約）。
代わりに Grok が**セリフのプールとイベント別テンプレを大量に書き**、ゲーム側（`index.html` の
`[S18]` セクション `CHAT_LINES`）がAIの性格・行動に紐付けて**ルールベースで再生**する。

**Grokがやること = `CHAT_LINES` の各プールにセリフを増量・改良すること**。
実装（再生ロジック・UI）はClaudeが担当済み。Grokはコードを書かず、下記フォーマットのセリフを納品する。

## 実装済みの仕組み（前提）

- 発言者は既存の `aiUsers`。性格 `personality` は3種: `expander`(拡張志向) / `balanced`(バランス) / `turtle`(内政・守り)
- 名前例: 北条庵 / 夜叉丸 / 百合園 / 鉄舟斎 / 花菱 / 雷鳥院 / 静waves / 黒曜 / 月白 / 杉浦党
- プレイヤーは「盟主」。同盟名は可変（既定「無名同盟」）
- セリフはランダムに1つ選ばれて再生される。**1プールに数が多いほど繰り返し感が減る**（各15〜30本が目標）

## テンプレ変数（そのまま書ける。実行時に置換される）

| 変数 | 置換内容 |
|---|---|
| `{name}` | 発言者(AI)の名前 |
| `{ally}` | 同盟名 |
| `{other}` | **発言者以外の同盟メンバー名**（他人への言及・掛け合いに使う） |
| `{tile}` | 対象タイル名（`onPlayerCapture` と戦争プール。例:農地/森林/岩山/鉄鉱/聖地/関所/皇都） |
| `{enemy}` | **敵対勢力の名前**（戦争プールのみ。攻めてきた／攻め落とした相手のAI名） |

**`{other}` について**: 同盟に自分以外のAIが居ない場合、そのセリフは**候補から自動的に除外される**
（空欄になって文が壊れることはない）。必ず発言者本人以外が入るので「自分に文句を言う」事故も起きない。
どのプールでも使ってよく、他人を茶化す・心配する・張り合う等の掛け合いに向く。
ただし**`{other}` に依存しすぎると同盟員が少ない序盤にセリフが痩せる**ので、
1プールにつき2〜4割程度に留めてほしい。

## 世界観・トーン

- 和風・戦国ファンタジー。武将らしい古風な口調（「〜にござる」「〜ぞ」「御意」等）。ただし堅すぎず、キャラの個性が出るとよい
- 性格でトーンを変える: expander=好戦的・勢いがある / balanced=思慮深い・穏当 / turtle=慎重・守り重視で頑固
- 長さは1行（スマホ幅で2〜3行に収まる程度、全角40字以内目安）
- 禁止: 現代語すぎる表現、実在の特定作品固有の語、過度に暴力的/不快な表現

## 納品フォーマット（このJS配列をそのまま埋められる形で）

`index.html` の `CHAT_LINES` は下記構造。各配列にセリフ（文字列）を足す。**変数は `{name}` の形で**。

```js
const CHAT_LINES = {
  // 環境チャット（性格別・平常時のつぶやき）
  ambient: {
    expander: [ "…", ... ],   // 拡張志向: 攻めたい・領土を広げたい
    balanced: [ "…", ... ],   // バランス: 内政と軍事の釣り合い
    turtle:   [ "…", ... ],   // 守り: 防備・慎重
  },
  onJoin:          [ "…" ],  // AIが同盟に加入した瞬間、その本人の挨拶
  onPlayerCapture: [ "…" ],  // 盟主がタイルを制圧した時の祝い（{tile}使用可）
  onPlayerCastleUp:[ "…" ],  // 盟主の本城がレベルアップした時
  onRole: {                  // 役職を任命された本人の返答（役職別）
    "将軍": [ "…" ], "都督": [ "…" ], "斥候": [ "…" ], "軍師": [ "…" ], "一般": [ "…" ],
  },
  onHolyWin:       [ "…" ],  // 聖地制圧＝勝利条件達成の歓喜
  onPlayerSend:    [ "…" ],  // 盟主がチャットで発言した時の相槌（短め）
};
```

**現状（2026-07-25 更新）: Grok納品の第1弾を反映済み。計185本。**

| プール | 本数 |
|---|---|
| ambient.expander / balanced / turtle | 22 / 21 / 21 |
| onJoin | 18 |
| onPlayerCapture | 17 |
| onPlayerCastleUp | 14 |
| onRole（将軍/都督/斥候/軍師/一般 各8） | 40 |
| onHolyWin | 14 |
| onPlayerSend | 18 |

さらに増量する場合も同じフォーマットで納品すればそのまま差し替えられる。

## 追加プールの提案（新イベントを足したい場合）

新しい発話タイミングが欲しければ、Grokは案を出す→Claudeが `chatEvent("<新kind>", ...)` の
発火点を実装する。候補: 出兵失敗の慰め / 資源満タンの通知 / ガチャSSR排出の祝い /
夜間・時間帯の挨拶 / AI同士の掛け合い（2連投）など。

## 【第2弾・納品済み】同盟内事件システムのセリフ（2026-07-25 反映 / 計153本）

| プール | 本数 |
|---|---|
| onIncident.noRole / noReward / idle | 15 / 14 / 14 |
| onSoothe / onSootheFail / onPromoteFix | 14 / 12 / 12 |
| onIgnore（expander/balanced/turtle 各8） | 24 |
| onExile（同上） | 24 |
| onLeave（同上） | 24 |

**チャット全体の合計は 338本**（第1弾185＋第2弾153）。以下は依頼時の仕様（増量時も同じ形式で納品可）。

### 依頼仕様（参考・第2弾）

同盟内事件（[S19]）を実装済み。**忠誠度(loyalty)が下がったAIが不満を訴え、盟主が4択で対処する**。
現在は各プール暫定3本しか無いので、**Grokに増量を依頼したい（各12〜20本目標）**。

### 事件の流れ
1. 一定間隔で同盟AIの忠誠が低下（役職なしは早く下がる）
2. 忠誠45未満で**不満を訴える**＝事件発生（`onIncident`、同時1件）
3. 盟主が選ぶ: **なだめる**（食料800・低忠誠ほど失敗）／**役職を与える**（役職なしの相手のみ・回復最大）／**放置**／**追放**
4. 未対応のまま4分経過すると自動的に「放置」扱い
5. 放置で忠誠12未満になると**自主脱退**（再勧誘は可能）。**追放**は二度と勧誘不可

### 増量してほしいプール

```js
  onIncident: {
    noRole:   [ "…" ],  // 役職を与えられていない不満（一般のまま放置された）
    noReward: [ "…" ],  // 待遇・恩賞への不満
    idle:     [ "…" ],  // 同盟が動かない/戦果が無いことへの苛立ち
  },
  onSoothe:     [ "…" ],  // なだめ成功。矛を収める（渋々〜納得まで幅があると良い）
  onSootheFail: [ "…" ],  // なだめ失敗。言葉だけでは不十分と突っぱねる
  onPromoteFix: [ "…" ],  // 役職を与えられて解決。感激・張り切る
  // ▼ 結末3種は【性格別】に分ける（expander/balanced/turtle の3キー必須）
  onIgnore: { expander:[ "…" ], balanced:[ "…" ], turtle:[ "…" ] },  // 放置された。失望・恨み言（まだ同盟に残る）
  onExile:  { expander:[ "…" ], balanced:[ "…" ], turtle:[ "…" ] },  // 追放された。決別の言葉
  onLeave:  { expander:[ "…" ], balanced:[ "…" ], turtle:[ "…" ] },  // 忠誠が尽きて自ら去る。別れの言葉
```

**トーン指針**: `onIncident` は不満だが同盟員としての礼は保つ（反逆ではなく訴え）。
結末3種は温度差をつける — **放置=失望／追放=決別／脱退=諦め**。

**結末3種の性格別トーン**（`personality` は既存のAI性格3種）:
| 性格 | 気質 | 結末での出方 |
|---|---|---|
| `expander` | 好戦的・勢い重視 | 激しく怒る。恨み・報復をちらつかせる |
| `balanced` | 思慮深い・穏当 | 冷静に筋を通す。皮肉や惜別が混じる |
| `turtle` | 慎重・守り重視 | 淡々と諦める。静かに引き下がる |

コードは**フラット配列でも性格別オブジェクトでも受け付ける**（`resolveChatPool`）。
性格キーが欠けていても全プールを平坦化して再生するため発話は消えないが、
**3キーとも埋めてもらうのが望ましい**。

変数は同じく `{name}`（本人）/`{ally}`（同盟名）が使える。`{tile}` は使えない。

---

## 【第3弾・納品済み】戦争のセリフ（2026-07-27 反映 / 計153本）

納品内訳: onEnemyRaid 20 ／ onTileLost 49（拡17・均16・守16）／ onDefend 18 ／
onCastleFall 48（各16）／ onCastleFail 18。**チャット全体で491本**になった。
以下は執筆時のブリーフ（増量時もこの方針で）。

2026-07-27に**AI城への攻撃**と**AIからの反撃**を実装した（[S09]/[S12]）。
これまでAIはプレイヤー領を奪わず、AI城も攻撃できなかったため、同盟員は戦争に
まったく無反応のままになっている。ここに声を入れたい。

### 何が起きるゲームなのか（セリフを書く前提）

- 非同盟のAI勢力が、**プレイヤーの領地のうち守りが最も薄い1枚**を狙って進軍してくる
- 進軍には最低25秒かかり、その間マップに赤い線と警告リングが出る＝**予告がある**
- 着弾時に「敵の攻撃戦力 vs こちらの守り」で判定し、**奪われる**か**撃退する**か決まる
- 守りは本城と兵舎のレベル（＋研究）で上がる。つまり**内政を怠ると削られる**
- 逆にプレイヤーからAIの城を攻め落とすこともできる。落とすと**その勢力は滅亡**し、
  そいつが持っていた領地はすべて無主に戻る
- **同盟員の城は攻撃できない**（攻めるには先に追放が必要）＝勧誘か征服かの選択がある

### 依頼する5プール（各15〜25本）

| キー | 場面 | 誰が喋るか | 使える変数 |
|---|---|---|---|
| `onEnemyRaid` | 敵が侵攻を始めた（**着弾前**の警告の瞬間） | 同盟員の誰か | `{enemy}` `{tile}` |
| `onTileLost` | 領地を奪われた（**性格別**） | 同盟員の誰か | `{enemy}` `{tile}` |
| `onDefend` | 侵攻を撃退して領地を守り抜いた | 同盟員の誰か | `{enemy}` `{tile}` |
| `onCastleFall` | 盟主が敵城を落とし一勢力を滅ぼした（**性格別**） | 同盟員の誰か | `{enemy}` |
| `onCastleFail` | 敵城攻めに失敗した | 同盟員の誰か | `{enemy}` |

### 各プールの温度感

- **onEnemyRaid** — 緊張。「まだ間に合う」空気。焦り・警告・献策。
  まだ**負けてはいない**ので悲観しすぎない。「守りを固めろ」「兵を戻せ」など具体的な献策が入るとよい
- **onTileLost** — 悔しさ。ここだけ性格でくっきり分ける:
  - `expander` = 報復に燃える。「取り返しましょう」「このままでは済まさぬ」
  - `balanced` = 冷静に立て直す。「損害を数えましょう」「痛いが致命傷ではない」
  - `turtle` = 守りの不足を突く。「だから備えをと申した」。やや小言・頑固でよい
- **onDefend** — 安堵と称賛。「守り切った」。**内政の成果が効いた**という手応えが出るとよい
  （このゲームでは守りの強さ＝本城と兵舎のレベルなので、そこを褒める台詞が刺さる）
- **onCastleFall** — 一国が滅んだ重み。祝勝一色にしない。性格で分ける:
  - `expander` = 高揚。「次はどこだ」
  - `balanced` = 畏怖と複雑さ。「一つの家が消えた……」勝利を喜びつつ静かな感慨
  - `turtle` = 警戒。「恨みを買った」「他の勢力が警戒するぞ」
- **onCastleFail** — 慰めと再起。損害を認めつつ次を促す。盟主を責めすぎない

### 注意

- `{enemy}` は**敵の名前**。同盟員ではないので、敬称や仲間扱いはしない
- `onEnemyRaid` は**まだ結果が出ていない**場面。「奪われた」と書かないこと
- `onDefend` と `onTileLost` は同じ相手・同じタイルで対になる。**同じ言い回しの使い回しは避ける**
- 性格別プールは**一部だけ埋めても動く**（空の性格は他の性格のセリフで埋め合わされる）。
  分割納品してよいが、`onTileLost` と `onCastleFall` は3性格そろって初めて狙いが出る

### 納品フォーマット

```js
  onEnemyRaid: [
    "…", "…",
  ],
  onTileLost: {
    expander: [ "…" ],
    balanced: [ "…" ],
    turtle:   [ "…" ],
  },
  onDefend: [
    "…",
  ],
  onCastleFall: {
    expander: [ "…" ],
    balanced: [ "…" ],
    turtle:   [ "…" ],
  },
  onCastleFail: [
    "…",
  ],
```

コード側のフックと空プールは配置済み。**この5キーの中身が届き次第、差し替えるだけで動く。**

### 反映時に分かったこと

- **城陥落では `onPlayerCapture` を抑制した**。`resolveMarches` は制圧成功で必ず
  `onPlayerCapture` を呼んでいたため、AI城を落とすと `onCastleFall`（一国の滅亡という重い場面）の
  直後に「良い土地を押さえました。これで内政も楽になります」等の資源タイル向け祝辞が続き、
  緊張が台無しになっていた。`m.targetAiId` があるときは `onPlayerCapture` を出さない。
- 納品は変数の綴り・重複・全角40字超・非日本語混入を機械チェックしてから反映している。
