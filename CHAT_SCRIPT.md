# 同盟チャット セリフ執筆ブリーフ（Grok担当）

箱庭クロニクルの同盟チャットは **実行時にLLM/APIを使わない**（オフライン完結の制約）。
代わりに Grok が**セリフのプールとイベント別テンプレを大量に書き**、ゲーム側（`index.html` の
`[S18]` セクション `CHAT_LINES`）がAIの性格・行動に紐付けて**ルールベースで再生**する。

**Grokがやること = `CHAT_LINES` の各プールにセリフを増量・改良すること**。
実装（再生ロジック・UI）はClaudeが担当済み。Grokはコードを書かず、下記フォーマットのセリフを納品する。

## 実装済みの仕組み（前提）

- 発言者は既存の `aiUsers`。性格 `personality` は3種: `expander`(拡張志向) / `balanced`(バランス) / `turtle`(内政・守り)
- 名前例: 北条庵 / 夜叉丸 / 百合園 / 鉄舟斎 / 花菱 / 雷鳥院 / 静waves / 黒曜 / 月白 / 杉浦党
- プレイヤーは「盟主」。同盟名は可変（既定「無名同盟」）
- セリフはランダムに1つ選ばれて再生される。**1プールに数が多いほど繰り返し感が減る**（各15〜30本が目標）

## テンプレ変数（そのまま書ける。実行時に置換される）

| 変数 | 置換内容 |
|---|---|
| `{name}` | 発言者(AI)の名前 |
| `{ally}` | 同盟名 |
| `{other}` | **発言者以外の同盟メンバー名**（他人への言及・掛け合いに使う） |
| `{tile}` | 対象タイル名（`onPlayerCapture` と戦争プール。例:農地/森林/岩山/鉄鉱/聖地/関所/皇都） |
| `{enemy}` | **敵対勢力の名前**（戦争プールのみ。攻めてきた／攻め落とした相手のAI名） |

**`{other}` について**: 同盟に自分以外のAIが居ない場合、そのセリフは**候補から自動的に除外される**
（空欄になって文が壊れることはない）。必ず発言者本人以外が入るので「自分に文句を言う」事故も起きない。
どのプールでも使ってよく、他人を茶化す・心配する・張り合う等の掛け合いに向く。
ただし**`{other}` に依存しすぎると同盟員が少ない序盤にセリフが痩せる**ので、
1プールにつき2〜4割程度に留めてほしい。

## 世界観・トーン

- 和風・戦国ファンタジー。武将らしい古風な口調（「〜にござる」「〜ぞ」「御意」等）。ただし堅すぎず、キャラの個性が出るとよい
- 性格でトーンを変える: expander=好戦的・勢いがある / balanced=思慮深い・穏当 / turtle=慎重・守り重視で頑固
- 長さは1行（スマホ幅で2〜3行に収まる程度、全角40字以内目安）
- 禁止: 現代語すぎる表現、実在の特定作品固有の語、過度に暴力的/不快な表現

## 納品フォーマット（このJS配列をそのまま埋められる形で）

`index.html` の `CHAT_LINES` は下記構造。各配列にセリフ（文字列）を足す。**変数は `{name}` の形で**。

```js
const CHAT_LINES = {
  // 環境チャット（性格別・平常時のつぶやき）
  ambient: {
    expander: [ "…", ... ],   // 拡張志向: 攻めたい・領土を広げたい
    balanced: [ "…", ... ],   // バランス: 内政と軍事の釣り合い
    turtle:   [ "…", ... ],   // 守り: 防備・慎重
  },
  onJoin:          [ "…" ],  // AIが同盟に加入した瞬間、その本人の挨拶
  onPlayerCapture: [ "…" ],  // 盟主がタイルを制圧した時の祝い（{tile}使用可）
  onPlayerCastleUp:[ "…" ],  // 盟主の本城がレベルアップした時
  onRole: {                  // 役職を任命された本人の返答（役職別）
    "将軍": [ "…" ], "都督": [ "…" ], "斥候": [ "…" ], "軍師": [ "…" ], "一般": [ "…" ],
  },
  onHolyWin:       [ "…" ],  // 聖地制圧＝勝利条件達成の歓喜
  onPlayerSend:    [ "…" ],  // 盟主がチャットで発言した時の相槌（短め）
};
```

**現状（2026-07-25 更新）: Grok納品の第1弾を反映済み。計185本。**

| プール | 本数 |
|---|---|
| ambient.expander / balanced / turtle | 22 / 21 / 21 |
| onJoin | 18 |
| onPlayerCapture | 17 |
| onPlayerCastleUp | 14 |
| onRole（将軍/都督/斥候/軍師/一般 各8） | 40 |
| onHolyWin | 14 |
| onPlayerSend | 18 |

さらに増量する場合も同じフォーマットで納品すればそのまま差し替えられる。

## 追加プールの提案（新イベントを足したい場合）

新しい発話タイミングが欲しければ、Grokは案を出す→Claudeが `chatEvent("<新kind>", ...)` の
発火点を実装する。候補: 出兵失敗の慰め / 資源満タンの通知 / ガチャSSR排出の祝い /
夜間・時間帯の挨拶 / AI同士の掛け合い（2連投）など。

## 【第2弾・納品済み】同盟内事件システムのセリフ（2026-07-25 反映 / 計153本）

| プール | 本数 |
|---|---|
| onIncident.noRole / noReward / idle | 15 / 14 / 14 |
| onSoothe / onSootheFail / onPromoteFix | 14 / 12 / 12 |
| onIgnore（expander/balanced/turtle 各8） | 24 |
| onExile（同上） | 24 |
| onLeave（同上） | 24 |

**チャット全体の合計は 338本**（第1弾185＋第2弾153）。以下は依頼時の仕様（増量時も同じ形式で納品可）。

### 依頼仕様（参考・第2弾）

同盟内事件（[S19]）を実装済み。**忠誠度(loyalty)が下がったAIが不満を訴え、盟主が4択で対処する**。
現在は各プール暫定3本しか無いので、**Grokに増量を依頼したい（各12〜20本目標）**。

### 事件の流れ
1. 一定間隔で同盟AIの忠誠が低下（役職なしは早く下がる）
2. 忠誠45未満で**不満を訴える**＝事件発生（`onIncident`、同時1件）
3. 盟主が選ぶ: **なだめる**（食料800・低忠誠ほど失敗）／**役職を与える**（役職なしの相手のみ・回復最大）／**放置**／**追放**
4. 未対応のまま4分経過すると自動的に「放置」扱い
5. 放置で忠誠12未満になると**自主脱退**（再勧誘は可能）。**追放**は二度と勧誘不可

### 増量してほしいプール

```js
  onIncident: {
    noRole:   [ "…" ],  // 役職を与えられていない不満（一般のまま放置された）
    noReward: [ "…" ],  // 待遇・恩賞への不満
    idle:     [ "…" ],  // 同盟が動かない/戦果が無いことへの苛立ち
  },
  onSoothe:     [ "…" ],  // なだめ成功。矛を収める（渋々〜納得まで幅があると良い）
  onSootheFail: [ "…" ],  // なだめ失敗。言葉だけでは不十分と突っぱねる
  onPromoteFix: [ "…" ],  // 役職を与えられて解決。感激・張り切る
  // ▼ 結末3種は【性格別】に分ける（expander/balanced/turtle の3キー必須）
  onIgnore: { expander:[ "…" ], balanced:[ "…" ], turtle:[ "…" ] },  // 放置された。失望・恨み言（まだ同盟に残る）
  onExile:  { expander:[ "…" ], balanced:[ "…" ], turtle:[ "…" ] },  // 追放された。決別の言葉
  onLeave:  { expander:[ "…" ], balanced:[ "…" ], turtle:[ "…" ] },  // 忠誠が尽きて自ら去る。別れの言葉
```

**トーン指針**: `onIncident` は不満だが同盟員としての礼は保つ（反逆ではなく訴え）。
結末3種は温度差をつける — **放置=失望／追放=決別／脱退=諦め**。

**結末3種の性格別トーン**（`personality` は既存のAI性格3種）:
| 性格 | 気質 | 結末での出方 |
|---|---|---|
| `expander` | 好戦的・勢い重視 | 激しく怒る。恨み・報復をちらつかせる |
| `balanced` | 思慮深い・穏当 | 冷静に筋を通す。皮肉や惜別が混じる |
| `turtle` | 慎重・守り重視 | 淡々と諦める。静かに引き下がる |

コードは**フラット配列でも性格別オブジェクトでも受け付ける**（`resolveChatPool`）。
性格キーが欠けていても全プールを平坦化して再生するため発話は消えないが、
**3キーとも埋めてもらうのが望ましい**。

変数は同じく `{name}`（本人）/`{ally}`（同盟名）が使える。`{tile}` は使えない。

---

## 【第3弾・納品済み】戦争のセリフ（2026-07-27 反映 / 計153本）

納品内訳: onEnemyRaid 20 ／ onTileLost 49（拡17・均16・守16）／ onDefend 18 ／
onCastleFall 48（各16）／ onCastleFail 18。**チャット全体で491本**になった。
以下は執筆時のブリーフ（増量時もこの方針で）。

2026-07-27に**AI城への攻撃**と**AIからの反撃**を実装した（[S09]/[S12]）。
これまでAIはプレイヤー領を奪わず、AI城も攻撃できなかったため、同盟員は戦争に
まったく無反応のままになっている。ここに声を入れたい。

### 何が起きるゲームなのか（セリフを書く前提）

- 非同盟のAI勢力が、**プレイヤーの領地のうち守りが最も薄い1枚**を狙って進軍してくる
- 進軍には最低25秒かかり、その間マップに赤い線と警告リングが出る＝**予告がある**
- 着弾時に「敵の攻撃戦力 vs こちらの守り」で判定し、**奪われる**か**撃退する**か決まる
- 守りは本城と兵舎のレベル（＋研究）で上がる。つまり**内政を怠ると削られる**
- 逆にプレイヤーからAIの城を攻め落とすこともできる。落とすと**その勢力は滅亡**し、
  そいつが持っていた領地はすべて無主に戻る
- **同盟員の城は攻撃できない**（攻めるには先に追放が必要）＝勧誘か征服かの選択がある

### 依頼する5プール（各15〜25本）

| キー | 場面 | 誰が喋るか | 使える変数 |
|---|---|---|---|
| `onEnemyRaid` | 敵が侵攻を始めた（**着弾前**の警告の瞬間） | 同盟員の誰か | `{enemy}` `{tile}` |
| `onTileLost` | 領地を奪われた（**性格別**） | 同盟員の誰か | `{enemy}` `{tile}` |
| `onDefend` | 侵攻を撃退して領地を守り抜いた | 同盟員の誰か | `{enemy}` `{tile}` |
| `onCastleFall` | 盟主が敵城を落とし一勢力を滅ぼした（**性格別**） | 同盟員の誰か | `{enemy}` |
| `onCastleFail` | 敵城攻めに失敗した | 同盟員の誰か | `{enemy}` |

### 各プールの温度感

- **onEnemyRaid** — 緊張。「まだ間に合う」空気。焦り・警告・献策。
  まだ**負けてはいない**ので悲観しすぎない。「守りを固めろ」「兵を戻せ」など具体的な献策が入るとよい
- **onTileLost** — 悔しさ。ここだけ性格でくっきり分ける:
  - `expander` = 報復に燃える。「取り返しましょう」「このままでは済まさぬ」
  - `balanced` = 冷静に立て直す。「損害を数えましょう」「痛いが致命傷ではない」
  - `turtle` = 守りの不足を突く。「だから備えをと申した」。やや小言・頑固でよい
- **onDefend** — 安堵と称賛。「守り切った」。**内政の成果が効いた**という手応えが出るとよい
  （このゲームでは守りの強さ＝本城と兵舎のレベルなので、そこを褒める台詞が刺さる）
- **onCastleFall** — 一国が滅んだ重み。祝勝一色にしない。性格で分ける:
  - `expander` = 高揚。「次はどこだ」
  - `balanced` = 畏怖と複雑さ。「一つの家が消えた……」勝利を喜びつつ静かな感慨
  - `turtle` = 警戒。「恨みを買った」「他の勢力が警戒するぞ」
- **onCastleFail** — 慰めと再起。損害を認めつつ次を促す。盟主を責めすぎない

### 注意

- `{enemy}` は**敵の名前**。同盟員ではないので、敬称や仲間扱いはしない
- `onEnemyRaid` は**まだ結果が出ていない**場面。「奪われた」と書かないこと
- `onDefend` と `onTileLost` は同じ相手・同じタイルで対になる。**同じ言い回しの使い回しは避ける**
- 性格別プールは**一部だけ埋めても動く**（空の性格は他の性格のセリフで埋め合わされる）。
  分割納品してよいが、`onTileLost` と `onCastleFall` は3性格そろって初めて狙いが出る

### 納品フォーマット

```js
  onEnemyRaid: [
    "…", "…",
  ],
  onTileLost: {
    expander: [ "…" ],
    balanced: [ "…" ],
    turtle:   [ "…" ],
  },
  onDefend: [
    "…",
  ],
  onCastleFall: {
    expander: [ "…" ],
    balanced: [ "…" ],
    turtle:   [ "…" ],
  },
  onCastleFail: [
    "…",
  ],
```

コード側のフックと空プールは配置済み。**この5キーの中身が届き次第、差し替えるだけで動く。**

### 反映時に分かったこと

- **城陥落では `onPlayerCapture` を抑制した**。`resolveMarches` は制圧成功で必ず
  `onPlayerCapture` を呼んでいたため、AI城を落とすと `onCastleFall`（一国の滅亡という重い場面）の
  直後に「良い土地を押さえました。これで内政も楽になります」等の資源タイル向け祝辞が続き、
  緊張が台無しになっていた。`m.targetAiId` があるときは `onPlayerCapture` を出さない。
- 納品は変数の綴り・重複・全角40字超・非日本語混入を機械チェックしてから反映している。

---

## 【第4弾・実装済み】下知への応答と挨拶（2026-07-27 / 計138本・Claude執筆）

Grokに出す前にClaude側で書いて入れた分。**増量・差し替えはいつでも歓迎**なので、
同じ構造・同じトーンで書き足してもらえれば差し替えるだけで反映される。

### `onOrder`（性格別・各10本）
盟主が攻撃目標を下知した時の応答（[S24]）。`{tile}` に目標の種別名が入る。
- `expander` = 待ってましたと勇む ／ `balanced` = 手筈を整える ／ `turtle` = 受けるが慎重に釘を刺す

### `onPlayerSend`（意図別・各10本＋既存の相槌18本＝計108本）
**構造が変わった**。従来はフラットな相槌配列だったが、盟主の発言内容から意図を判定して
sub を切り替えるオブジェクトになっている（`detectChatIntent`）。

| sub | 拾う語の例 |
|---|---|
| `morning` | おはよ／早う |
| `day` | こんにち／ちは／ごきげんよう |
| `evening` | こんばん／晩は |
| `night` | おやすみ／また明日／落ちます／ログアウト |
| `greet` | よろしく／はじめまして／頼む |
| `thanks` | ありがと／感謝／助かっ |
| `apology` | ごめん／すまん／申し訳 |
| `praise` | すごい／さすが／見事／強い |
| `question` | 文末が `?` または `？` |
| `default` | 上記以外（従来の相槌） |

**注意**: `resolveChatPool` はサブキーが空だと全プールを平坦化して返すため、
**すべての sub を埋めておくこと**。空にすると「おはよう」に「おやすみなさい」が
返るような取り違えが起きる。

**判定順は「より具体的なものを先に」**。`CHAT_INTENT_RULES` は上から順に評価するので、
例えば `greet` の「ございます」を先に置くと「おはようございます」が greet に落ちてしまう。

挨拶などの意図が取れた発言には**必ず応答する**（無視されると不自然なため）。
意図が取れない発言は従来どおり8割の確率で相槌を打つ。

---

## 【第5弾・納品済み】既存プールの増量（2026-07-27 反映 / 納品185本→採用162本）

**チャット全体773本**になった。反映時の内訳:

| プール | 反映後 | 納品 | 採用 | 重複除外 |
|---|---|---|---|---|
| `ambient` | 139（拡47・均46・守46） | 75 | 75 | 0 |
| `onPlayerCapture` | 42 | 25 | 25 | 0 |
| `onEnemyRaid` | 35 | 15 | 15 | 0 |
| `onDefend` | 33 | 15 | 15 | 0 |
| `onPlayerCastleUp` | 29 | 15 | 15 | 0 |
| `onSoothe` | 20 | 10 | 6 | 4 |
| `onPromoteFix` | 18 | 10 | 6 | 4 |
| `onJoin` | 18 | 10 | **0** | **10** |
| `onSootheFail` | 17 | 10 | 5 | 5 |

### ★増量発注で必ずやること: 既存行との重複チェック

第5弾では**185本中23本が既存行と完全に一致**していた。特に `onJoin` は
**10本すべてが既存プールの再送**で、新規はゼロだった。
そのまま追記すると同じ台詞が二重に入り、抽選で偏る（実質的な水増しになる）。

増量納品を反映する時は、**納品内の重複だけでなく既存行との重複も必ず突き合わせる**こと。
手順は `index.html` の `CHAT_LINES` ブロックから `^\s+"(.*)",$` で全行を抜き、
集合で差分を取るだけ。

（発注時に「既存と重複しないもののみ」と明記しておくと歩留まりが上がるはず）

以下は発注時のブリーフ。

現状**20プール611本**。ただし内訳に偏りがあり、**最も頻繁に流れるプールほど薄い**。
増量は「本数が少ない順」ではなく**再生頻度の高い順**に行う。

### 現状の本数（少ない順）

| プール | 本数 | 再生頻度 | 増量優先度 |
|---|---|---|---|
| `ambient` | 64（性格別21前後） | **25〜70秒ごとに常時** | **最優先** |
| `onPlayerCapture` | 17 | **タイル制圧のたび**（コアループ） | **最優先** |
| `onDefend` | 18 | 侵攻を撃退するたび | 高 |
| `onEnemyRaid` | 20 | AIが侵攻を始めるたび | 高 |
| `onPlayerCastleUp` | 14 | 本城を上げるたび | 高 |
| `onSoothe` / `onSootheFail` / `onPromoteFix` | 14 / 12 / 12 | 事件を解決するたび | 中 |
| `onJoin` | 18 | 勧誘成功時 | 中 |
| `onHolyWin` | 14 | 1ゲームに1回 | 低（据え置き可） |
| `onCastleFall` | 48 | 勢力を滅ぼした時（稀） | 低 |

### ★増量と同時に直したいこと: セリフが「昔のゲーム」を語っている

第1弾185本を書いてもらった時点では、以下がまだ**存在しなかった**。
そのため常時流れる `ambient` が、今プレイヤーが遊んでいるゲームに一切触れていない。

- 兵種6種の相性（歩兵→槍兵→騎兵→弓兵→楯兵→歩兵の輪＋兵器）
- 兵力と損耗（連戦すると弱る／徴兵所で補充）
- 地形（越えられない山・大河、関所と埠頭、中央の皇都）
- **AIがこちらの領地を奪いに来る**（守りが薄い所を狙われる）
- 幕舎と駐留（前線基地・そこから出撃・そこで回復）
- 防衛施設（本城Lv10で解禁）
- 研究ツリー（研究所で恒久バフ）／カード合体（ダブりを5枚で強化）
- 同盟倉庫（加入者が資源を納める）／盟主の下知による包囲

増量分はこれらに触れた内容を厚めに。「今この世界で何が起きているか」を喋ってほしい。

### `{other}` をもっと使ってよい

AI同士の掛け合いの土台（`{other}`＝発言者以外の同盟メンバー名）は既にある。
現状はほとんど使われていないので、増量分では**2〜4割程度**で使ってほしい。
同盟員が2人以上いる時だけ再生され、居なければ自動的に候補から外れるので文は壊れない。

---

## 【第6弾・納品済み】第二ステージ「異民族」のセリフ（2026-07-31 反映 / 納品111本→採用110本）

**チャット全体883本**（24プール）になった。反映時の内訳:

| プール | 反映後 | 納品 | 採用 | 重複除外 |
|---|---|---|---|---|
| `onTribeWave` | 36（拡12・均12・守12） | 36 | 36 | 0 |
| `onCapitalLost` | 29（拡10・均10・守9） | 30 | 29 | 1 |
| `onCapitalRetaken` | 15 | 15 | 15 | 0 |
| `onTribeCleared` | 30（拡10・均10・守10） | 30 | 30 | 0 |

第5弾の反省（既存行との重複）を発注文に書いた効果で、**既存と重複したのは1本だけ**
（`onCapitalLost.turtle` の「{enemy}の侵攻を止められなかった。……反省が要る。」）。
納品内の重複・非日本語の混入・40字超えはいずれもゼロ。

### 反映時に足した仕掛け: `chatEvent` の `filter`

`onTribeWave` 36本のうち**11本が「また来たか」「まだ来るのか」「波が続く」など
前の波があった前提**の言い回しだった。第1波は第二ステージが開く一番印象に残る場面なので、
そこで「また来たか」が出ると台無しになる（実測で30回中9回ほど引いていた）。

`chatEvent(kind, { filter })` を足し、`tribeTick` の第1波だけ
`TRIBE_FIRST_WAVE_OK`（反復前提の語を弾く述語）を渡すようにした。
絞った結果が0本になったら元のプールに戻すので、**フィルタで無言になることはない**。
第2波以降は無加工なので、11本はそのまま「まだ来るのか」の重さとして効く。

以下は発注時のブリーフ。

---

## 【第6弾のブリーフ】第二ステージ「異民族」のセリフ

2026-07-27に**第二ステージ**を実装した（[S27]）。皇都を制圧し本城Lv10に達すると、
北の草原から**異民族**が波状に流入してくる。全勢力を討伐し、かつ**皇都を保持したまま**が勝利条件。

同盟員がこの局面に無反応なので、ここに声を入れたい。

### ゲーム側で何が起きるか（セリフの前提）

- 異民族は**勧誘できない侵略者**。通常のAI勢力とは別物として扱う
- **3波**で押し寄せる: 第1波 城Lv12×3 → 全滅 → 第2波 Lv14×3 → 全滅 → 第3波 Lv16×2
- 通常AIは自分の地方から出ないが、**異民族は関所を越えて皇都へ南下してくる**
- 攻撃は通常AIの1.4倍、同時に最大5部隊が侵攻してくる
- **皇都を奪われると勝利条件が崩れる**。全滅させても皇都が敵の手にある限り討伐は成立しない
- 皇都を取り返すと再び道が開ける

### 依頼する4プール

| キー | 場面 | 変数 | 本数 |
|---|---|---|---|
| `onTribeWave` | **波が来た瞬間**（第1〜3波） | `{wave}`=波の番号 | 性格別に各12本 |
| `onCapitalLost` | **皇都を奪われた** | `{enemy}`=奪った異民族名 | 性格別に各10本 |
| `onCapitalRetaken` | **皇都を取り返した** | — | 15本（性格別でなくてよい） |
| `onTribeCleared` | **全異民族を討伐した**（ゲーム最大の達成） | — | 性格別に各10本 |

### 温度感

- **onTribeWave** — 第1波は「未知の脅威が来た」驚き、第2・3波は「まだ来るのか」という消耗と覚悟。
  `{wave}` で番号が入るので、波を重ねるほど重くなる書き方ができる。
  ・`expander` = 望むところだと燃える ／ `balanced` = 冷静に迎撃態勢 ／ `turtle` = 皇都を固めよと訴える
- **onCapitalLost** — **このゲームで最も重い瞬間**。積み上げたものが崩れる。
  ・`expander` = 即座に奪い返すと激高 ／ `balanced` = 立て直しの算段 ／ `turtle` = 守りを説いた者の悔恨
- **onCapitalRetaken** — 安堵と再起。ここから討伐に戻れる。
- **onTribeCleared** — **ゲーム全体の終着点**。派手に祝ってよい。長い戦いが終わった感慨、
  盟主への敬意、これまでの犠牲への言及があると締まる。

### 注意

- `{enemy}` は異民族の名前（狼牙部・赤角衆・凍原の民・砂嵐衆・黒鷲部・鉄蹄衆・白狼部・雷斧衆）。
  **敬称や仲間扱いはしない**。既存の敵AIとも違う「外から来た侵略者」として扱ってほしい
- `onCapitalLost` は**まだ負けていない**（取り返せる）。絶望で終わらせず、次に繋がる言葉を残す
- 既存の `onEnemyRaid`（侵攻の警告）とは別物。あちらは日常的な小競り合い、
  こちらは**ステージ全体を賭けた大戦**として書き分けてほしい

### 納品フォーマット

```js
  onTribeWave: {
    expander: [ "…" ], balanced: [ "…" ], turtle: [ "…" ],
  },
  onCapitalLost: {
    expander: [ "…" ], balanced: [ "…" ], turtle: [ "…" ],
  },
  onCapitalRetaken: [ "…" ],
  onTribeCleared: {
    expander: [ "…" ], balanced: [ "…" ], turtle: [ "…" ],
  },
```

**★既存のセリフと重複しないものだけ**をお願いします（第5弾で185本中23本が既存と同一で、
うち `onJoin` は10本すべてが再送でした）。

コード側のフックと空プールは配置済み。**この4キーの中身が届き次第、差し替えるだけで動く。**
