# 設計発注ブリーフ集

ChatGPT に**確定仕様**を書いてもらうための資料。CLAUDE.md §6 の鉄則により、
Claude は「①触るセクション ②CONFIGに足す定数と意味 ③Sスキーマ変更とマイグレーション方針
④受け入れ条件」が揃った仕様を受け取ってから実装する。
末尾に **Claude 自身が起草した設計案**（総監督の求めに応じて出すもの）も置く。

---

# 【Claude設計案・✅実装済み】負傷兵システム（兵力）

> **2026-07-26 実装完了**（SAVE_VERSION 9）。総監督の裁定どおり実装し、受け入れ条件28項目を検証。
> 実装中の補正: 編成強化や兵舎レベルアップで最大兵力が増えると充足率が下がり
> 「強い武将を入れたのに弱くなる」挙動になったため、`withTroopRatioPreserved()` で
> **充足率を保ったまま**最大兵力を変える形に修正した。


> 総監督指示: 「戦闘における時間による負傷兵の回復速度は遅めで、早く戦線に復帰したいときは
> 負傷兵の補充で徴兵所で予備兵数に応じて食料を消費」／「負傷兵は兵数が減るということ」

## 0. 設計の骨子

**部隊に「兵力（兵数）」を持たせ、戦闘で減り、時間で遅く回復し、徴兵所で食料を払って即補充できるようにする。**
戦力は `カード戦力 × 兵力充足率` になるため、**連戦すると弱くなり、休ませるか補充するかの判断が生まれる**。

### なぜ「部隊ごと」に持たせるか（武将ごとではなく）
- 武将ごとに兵力を持たせると、**負傷した武将を外して別の武将を入れる**ことで消耗を回避できてしまう
- 部隊（＝軍団）に属させれば、カードを入れ替えても兵力は引き継がれ、抜け穴が無い
- UIも「部隊タブに兵力バー1本」で済み、26枚のカードすべてを管理する煩雑さを避けられる

### なぜ「統率」を最大兵力の基準にするか
`lead`（統率）は現在**戦力計算にしか使われていない**。「統率＝率いられる兵の数」という
本来の意味を与えることで、ステータスに役割が増え、編成の判断軸も増える。

## 1. 触るセクション

| セクション | 変更内容 |
|---|---|
| [S01] CONFIG | 兵力・損耗・回復・徴兵所の定数を追加 |
| [S02] STATIC DEFS | `BUILDING_DEFS` に **徴兵所 `camp`** を追加（8棟→9棟）。`DEFAULT_BUILDING_LAYOUT` にも配置 |
| [S04] PERSISTENCE | `SAVE_VERSION` を 8→9。squads へ `troops`、player へ `reserves` を補完 |
| [S06] STATE INIT | 新規時は満タンの兵力で開始 |
| [S07] ECONOMY | 徴兵所による**予備兵の自然増加**を tick に追加（上限あり） |
| [S09] MILITARY | `getSquadMaxTroops()` / `getSquadTroopRatio()` を追加。`getSquadPower()` に充足率を掛ける。<br>`startMarch()` に最低兵力チェック。`resolveMarches()` で**損耗**を適用。tickで**自然回復** |
| [S13] UI | 部隊編成タブに兵力バーと補充ボタン。出撃画面に兵力と損耗見込みを表示。城タブに徴兵所 |
| [S15] UI: ACTIONS | `UIA.reinforce(squadId)`（予備兵から補充・食料消費） |

## 2. CONFIG に足す定数

```js
// --- 兵力（負傷兵）---
TROOPS_PER_LEAD: 20,             // 最大兵力 = 編成カードの統率合計 × この値 + 兵舎Lv × 下記
TROOPS_PER_BARRACKS_LV: 200,     // 兵舎レベルごとの最大兵力ボーナス
TROOPS_MIN_RATIO_TO_MARCH: 0.2,  // 充足率がこれ未満の部隊は出撃できない（休ませる判断を促す）
TROOPS_HEAL_RATIO_PER_MIN: 0.008,// 自然回復。最大兵力×0.8%/分（満タンまで約2時間＝「遅め」）
// 損耗。敵防御力÷自軍戦力(相性込み)の比が大きいほど激しく減る
CASUALTY_WIN_FACTOR: 0.25,       // 勝利時: 比×この値（下限3%・上限35%）
CASUALTY_WIN_MIN: 0.03, CASUALTY_WIN_MAX: 0.35,
CASUALTY_LOSE_FACTOR: 0.35,      // 敗北時: 比×この値（下限20%・上限60%）
CASUALTY_LOSE_MIN: 0.20, CASUALTY_LOSE_MAX: 0.60,
// --- 徴兵所 ---
RESERVE_PER_CAMP_LV: 500,        // 予備兵の上限 = 徴兵所Lv × この値
RESERVE_GAIN_PER_CAMP_LV_MIN: 20,// 予備兵の自然増加 = 徴兵所Lv × この値／分
FOOD_PER_SOLDIER: 2,             // 補充1兵あたりの食料
```

### バランス試算（実データで確認済み）

**最大兵力** = 統率合計×20 + 兵舎Lv×200

| 局面 | 編成 | 統率合計 | 最大兵力 |
|---|---|---|---|
| 初期 | N2枚・兵舎Lv1 | 49 | **1,180** |
| 中盤 | SR5枚・兵舎Lv5 | 300 | **7,000** |
| 終盤 | UR/SSR5枚・兵舎Lv10 | 461 | **11,220** |

**損耗の目安**（敵防御÷自軍戦力の比で決まる）

| 状況 | 比 | 勝利時の損耗 | 敗北時の損耗 |
|---|---|---|---|
| 楽勝（資源Lv1に初期部隊） | 0.35 | 8.8% | — |
| 互角（関所1500に戦力1700） | 0.88 | 22% | — |
| 格上に挑む（皇都4000に戦力3000） | 1.33 | — | 47% |

→ **楽な相手なら連戦できるが、関所や皇都に挑むと数回で兵が尽きる**。
補充のために食料を貯める必要が生まれ、食料の使い道（現在は出兵100・勧誘500のみ）が増える。

**補充コスト**: 初期部隊(1,180)を全快させると食料2,360。中盤(7,000)なら14,000。
倉庫容量（Lv1で6,000／Lv2で19,494）と噛み合う規模。

## 3. Sスキーマの変更とマイグレーション

```js
player: {
  ...
  reserves: 0,                                  // 徴兵所にプールされた予備兵
  squads: [{ id, name, cardUids, troops }],     // troops = 現在兵力（新規）
}
```
- **現在ステップ等と同様、最大兵力は保存しない**（編成と兵舎Lvから毎回算出する）
- 自然回復は tick の `dtSec` で加算するため `lastHealAt` は不要（オフライン停止と自然に整合）

### マイグレーション（v8 → v9）
```js
// 既存プレイヤーは「満タンの兵力」で始める（いきなり弱くならないように）
for (const sq of data.player.squads) {
  if (sq.troops == null) sq.troops = -1;   // -1 = 未初期化。boot後に最大値で埋める
}
if (data.player.reserves == null) data.player.reserves = 0;
// 徴兵所が無い旧セーブに Lv1 で追加（他建物と同じ形）
if (!data.player.buildings.some(b => b.type === "camp")) {
  const p = DEFAULT_BUILDING_LAYOUT.camp;
  data.player.buildings.push({ id: "b_camp", type: "camp", level: 1, upgradeEndsAt: null, x: p.x, y: p.y });
}
```
`troops === -1` の部隊は初回参照時に `getSquadMaxTroops()` で満タン初期化する
（マイグレーション時点では建物や編成が揃っていない可能性があるため、遅延初期化にする）。

## 4. 受け入れ条件

### 兵力の基本
1. 新規ゲームで第一隊の兵力が最大値（初期編成なら1,180前後）になっている
2. 部隊編成タブに兵力バーと「現在/最大」が表示される
3. 編成カードを増減させると最大兵力が変わる（統率合計に連動）
4. 兵舎をレベルアップすると最大兵力が増える

### 戦力への反映
5. 充足率100%のとき、戦力は今まで（v8）と同じ値になる
6. 充足率50%のとき、戦力が半分になる
7. 出撃画面に「兵力 X/Y（Z%）」が表示され、戦力が充足率を反映している

### 損耗
8. 勝利しても兵力が減る（弱い相手なら数%、互角なら20%前後）
9. 敗北すると大きく減る（20〜60%）
10. 損耗量が制圧/敗北トーストと出来事ログに残る
11. 兵力は0未満にならない

### 出撃制限
12. 充足率が20%未満の部隊は出撃できず、理由が表示される
13. 全部隊が兵力不足のとき、その旨が分かる

### 回復
14. 時間経過で兵力が回復する（最大兵力の0.8%/分・満タンで停止）
15. 離席中は回復しない（オフライン進行停止と整合）

### 徴兵所
16. 城タブに徴兵所が追加され、レベルアップできる
17. 予備兵が時間で増え、徴兵所レベル×500 で上限に達する
18. 「補充」で予備兵を部隊へ移せ、1兵あたり食料2を消費する
19. 予備兵・食料のどちらかが足りなければ、その分だけ補充される（または理由を表示）
20. 補充後、戦力が即座に回復している

### 既存への影響
21. v8セーブから移行しても**兵力は満タン**で、いきなり弱くならない
22. 旧セーブに徴兵所がLv1で追加され、他の建物の配置が崩れない
23. 初陣指南の5段階が従来どおり達成できる（初期戦力で資源地Lv1に勝てる）

## 5. 総監督の裁定（2026-07-26・確定）

| 論点 | 裁定 |
|---|---|
| 損耗の重さ | **上表のまま**（関所に2〜3回挑むと補充が必要） |
| 自然回復の速さ | **満タンまで約2時間のまま** |
| 徴兵所の予備兵 | **時間で無償に増える**方式で確定 |
| 兵器の扱い | **城への攻撃力は高いが消耗しやすい。後方攻撃でカタパルトのように攻撃範囲は広いが防御力は低い** |

### 兵器（siege）の特性 — メカニクスへの落とし込み

「後方攻撃・範囲が広い・防御が低い」を、現行システム（射程や範囲の概念が無い）に無理なく載せる。

| 総監督の指示 | 実装 |
|---|---|
| 城への攻撃力が高い | **実装済み**: 要塞（関所/皇都/城/聖地）に ×1.6、野戦では ×0.75 |
| 後方攻撃／攻撃範囲が広い | **射程2**。兵器を含む部隊は、自領から**2歩先**のタイルにも出撃できる（他兵種は1歩=隣接のみ）。<br>ただし**未所有の要衝（関所・埠頭・皇都）は飛び越えられない** — ここを通り越せると「関を破らねば先へ進めない」という地形の要点が崩れるため |
| 消耗しやすい／防御力が低い | **損耗倍率 ×1.6**。部隊の損耗倍率は編成カードの平均を取るので、兵器を1枚混ぜる程度なら影響は小さく、兵器で固めると激しく消耗する |

- 部隊の**射程**は編成内の最大値（カタパルトを連れて行けば遠くを叩ける）
- 部隊の**損耗倍率**は編成内の平均（兵器の比率に比例して脆くなる）
- 結果、兵器隊は「**関所を飛び越えて奥を叩けるが、兵の消耗が激しく補充コストが重い**」という
  ハイリスク・ハイリターンの位置づけになる

### 追加するCONFIG（兵器特性ぶん）
```js
TROOP_RANGE_DEFAULT: 1,          // 通常兵種の射程（＝隣接のみ）
// TROOP_DEFS 側に持たせる: siege = { range: 2, casualtyMult: 1.6 }
```

### 追加する受け入れ条件
24. 兵器を含む部隊は、自領から2歩先のタイルにも出撃できる
25. 兵器を含まない部隊は従来どおり隣接タイルのみ
26. 未所有の関所・埠頭・皇都を飛び越えた先へは、兵器隊でも出撃できない
27. マップのハイライトが「いずれかの待機部隊が攻撃できる範囲」を示す
28. 兵器で固めた部隊は損耗が約1.6倍になり、混成なら比率に応じて緩和される

---

# 【依頼1・✅納品済み／実装済み】初回導線（チュートリアル／クエスト）の設計

> **2026-07-26 完了**。ChatGPTから確定仕様「初陣指南」を受領し [S15A] として実装。
> 5段階（領地→本城Lv2→兵舎Lv2→10連→もう1枚領地）でコアループを1周させる方式。
> 受け入れ条件27項目を検証済み。以下は依頼時の資料（再依頼や仕様追補の際の参考）。
>
> 実装時の補正: 仕様書の `SAVE_VERSION: 2` は当時の情報に基づく記載だったため、
> 現行に合わせて **6** で実装（`SAVE_KEY` 据え置きの指示は遵守）。
> また初制圧の通知が3件重なったため、`capture1.done` を空にして
> 「🏳️ 初領地を得た！ 💎+6/h ／ 🌾+200/h が毎時入り続けます」の1本に集約した。


## 背景

Gemini のUX検品で**影響度[高]**の指摘が1件残っている。

> 本作のコアサイクルである「領土を広げる → ダイヤが増える → ガチャを引く → 戦力が上がる →
> さらに領土を広げる」というループの起点がUIから読み取れない。特に「兵舎」を上げることの
> 重要性（戦力アップ）と「ダイヤの入手経路（領地からの毎時生産）」が初見では繋がらず、
> ただ農場などをポチポチとレベルアップさせて終わってしまう危険性がある。

同時に、**チュートリアル／目標提示が一切ない**（未実装#6）。この2つは同じ問題なので統合して設計したい。

## 現状：起動直後のプレイヤーが持っているもの

| 項目 | 初期値 |
|---|---|
| 資源 | 🌾食料1000 / 🪵木材1000 / 🪨石材600 / ⚙️鉄300 |
| 💎ダイヤ | 1000（＝10連ガチャ1回分。単発は100） |
| 本城 | Lv1（毎時 食料612/木材612/石材408/鉄306を生産） |
| 建物 | 8棟すべてLv1（農場・伐採所・採石場・製鉄所・兵舎・研究所・倉庫・本城） |
| カード | N2枚（部隊戦力228） |
| 領地 | 0枚（＝ダイヤ収入0/h） |
| 同盟 | 自分のみ |

**最初の一手にかかるコスト・時間の実測値:**
- 出兵1回 = 🌾100、行軍3秒〜（隣接タイルなら最短）
- 隣接タイルの防御力 = 空地24 / 資源地Lv1で80（初期戦力228なら**確実に勝てる**）
- 領地1枚（Lv1資源地）を取ると 💎+20（一時金）＋ 💎6/h ＋ 資源200/h
- 農場Lv1→2 = 🪵100+🪨50、10秒
- 本城Lv1→2 = 🌾200+🪵200+🪨150+⚙️80、20秒（他建物は本城Lv以下までしか上げられない）
- 勧誘1回 = 🌾500（成功率は自城とAI城のLv差で変動。初期は約44〜50%）
- 10連ガチャ = 💎900（SR以上1枚保証／天井50連でSSR確定）

**つまり初期資源だけで「出兵→制圧→建物強化→10連ガチャ」まで全部できる**が、
その順番も意味も提示されていないのが現状。

## すでに実装済み（＝仕様で前提にしてよいもの）

- **マップの視覚的誘導**: 出兵可能タイルが光る／自城に「自城」ラベル＋金リング／
  画面外の聖地を指すコンパス（タップでジャンプ）
- **出来事ログ [S20]**: `toast(msg, kind)` で記録され📜ボタンから読み返せる。未読バッジ付き
- **勧誘導線**: マップのAI城に「盟」「勧」バッジ／同盟タブに勧誘候補一覧（距離・成功率・その場で勧誘）
- **同盟チャット [S18] / 同盟内事件 [S19]**: AI同盟員が状況に応じて発言する（セリフは338本）
- UI部品: トースト／ボトムシート(`#sheet`)／パネル／タブバーのバッジ／ボトムシート内のボタン群

## 設計してほしいもの

**「初回起動から最初の10分」でプレイヤーがコアループを一周する導線。**
形式は問わない（クエスト方式／段階的ハイライト／目標カード等）。以下を判断してほしい:

1. **提示の方式** — 常駐の「目標」パネルか、達成時だけ出るトーストか、専用タブか。
   ※タブは現在5つ（マップ/城/カード/ガチャ/同盟）で、モバイル幅的にこれ以上増やしたくない
2. **ステップの内容と順序** — 何を、どの順で、どう伝えるか（コアループの因果が伝わることが最重要）
3. **報酬の有無** — ステップ達成に報酬を出すか（出すなら本作の「無料でも潤沢」方針と矛盾しない範囲で）
4. **完了後の扱い** — チュートリアル後も継続する「日々の目標」に発展させるか、消えるか
5. **Geminiの個別提案の採否** — ①ヘッダーのダイヤに `+12/h ← 領地` と出どころ併記
   ②城タブの兵舎に「ガチャへ」ショートカット。採用/不採用と理由

## 制約（これに反する案は実装できない）

- 単一HTMLファイル・外部ライブラリ不可・実行時にLLM/外部API不可（完全オフライン）
- サーバー無し／マルチプレイ無し／課金要素ゼロ
- モバイル縦持ち優先。画面が狭いので**常駐UIを増やすなら1要素まで**
- 既存プレイヤーのセーブを壊さない（スキーマ変更時は非破壊マイグレーション必須）
- 進行状況を持つなら `S` に追加 → `CONFIG.SAVE_VERSION` を上げて `migrateSave()` に移行処理

## 納品してほしい形式（確定仕様）

1. **触るセクション**（例: [S13] UI: PANELS に◯◯を追加、[S16] tick に判定を追加）
2. **CONFIGに足す定数**と意味・初期値
3. **Sスキーマの変更**（追加するフィールドと型）と**マイグレーション方針**
4. **受け入れ条件**（何ができたら完成か。テストできる粒度で）
5. ステップの文言案（ゲーム内に出す日本語テキスト。和風トーンで）

---

# 【依頼2・✅納品済み／実装済み】地形システムの設計

> **2026-07-26 完了**。ChatGPTから確定仕様を受領し実装。40×40維持・固定骨格＋ランダム資源・
> 三地方＋皇都州・BFSは起動時1回のみ・旧セーブは旧マップのまま、という判断がいずれも
> 現行コードと噛み合っており、受け入れ条件40項目を検証済み。以下は依頼時の資料。
>
> 実装時の補正: 地形が縮小表示で判別しにくかったため、`TILE_DEFS` の色を
> 資源地とはっきり差がつく値に調整（山 `#736758` / 川 `#1b4b7d` など）。


## やりたいこと（総監督の構想）

現在のマップは「中心から離れるほど高レベルな資源タイルがランダムに散らばる」だけ。
ここに**地形**を導入して戦略性を作りたい。

- **通行できない山** — 進軍を遮る壁
- **川** — 通れないが、**埠頭（渡河点）でのみ渡れる**
- **三国鼎立** — 勢力が最終的に3つの領域に分かれて対峙するような地勢
- **関所** — 領域の入口・出口にある大きな防衛施設（高防御。ここを抜かないと先へ進めない）
- **首都の資源州** — 「洛陽」のような、**Lv10級の資源が密集する中央の一等地**

## 現在の実装（設計はこれを踏まえること）

**タイル**: `{ x, y, type, level, ownerId }` の40×40グリッド（`S.world.tiles` はフラット配列、index = y*40+x）
- `type`: `empty` / `food` / `wood` / `stone` / `iron` / `holy`(聖地) / `castle`(城)
- `level`: 1〜5（中心からの距離帯で決定）、聖地は6
- 生成は `newGameState()` 内で1回だけ（[S05]）

**進軍のルール**（ここが地形と最も強く干渉する）:
- 自領（自城含む）に**8方向で隣接**するタイルにのみ出兵できる（`REQUIRE_ADJACENCY`）
- 行軍時間は自城からの**チェビシェフ距離÷速度**。**経路探索は無い**（直線扱い）
- 出兵可能タイルは `getMarchableTiles()` が自領の8近傍から算出し、マップ上で光らせている
- AIの拡張 `aiExpand()` も同じく「自領の8近傍の未所有タイル」を取る

**つまり地形を入れると、以下が連鎖的に変わる**（仕様で方針を示してほしい）:
- 山/川は「隣接していても出兵できない」＝通行判定を `getMarchableTiles` / `isAdjacentToOwned` /
  `aiExpand` に追加する必要がある
- 埠頭・関所は「そこを通らないと向こう側へ行けない」＝**地続き拡張のルールが実質的な経路制約**になる
  （厳密な経路探索を導入するか、隣接ルールのままで地形配置によって自然に制約するかは設計判断）
- 行軍時間を距離ベースのままにするか、迂回を考慮するか

## 判断してほしい点

1. **マップサイズ** — 三国＋関所＋首都州を配置するのに40×40で足りるか（拡大するなら何×何か）。
   ※タイル数が増えると描画コストとセーブサイズが増える（現在1600タイル。80×80なら6400）
2. **タイル種別の追加** — `mountain` / `river` / `port` / `gate` / `capital` 等、必要な type と
   それぞれの通行可否・防御力・生産の有無
3. **地形の生成方法** — 完全固定マップ（毎回同じ）か、パラメータ付き手続き生成か。
   ※固定なら「三国鼎立」の地勢を確実に作れるが、リプレイ性は下がる
4. **三国鼎立の作り方** — AI10勢力を3陣営に分けるのか、地形で3領域に分かれるだけなのか。
   現在AIは同盟の概念を持たず、プレイヤーの同盟にのみ加入する（AI同士の同盟は未実装）
5. **首都の資源州** — Lv10資源を置くなら現在のLv1〜5帯から逸脱する。防御力・生産量・
   ダイヤ換算のバランスをどう置くか（`TILE_DEF_BASE × level^1.5` / `TILE_PROD_PER_LEVEL_PER_HOUR`）
6. **勝利条件** — 現在は「四隅の聖地4つのうち1つを領有」。首都州や三国構造を入れるなら
   勝利条件も見直すか（例: 首都州の制圧、3陣営の統一 等）
7. **既存セーブの扱い** — ワールドは新規開始時に1回だけ生成される。地形導入後、
   **既存プレイヤーのマップは古いまま**になる。(a)新ワールドを強制（実質リセット）
   (b)旧セーブは旧マップのまま遊べる (c)移行を試みる — どれを選ぶか

## 制約

依頼1と同じ（単一HTML・オフライン・無料・モバイル優先・非破壊マイグレーション）。
加えて、**マップ描画は canvas で毎フレーム可視範囲を描き直している**ため、
タイルごとの判定は軽量に保つ必要がある（重い経路探索を毎フレーム実行するような設計は不可）。

## 納品形式

依頼1と同じ4点＋、**地形の配置図**（テキストの模式図でよい。例: 40×40のどこに山脈・川・関所・
首都州・各陣営の領域を置くか）があると実装がぶれない。
